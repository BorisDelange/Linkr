image: node:22-alpine

stages:
  - quality
  - build
  - deploy

# ---------------------------------------------------------------------------
# Quality gate. Runs on every branch/MR.
#
# Adoption is staged because the repo carries a backlog of pre-existing lint
# and type errors (see docs/conventions.md / REVIEW-LOG.md):
#   - test:      BLOCKING — the suite is green, keep it green.
#   - lint:      non-blocking for now (allow_failure) — visible, not a wall.
#   - typecheck: non-blocking for now (allow_failure) — ~hundreds of legacy errors.
# As the backlog is burned down, flip allow_failure → false to make them hard gates.
# ---------------------------------------------------------------------------

.node_setup: &node_setup
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - node_modules/
      - apps/web/node_modules/
  before_script:
    - npm ci

test:
  stage: quality
  <<: *node_setup
  script:
    - cd apps/web && npm run test

lint:
  stage: quality
  <<: *node_setup
  allow_failure: true
  script:
    - cd apps/web && npm run lint

typecheck:
  stage: quality
  <<: *node_setup
  allow_failure: true
  script:
    - cd apps/web && npm run typecheck

build:
  stage: build
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - node_modules/
      - apps/web/node_modules/
  script:
    - npm ci
    - cd apps/web && npx vite build --base /
  artifacts:
    paths:
      - apps/web/dist

pages:
  stage: deploy
  dependencies:
    - build
  script:
    - mkdir -p public
    - cp -r apps/web/dist/* public/
    # Headers for SharedArrayBuffer (required by DuckDB-WASM, Pyodide, webR)
    - |
      cat > public/_headers <<'EOF'
      /*
        Cross-Origin-Opener-Policy: same-origin
        Cross-Origin-Embedder-Policy: credentialless
      EOF
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
