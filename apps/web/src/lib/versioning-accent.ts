/** Shared visual accent for the primary "Sync all" quick action, so it reads the
 *  same across every versioning page (project, workspace, settings, mapping project).
 *  Colored header strip (like the mapping-project Export cards) + matching icon. */
export const SYNC_ALL_ACCENT = {
  /** Header strip background. */
  headerBg: 'bg-sky-50 dark:bg-sky-950/30',
  /** Icon + title tint on the header. */
  icon: 'text-sky-600 dark:text-sky-400',
  /** Left border accent for the whole card. */
  border: 'border-sky-200 dark:border-sky-900',
}
